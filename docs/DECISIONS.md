# 의사결정 기록

### D-139 성동구 검토와 지역 규모 연동 목표·견적·공통 문서 배치 (2026-09-10)

- 사용자 승인: 이후 모든 생성에도 비교 3항목, KPI 증가량·비율, 근거 설명, 지역 규모 연동 견적 및 줄바꿈 보정을 공통 적용한다.
- 목표: 선정 환급 사업이 강진 사례와 연결된 경우 기본 최종월 목표는 25% × 80% = 20%, 그 외 5% 계획 가정. 사용자 지정 목표는 우선한다. 80%는 단계 운영을 고려한 기획 판단이지 인구·통계 보정이 아니다. 강진 2024년 1~10월 248만·전년 대비 25%의 국회도서관 회의록을 재확인했다. 사업 단독 효과·ML 대비 실적·성동구에서 재현될 효과로 주장하지 않는다.
- 예산: 월별 추가 방문 목표 합계 × 시범 직접 담당 비중 1%(계획 가정, 올림)를 참여 건수로 사용. 환급 건당 5만원/일반 혜택 1만원, 운영 인일=max(월20인일×기간, 참여건수÷25건 올림), 인일15만원, 시스템800만·홍보500만·평가300만원, 직접비10% 예비비. 모든 단가·1%·처리량은 가정이며 인구 데이터로 예측한 견적이 아니다. 사용자 참고 총액은 기존 항목 비중 배분을 우선하며, legacy 상한은 참여량과 인력을 함께 조정한다.
- 내용: 본문의 expected_effect 수치와 공통 execution_scenario를 일치시키며 관측·ML 원수치는 보존한다. 후보 작성에 환급·야간 반복 대신 제공 문서 내 다른 이용 흐름 비교를 추가했다. 실제 새 LLM 창의성 향상은 미검증이며 지원 유형·공식 사례 풀 확대는 별도 작업이다.
- PPT v17: 6장은 사업 이용 흐름/지역 소비·체류/시범 규모만 표시. 미보유 환경 비교 안내 행 삭제. 3장 사진 출처 배경 높이 확대, 4장 ML·목표 계산 설명, 8장 각 행 추가량·비율 화살표, 10장 항목/금액/초록 산출근거 열. 공통 글상자·표 셀 여백을 폰트 렌더 차이에 대비해 보정. 5장 실제 사업/유사 참고 이미지·포스터 체계 유지.
- Word v5: 동일 목표·견적, 산출근거 열, 시범 참여량 산식 표시. 실행표와 중복되는 일정 그림을 제거하고 실행표 행간을 정리해 고립된 빈 페이지 방지. 주요 사례는 PPT와 같은 report_cases 함수로 선택한다.
- 성동구 최종 검토 사본: 3개월 추가 방문 목표 2,635,826명, 시범 참여26,359건, 예상 총액1,641,420,000원. 14장 PPT와 4페이지 Word를 원본과 분리 생성. 기존 저장 보고서/ML/승인 상태를 덮어쓰지 않음. 새 전체 LLM/유료 생성 없음.
- 검증: 수치·기존 사용자 목표·견적 상한·문서 회귀39개 통과, React lint/build 통과. 설치 PowerPoint/Word로 실제 렌더링 후 핵심 슬라이드와 전체 페이지 확인. LibreOffice 부재로 Word COM→PDF→Poppler 렌더 대체 사용.

### D-137 지역 자료 준비 색상과 로컬 연결 점검 분리 (2026-09-10)

- 사용자 요청: 생성에 필요한 자료가 준비된 지역을 초록색으로 구분한다.
- 기존 `verified`(저장 기획안 품질·출력 승인)와 별도로 `generation_ready = data_ready AND 현재 Qwen/Gemma 설치·연결 확인`을 제공한다. 24시간 이내 자료 감사만 사용하며 만료/조회 실패는 초록색이 아니다. 모델 추론 성공이나 향후 오류 없음의 보장은 아니다.
- 대시보드와 공통 지역 선택기에 초록색·준비됨을 함께 표시한다. 미준비/미점검은 확인 필요로 표시하지만 대시보드 조회는 유지한다. 로컬 상태는 60초마다 재확인하며 생성 시작에서도 별도로 확인한다.
- Ollama 목록 조회 제한은 5→15초, 생성 선행 연결 실패는 무료 목록 조회만 1회 재시도한다. 지역 자료 오류와 모델 연결/설치 오류를 구분해 안내한다. 유료 생성 상한·추론 재시도는 늘리지 않는다.
- 실측: 등록 89지역의 기존 데이터 감사 조건 통과, 관악구 포함. 저장 기획안 최종 품질/출력까지 verified인 지역은 0개이며 이를 승인으로 바꾸지 않았다. 두 로컬 모델 상태 active 확인. 실제 새 LLM 기획안 생성은 실행하지 않았다.

### D-136 기획 조건 입력을 지원 범위로 제한 (2026-09-10)

- 사용자 승인: 자유 입력으로 기획안이 흔들리지 않도록 감당 가능한 항목만 남긴다.
- React 생성 화면은 `guided_v1`: 지역, 사업 방향(추천/소비·환급/숙박·체류/야간/재방문), 참고 예산 총액, 시작 월부터 3개월, 자원 300자, 제외 선택, 메모 500자만 받는다. KPI 직접 입력·최소/최대 예산 범위·확정 상한·종료일·첨부·장문 필수 조건은 제거한다.
- 서버가 enum·문자 수·충돌·3개월 날짜를 검증한다. 시작 월 미입력은 서버 현재 월로 정한다. ML 전망이 전체 기간을 포함하지 못하면 외부 사례 조사 전에 안내한다.
- 공식 사례와 설계 후보를 사업 방식 함수로 제한하며, 제외 조건은 혼합 방식에도 적용한다. 허용된 방식이 하나뿐이면 비교를 위해 다른 사업을 억지로 요구하지 않는다. 선택 후보가 조건 밖이면 다른 후보의 제목과 기존 요약을 섞지 않고 조정 안내를 반환한다. 다운로드 사례 카드에도 같은 필터를 적용한다.
- 입력 예산은 참고 견적 총액을 항목 비중으로 재배분한다. ML 수치와 목표 달성 가능성을 역산하지 않으며, 인력·운영량·실제 집행 가능성은 보장하지 않는다. 기존 저장 보고서와 legacy API 계약은 유지한다. 구형 브라우저 초안을 새 폼에서 읽을 때 숨겨진 필드는 전송하지 않는다.
- 검증은 입력/선정/견적 코드와 기존 회귀 테스트로 수행한다. 실제 LLM 생성 품질 승인과 구분한다. 유료 호출·새 라이브러리·모델 재학습 없음.

### D-135 미등록 사례의 유사 사업 웹 이미지 허용 (2026-09-10)

- 사용자 승인: 해당 사업 사진이 없으면 웹에서 찾은 비슷한 사업 사진을 사용한다. 정확히 연결된 이미지 → 사업 유형별 유사 이미지 → 일반 관광 참고 이미지 순으로 적용한다.
- 다른 지역 사진에는 `유사 사업 참고` 또는 `관광 참고 이미지`와 실제 사진 지역·기관·원문 링크를 표시한다. 사례 실적·지리 유사성 근거로 취급하지 않는다. 생성 이미지는 사용하지 않는다.
- 여수 야간관광 공식 소개 사진을 웹에서 확보하고 야간 유형에 연결했다. 다운로드한 검수 자산을 재사용하며 생성 시 Google 검색/API 호출을 추가하지 않는다. 최신 PPT 캐시 버전 v16.

### D-134 사례 슬라이드 실제 웹 이미지 사용 (2026-09-10)

- 사용자 요청에 따라 5장 AI 생성 이미지 연결을 제거했다. 강진 공식 반값여행 홍보물, 인천 섬포털 여행 사진, 정책브리핑 관광주민증 홍보물을 다운로드하여 사례 ID별로 연결한다.
- 지역/자료 종류·기관 출처·원문 링크를 표시하고 원본 비율·전체 내용을 보존한다. 다른 지역으로 생성해도 참고 사례의 사진이 붙으며, 미등록 사례에 생성 이미지나 다른 지역 사진을 대입하지 않는다.
- `case_images.py`에 원문·이미지 URL과 확인일을 기록한다. 생성 때 네트워크를 새로 호출하지 않는 로컬 자산 방식이며 모든 신규 웹 사례의 사진을 자동 확보하는 기능은 아니다. PPT 캐시 버전 v15.

### D-133 문서 운영 방식으로 사례 연결·PPT 근거 설명 개편 (2026-09-10)

- 사용자 요청: 춘천 PPT 사례 선정의 실제 알고리즘·출처, KPI·견적·실행 안내의 산식을 공개하고 사례 3열과 실행 안내 통합을 반영한다.
- 환급 기획에 전주 예산서가 연결된 오류를 확인했다. `case_recommendation` 함수로 예산 문서를 운영 후보에서 제외하고 사업 방식이 일치하는 공식 문서만 연결한다. LLM 원판단은 감사 필드에 남기며 지역 환경 유사성·정책 효과를 학습한 모델이라고 설명하지 않는다.
- 사례 수집 경로와 문서 유형·측정기간을 출처에 보존한다. PPT는 v14로 캐시를 갱신하며, 기존 저장 원문·승인·ML은 변경하지 않는다. AI 개념 이미지는 실제 장소 사진과 구분한다.
- 견적은 기존 가정 단가와 산식을 공개한다. KPI는 발표 관측값/역산/계획 가정을 분리하고 인구 보정을 했다고 주장하지 않는다. 세부 감사는 `docs/CHUNCHEON_PROPOSAL_AUDIT_20260910.md`.

### D-132 OpenAI 탭의 Agent 역할·도구·실행 흐름 설명 (2026-09-10)

- Agent 5개를 모두 OpenAI 호출로 설명하던 문구를 로컬 우선 역할 분담으로 교정한다. 병렬 조사 → Qwen 비교 → Gemma 작성 → 코드/Qwen 검수·필요 시 개정 → 통과본 OpenAI 최종 검수를 구분한다.
- 각 카드에 입력·실행 메서드·페르소나·도구·출력·task/환경변수 이름을 표시한다. 역할 지시문 첫 문단은 공개 코드에서 발췌하며 API 키·실제 접속 설정은 노출하지 않는다. 설명은 agent_learning_guide.py에서 관리한다.
- OpenAI 탭 학습 챗봇 폭은 540px에서 432px로 줄이고 좁은 화면에서는 아래에 배치한다. 생성 로직·모델 설정·호출 횟수는 변경하지 않는다.

### D-131 설명 60%·파일 트리 40% 동시 표시 (2026-09-10)

- 사용자 요청에 따라 상세 설명/담당 파일 탭을 제거하고 60:40 열로 나란히 표시한다. 트리는 스크롤 중에도 확인할 수 있도록 고정하며 좁은 화면에서는 세로 배치한다.
- 현재 파일 경로 외에 같은 폴더의 코드 파일·역할을 표시하고 현재 파일을 강조한다. 9개를 넘는 파일은 펼침 목록에서 확인한다. ML 지표와 단계 이동 시 담당 경로도 함께 갱신한다.

### D-130 구조 지도 읽기·이동 UX 개편 (2026-09-10)

- 사용자가 구조 지도 레이아웃 개선을 요청하여 D-129의 레이아웃 유지 제약을 변경한다. 기능 드롭다운·단계 직접 선택·이전/다음으로 탐색하고 상세 설명은 넓은 한 열로 읽는다.
- 서비스 전체 개요는 펼침 영역, 현재 파일 위치는 별도 탭으로 분리한다. ML 지표 선택은 현재 단계를 유지하며 해당 지표의 설명만 표시한다. 모든 지표와 호출 트리는 계속 확인 가능하다.
- 강제 자동 스크롤을 제거하고 좁은 화면은 세로 배치한다. 예측·LLM·보고서 생성에는 변경이 없다.

### D-129 전체 구조 지도에 ML 전략 연결 흐름 추가 (2026-09-10)

- 사용자 요청에 따라 기존 전체 구조 지도의 레이아웃·이전/다음 탐색·현재 파일 트리를 유지하고 기능 목록에 `ML 결과 → 전략`을 추가한다.
- D-128의 공통 `module_usage.py`에서 7단계 설명과 Target별 학습·집계·조사·출력 내용을 가져온다. 별도의 예측·재학습·LLM 호출이나 API 변경은 없다.

### D-128 ML 7개 Target의 전략 연결 설명 (2026-09-10)

- 관리자 `/ml-test`의 각 모듈에 원자료·학습·추론·기간 집계·사례 조사·Qwen/Gemma 전달·최종 출력의 7단계 설명과 호출 트리를 추가한다. `module_usage.py`가 설명을 관리하고 `learning_catalog.py`가 `strategy_usage`로 제공한다.
- 7개 지표 모두 전망과 신호로 전달되며 조사 질문은 방문/검색·숙박/체류·소비의 세 묶음이다. 방문·소비만 최종 전망 차트에 직접 표시한다. 지표에서 특정 사업을 자동 선택하는 규칙이나 LLM 내부 기여도 검증으로 설명하지 않는다. 해석 예시는 조건부 예시로 구분한다.
- 공통 학습 경로의 숙박일수 함수는 `make_univariate_supervised_frame`, 지역 예측 함수는 `predict_region_future_months`로 설명을 수정한다. ML·보고서 생성 로직·최종 PPT 양식은 바꾸지 않는다.
- 좁은 화면에서 고정 폭 챗봇이 본문을 덮는 현상은 세로 배치로 수정한다. ML 학습 챗봇에는 반복 단계/트리를 제외한 지표별 활용 설명만 전달한다. 새 유료 호출·재학습은 실행하지 않는다.

### D-127 PPT 사례 카드의 줄바꿈·오른쪽 여백 (2026-09-09)

- 사례 제목과 설명의 줄 계산 폭을 줄이고 실제 텍스트 상자는 더 넓게 확보하여 폰트 대체에 따른 오른쪽 넘침을 방지한다. PowerPoint 자동 줄바꿈도 켜고 카드 오른쪽 여백을 늘린다. 내용과 수치는 유지한다.
- 생성 캐시는 `pptx-case-wrap-v13`으로 갱신한다. 원주 실제 입력의 사례 페이지를 PowerPoint로 출력하여 카드 경계 안의 줄바꿈을 확인했고 관련 테스트 10개를 통과했다.

### D-126 방문 증가분으로 설명하는 소비 목표 산식 (2026-09-09)

- 사용자 요청에 따라 방문·소비 목표율이 같은 기본 기획안은 `월별 ML 소비액 ÷ ML 방문 수`를 기획용 비율로 구하고, `ML 소비액 + 추가 방문 수 × 해당 월 비율`로 PPT 소비 목표를 계산한다. 실제 개인별 평균 소비액이나 검증된 사업 효과로 표시하지 않는다.
- 동일 월 비율을 적용하면 월별 방문·소비 증가율은 수학적으로 같다. 3개월 합계는 월별 가중치가 달라 다를 수 있다. 원주 고정 입력은 방문 3.39%, 소비 3.49%이며 기존 목표식과 동치다. 비율·산식·합계 증가율 설명을 KPI 슬라이드 회색 하단에 표시한다.
- 명시적으로 서로 다르게 지정한 방문·소비 목표율은 그대로 각각 적용한다. 방문이 0인 월의 소비/방문 비율은 만들지 않고 기존 명시 목표식으로 계산한다. 저장 보고서·ML·승인은 변경하지 않는다. 웹·Word 기존 목표값과 수학적으로 일치한다.
- PPT 캐시 버전은 `pptx-visitor-spending-formula-v12`. 실제 PowerPoint에서 KPI 슬라이드와 그래프를 출력하여 레이아웃을 확인한다.

### D-125 PPT 양식 유지와 사례 선정 근거·차트 겹침 수정 (2026-09-09)

- 사용자가 승인한 현재 PPT 양식을 생성·다운로드 공통 렌더러에서 유지한다. 사례 소개 뒤에 선정 근거 1장을 추가하여 연결된 공식 사례의 운영 방식, 기획 지역 관측값, 적용한 부분과 출처를 표시한다. 환경 유사성·인구·교통 유사성 또는 같은 성과율을 입증했다고 주장하지 않는다.
- 월 표시와 범례가 겹치는 문제는 범례를 차트 밖의 별도 줄에 배치하고, 짧아진 차트 안에서 PowerPoint가 축 여백을 계산하도록 수정한다. 수치는 수동 좌표 텍스트 대신 그래프 점에 연결되는 네이티브 데이터 레이블로 표시한다. 전망은 아래, 목표는 위에 놓고 하단 축 여백을 확보한다.
- 출력 버전을 `pptx-idea-case-basis-v11`로 올려 이전 캐시가 새 출력에 재사용되지 않게 한다. 저장 보고서·ML·목표 산식·기존 견적은 변경하지 않는다. 원주 저장 보고서로 실제 PowerPoint PNG 출력과 새 페이지 순서를 확인한다.

### D-124 사례 기반 아이디어 제안과 조정 가능한 목표·견적 (2026-09-09)

- 사용자 승인: 정상 결과의 검토용 초안·보완 경고·세부 준비 목록을 짧은 챗봇 편집 안내로 대체한다. 내부 검수/승인 값은 보존하며 품질 승인으로 바꾸지 않는다. 실제 생성 실패는 숨기지 않는다.
- 현 보고서의 근거 연결 후보만 설명·비교한다. 임의의 고층 건축·공원 신설은 범위 밖으로 안내한다. 후보 수는 실제 데이터로 계산하며 고정 4개를 꾸며내지 않는다.
- 명시한 목표가 없으면 최종월 방문·소비 각각 +5%의 조정 가능한 기획 가정을 제공한다. ML 전망은 변경하지 않고 월별로 목표까지 단계 적용한다. 사용자가 입력한 목표가 우선한다.
- 강진 사례 근거: 강진군의회 제307회 본회의 2024-11-20 시정연설은 10월 말 관광객 약 248만, 전년 대비 25% 증가를 발표했다. 전년 198.4만/차이49.6만은 역산값이다. 반값여행·축제의 동시기 실적이며 인과효과/ML 잔차가 아니다. 5%는 초기 시범 기획 판단이며 소비5%는 별도 가정이다. 다른 지역이나 다른 사업에 강진 실적을 자동 인용하지 않는다.
- 출처: https://clik.nanet.go.kr/minutes/viewer.do?DOCID=CLIKC3524169218564260&collection=minutes
- 견적은 사용자 승인에 따라 항목별 임시 단가와 총액을 제안하며 실제 계약액과 다를 수 있음을 표시한다. 챗봇의 수치 수정은 로컬 결정식 처리로 유료/로컬 LLM 추가 호출이 없다.
- 웹·Word·PPT는 같은 목표/견적 변환을 사용한다. 과거 DB 보고서와 ML은 수정하지 않는다. D-091의 세부 경고 노출과 기존 목표 미입력/견적 미확정 표시 정책은 이 승인 범위에서 변경한다.

### D-123 기획 조건 전체 입력량의 생성 전 제한 (2026-09-09)

- 기획안 생성 화면의 개별 자유 입력과 첨부문서는 각각 제한되어 있었지만, 모두 채우면 본문 합계가 약 2만 5천 자까지 늘어날 수 있었다. 지역·사례 근거가 긴 보고서에서는 이 입력이 로컬 모델의 문맥 한도를 다시 넘길 수 있으므로 사용자 자유 입력과 첨부 본문 합계를 6,000자로 제한한다.
- React는 현재 합계와 한도를 항상 표시하고 첨부 직후 및 생성 직전에 안내한다. API의 `PlanningBrief`도 같은 합계를 검증하여 브라우저 검사를 우회한 요청이 LLM 작업으로 등록되지 않게 한다.
- 확정된 시설·인력, 반드시 지킬 조건, 공개 데이터에 없는 현장 사실을 우선 입력하도록 안내한다. 목표율·희망 예산·희망 기간·선호·참고문서는 실제로 정해진 내용이 있을 때만 쓰며, 미정 상태는 유효한 입력으로 유지한다.
- 사용자 텍스트와 첨부 본문은 기존처럼 검증 전 참고 데이터로 취급하며 그 안의 명령문을 따르지 않는다. 공식 수치·ML·RAG 근거와 섞지 않는다.

### D-122 Gemma 초안의 과도한 출력 예약과 중복 전국 거시표 제거 (2026-09-09)

- 상태: 실제 실패 job `00dbdf36407645f5b47ddc51b4ff2354`를 확인하고 구현·문맥 사전검사 완료.
- 원인: 5176·8100·8112는 모두 실행 중이었고 Qwen/Gemma 상태도 active였다. 실패는 서버 누락이나 1,800초 timeout이 아니라, 후보 보완 뒤 길어진 근거 입력에 Gemma 초안 출력 16,000토큰을 미리 예약해 40,960 문맥을 넘긴 `OLLAMA_CONTEXT_BUDGET_EXCEEDED`였다. 과거 정상 원주 Gemma 초안의 실제 최종 출력은 1,285토큰이었다.
- 결정: Gemma 초안·개정의 로컬 출력 예약을 10,000토큰으로 통일한다. 후보 비교에서 이미 읽은 전국 거시 참고표는 본문 조립 단계에 중복 사전전달하지 않는다. 지역 관측, 지역 관광 현황, 전국 지역 비교, ML, 선택 후보와 공식 사례 원문은 유지한다. JSON 미완료 1회 재생성·Schema·출처 검증도 유지한다.
- 검증: 과거 실제 원주 보고서 크기로 본문 요청을 재구성한 결과 보수적 입력 23,783 + 출력 10,000 = 33,783토큰이며 40,960 문맥에서 7,177토큰 여유가 남았다. 사용자 승인 후 실제 LAN Gemma 초안을 실행해 64.164초, 입력 34,476 / 출력 1,764토큰으로 정상 완료했다. 같은 초안의 검수 지적 5건을 전달한 Gemma 개정도 23.606초, 입력 18,250 / 출력 1,349토큰으로 완료해 문맥 오류가 재현되지 않았다.
- 실제 내용 검토: 개정은 실행 단계 담당 누락 4건을 고쳤다. 남은 KPI 1건은 `수집방법: 사업 담당자 집계 원장`을 검사가 인식하지 못한 오판이어서 원장/수집방법을 유효한 원자료 표현으로 보완했다. 초안이 정량 사용 보류 사례의 `강진 소비 118.7억 원·1.7배 증가`와 기준선보다 약한 ML 41.26%를 다시 사용한 것은 품질 개선으로 인정하지 않는다. 전자는 새 결정적 검사로 차단하고 후자는 기존 ML 신뢰성 검사가 차단한다. 관련 unittest 114개 통과.
- 실행 장치 확인: 개발 PC의 `LOCAL_LLM_BASE_URL`은 localhost가 아니라 LAN Ollama 서버를 가리킨다. Ollama 0.33.3의 `/api/ps`는 Gemma4:26b에 `size_vram=1,023,903,005`를 보고해 GPU 오프로딩은 확인되지만 GPU 제품명은 제공하지 않는다. 개발 PC에는 `nvidia-smi`가 없으므로 RTX 4090 모델명은 Ollama 서버 노트북에서 별도 확인한다.

### D-121 후보 보완·본문 개정의 중복 문맥 제거와 직접 실행 저장 설정 통일 (2026-09-09)

- 상태: 실제 원주 전체 생성 trace의 두 실패 원인을 확인하고 구현·실제 Qwen 재생·회귀 검증 완료.
- 문제: 최초 Qwen 후보 생성은 완료됐지만 후보 보완과 Gemma 본문 개정은 각각 이전 후보·초안과 검수 오류를 추가한 뒤, 전국 거시 참고표·사례 비교표·선택 도구 왕복까지 다시 포함해 `OLLAMA_CONTEXT_BUDGET_EXCEEDED`로 중단됐다. 별도 PowerShell에서 AI 서버를 직접 실행하면 LLM 설정은 `.env`를 읽지만 MySQL 저장소는 프로세스 환경만 읽어 완성 보고서 저장이 실패했다.
- 결정: 오류 목록은 개요에 개수와 경로만 알리고 최종 작성 직전에 원문을 한 번 전달한다. 후보 보완·본문 개정에서는 지역 관측·지역 현황·전국 지역 비교·ML·이전 판단·관련 사례 원문·기존 초안을 필수 사전조회로 유지하되, 최초 조사용 전국 거시 참고표와 사례 비교 요약표는 중복 사전조회하지 않는다. 필수 조회가 완료된 보완 호출은 선택 도구 왕복 없이 최종 Schema 작성으로 진행한다. 임의 웹·파일·DB 도구 권한과 필수 출처 원문 확인은 그대로 유지한다.
- 저장: `strategy_store`도 공통 `load_project_env`를 사용해 IDE/직접 uvicorn 실행과 `start-dev.ps1`가 같은 MySQL 설정 우선순위를 적용한다. 비밀번호는 로그나 응답에 노출하지 않는다.
- 실측 사전검사: 저장된 원주 보고서와 현재 공식 사례 6건으로 후보 보완 최종 요청을 동결했다. 보수적 추정 입력 26,788토큰 + 출력 예약 8,000토큰 = 34,788토큰으로 Qwen 40,960 문맥 안에 들어오며 6,172토큰 여유가 남았다. 이는 응답 품질 승인 결과가 아니며 실제 로컬 응답과 후속 검수는 별도로 확인한다.
- 실측 결과: Qwen은 210.235초, 입력 18,326 / 출력 3,108토큰으로 완전 JSON을 반환해 컨텍스트 장애가 해소됐다. 다만 지역 전체 방문객 기반 환급 예산, 검색량 기반 적합성 추론, 출처 없는 전국 평균 비교와 임의 문턱값이 남아 원문은 품질 미달이었다. 이 세 유형을 추가 검사하고, 자동 보정 시 시범 수량 기반 임시 예산·후보 유형별 분자/분모·순차 도입 비교·발전 가능성 시나리오로 바꾸되 needs_evidence를 유지한다. 전체 AI unittest 349개 통과.
- 후속 로컬 재생도 컨텍스트 오류 없이 320.685초에 최종 작성과 사례 인용 교정을 마쳤다. Qwen 원문은 7건이 남아 승인하지 않았고, 서버 보정 문구와 검사의 `원자료` 어휘 불일치 2건을 수정했다. 같은 지역 전체 환급 산식 검사를 후보뿐 아니라 Gemma 본문 예산에도 적용한 뒤 전체 AI unittest 349개를 다시 통과했다. 이 변경 뒤 전체 보고서 GPU 재생성은 다음 검증으로 남는다.

### D-120 Qwen 지역 적용 JSON의 실측 출력 공간 확대 (2026-09-09)

- 30분 로컬 timeout을 적용한 원주 전체 생성 `72d058743f064c39b0b7af767efcb575`는 시간 초과 없이 약 1,199초 실행됐지만, Qwen의 최초 최종 JSON과 1회 재생성이 각각 정확히 5,600 출력 토큰에서 잘려 `OLLAMA_INCOMPLETE_RESPONSE`로 실패했다. 미완성 보고서는 저장하지 않았다.
- 두 최종 시도의 실제 입력은 28,279·28,347토큰이었다. Qwen 문맥 40,960 안에서 근거·Schema를 그대로 유지하면서 완전 JSON을 끝낼 수 있도록 운영 Transferability의 로컬 출력 상한만 8,000토큰으로 늘린다.
- 과거 5,600토큰 동결 비교 진단, 로컬 호출 횟수와 1회 미완료 재생성, 30분 timeout, OpenAI 최대 호출·유료 fallback 정책, 후보 품질 검사와 승인 기준은 유지한다. 출력 공간 확대 자체를 품질 개선으로 판단하지 않는다.
- 관련 계약 테스트 후 AI Server를 재시작하고 동일 원주 입력으로 전체 연결과 실제 응답 내용을 다시 검증한다.

### D-119 로컬 Qwen·Gemma 장시간 실행 허용 (2026-09-09)

- 사용자는 비용이 발생하는 OpenAI 호출 제한은 유지하되, 팀 노트북의 Qwen·Gemma는 오래 걸려도 충분히 기다리도록 승인했다.
- `LOCAL_LLM_TIMEOUT_SECONDS`와 미설정 기본값을 요청당 1,800초(30분)로 늘린다. 운영 Agent와 명시 실행 진단 도구가 같은 값을 사용하며 관리자 화면에도 분 단위로 표시한다.
- OpenAI 최대 3회, 유료 자동 fallback 금지, OpenAI 요청 timeout·토큰 상한, 로컬 출력 상한·문맥 길이·호출 횟수와 품질 승인 기준은 변경하지 않는다. 로컬 서버가 응답을 반환하지 않으면 30분 뒤에는 명시적으로 실패해 무한 대기는 막는다.
- AI Server 재시작 후 React `/ai` 프록시에서 timeout 1,800초, Qwen·Gemma active, 원주 `51130`의 `ready/can_generate=true`를 확인했다. Backend 8100·AI 8112·React 프록시는 모두 HTTP 200이며 관련 unittest 63개와 프런트 production build, Python 구문·diff 공백 검사를 통과했다.

### D-118 전체 생성의 로컬 LLM 시간 초과 여유 확대 (2026-09-09)

- 원주 전체 생성 작업 `8cd0daba9f5e40ebb066618ab27a33f0`은 AI·Backend·Ollama 연결이 모두 정상인 상태에서 지역 적용 가능성 검토의 개별 Qwen 요청이 기존 420초 제한을 넘어 실패했다. 작업은 11:49:54~12:05:41에 실행됐고 MySQL에는 실패 상태와 `OLLAMA_TIMEOUT` 사용자 메시지만 남았으며 미완성 보고서는 저장하지 않았다. 점검 당시 8111에는 여러 날짜에 시작한 개발 서버 리스너가 겹쳐 있어 오래된 환경 설정과 중복 복구 작업 가능성도 있었다.
- 이전 정상 전체 생성도 12~15분, 실제 Qwen 적용성 호출도 약 3~6분이 걸렸으므로 LAN GPU의 모델 적재·프롬프트 평가 변동을 수용하도록 `LOCAL_LLM_TIMEOUT_SECONDS` 기본값과 개발 설정을 600초로 조정한다.
- `start-dev.ps1`은 각 포트의 기존 리스너를 먼저 확인하고 이미 실행 중이면 새 서버 창을 만들지 않는다. 호출 횟수·출력 토큰·문맥 길이·모델·유료 fallback·품질 검사·승인 기준은 바꾸지 않는다. 제한 확대는 느린 완전 응답을 기다리는 실행 안정성 조치이며 품질 향상이나 성공 보장이 아니다.
- 현재 Windows 세션 밖에 남은 8111 리스너는 작업 환경에서 종료할 수 없고 구버전 서버가 요청을 받을 수 있어, D-034의 TP2-3 개발 AI 포트를 8112로 대체하고 Vite 프록시·실행 스크립트·진단 CLI·구조 설명을 함께 갱신한다. 배포의 Nginx `/ai`와 AI Server 8001 계약은 변경하지 않는다.
- 검증: React 5176의 실제 `/ai` 프록시에서 `local_llm_timeout_seconds=600`, Qwen·Gemma active, 원주 `51130`의 `ready/can_generate=true`를 확인했다. 8112 리스너는 1개이며 관련 unittest 89개, 프런트 production build, Python·PowerShell 구문과 diff 공백 검사를 통과했다.

### D-117 프로젝트 구조 지도의 팀 학습용 설명 강화 (2026-09-09)

- 구조 지도를 단순 파일 목록이 아니라 `전체 시스템 책임 경계 → 기능별 실행 단계 → 단계별 입력·담당 파일·출력 → 함수·데이터·판단 기준 → 현재 파일 위치` 순서로 읽는 팀 학습 화면으로 정리했다.
- 큰 제목과 카드 여백을 줄이고 설명 본문은 읽을 수 있는 크기를 유지했다. 기능 흐름과 전체 파일 트리는 탭으로 분리해 한 화면에서 서로 밀어내지 않도록 했다.
- 상단 파이프라인과 사례 적용 판단은 별도 이미지 파일 대신 반응형 HTML/CSS 인포그래픽으로 표시한다. 실제 코드·설명 변경과 함께 갱신할 수 있고 확대해도 흐려지지 않는다.
- 파일 역할 사전에는 중앙 라우트 계약 `frontend/src/routes.js`를 추가하고, `App.jsx`, `start-dev.ps1`, 구조 탐색기의 역할을 현재 코드와 맞췄다.
- 검증: Python 구문, 트리 CLI, 모든 기능 단계의 상세 설명·파일 경로 대응, Streamlit health와 Chrome LAN 화면의 기능별 구조·전체 트리 탭을 확인했다.

### D-116 프로젝트 전체 구조 화면 실행 누락 복구 (2026-09-09)

- `/project-tree` React route와 lazy loading은 정상이나, 화면 본문이 별도 Streamlit `8501`을 iframe으로 읽는 구조에서 `start-dev.ps1`이 해당 서비스를 시작하지 않아 연결 거부와 빈 화면이 발생했다.
- Streamlit이 설치된 개발 환경에서는 `start-dev.ps1`이 Backend·AI Server·Frontend와 구조 탐색기를 함께 시작한다. 미설치 환경은 기존 세 서버를 중단하지 않고 별도 요구사항 설치 안내를 출력한다. 새 패키지를 자동 설치하지 않는다.
- 기본 iframe 주소는 고정 `localhost` 대신 현재 React URL과 같은 호스트의 포트 `8501`로 계산한다. 따라서 팀원이 개발 PC의 LAN 주소로 접속해도 팀원 자신의 localhost로 잘못 연결되지 않는다. 명시한 `VITE_PROJECT_TREE_URL`은 계속 우선한다.
- 검증: localhost와 개발 PC LAN 주소의 `8501/_stcore/health`가 모두 200을 반환했다. 실제 Chrome의 LAN `/project-tree`에서 Streamlit iframe, 기능 선택, 실행 흐름, 현재 파일 위치와 전체 폴더 트리가 표시되는 것을 확인했다. 프런트 계약 11개, oxlint, production build, PowerShell 구문, 트리 CLI와 diff 검사를 통과했다.

### D-115 Ollama 복구 후 원주 fresh 실측과 사례 복사 차단 (2026-09-09)

- `/ai/v1/llm/status`에서 Qwen3:14b와 Gemma4:26b가 모두 active인 것을 확인했다. 유료 provider를 실패 처리하고 보고서 저장·승인을 막은 원주 저장 보고서 `0cb052838f0d4ab983891649fdf6a47b`의 `--fresh` 진단만 1회 실행했다.
- Qwen 진단 기록은 226.146초, 입력 59,994 / 출력 3,005토큰이었고 완전한 JSON을 반환했다. 원문 후보 계약은 9건을 지적해 `candidate_contract_passed=false`, `report_approved=false`였다. 연결과 구조화 응답은 정상이나 실제 기획 품질은 통과하지 않았다.
- 원문을 직접 검토하니 강진 반값여행을 원주 사업명·대상 이용자에 복사하고, 전주시 야간관광 예산 편성을 관광사업 후보로 제시했다. 검색량을 야간 콘텐츠 관심으로 해석하고 타지역 사례 성과를 비교집단으로 삼았으며, 근거 없는 10%·5% 확대/중단 문턱도 남았다.
- 서버 보정은 타지역 사례명이 선택 지역의 제목·대상·범위에 남으면 원주 조건부 시범으로 지역화한다. 다만 작동 방식 자체가 이용·예약·결제·체험 없이 예산 편성만 설명하면 안전한 제목으로 덮지 않고 제목과 mechanism을 모두 검수 항목으로 남긴다. 사례 효과나 확정 예산은 만들지 않으며 `needs_evidence`를 유지한다.
- 진단 출력은 모델 원문 검사와 서버 보정 후 검사를 분리해 `stabilized_candidate_contract_passed`, `stabilized_issues`, `automatic_corrections`, `stabilized_decision`을 기록한다. 이 값도 의미 품질이나 보고서 승인을 뜻하지 않는다.
- 보정 코드 적용 뒤 동일 원격 Ollama 실측 재실행은 내부 지역 자료와 공식 문서 근거를 원격 Ollama로 보내는 네트워크 반출 승인이 없어 자동 승인 검토에서 차단됐다. 결정적 계약과 전체 회귀만 검증했으며 유료 호출·보고서 저장·기존 ML·승인 상태는 변경하지 않았다.
- 검증: AI 서버 전체 unittest 346개, 전국 데이터 파이프라인 unittest 39개, 후보·라우팅·학습 카탈로그 중심 unittest 44개가 통과했다. 실제 보정 후 Qwen 응답 품질은 별도 승인된 재실행 전까지 미검증 상태다.

### D-114 React/Vite 화면 경로 계약 명시 (2026-09-09)

- React 19와 Vite 8의 진입점, 페이지 단위 lazy loading, `/api`·`/ai` 개발 프록시를 유지한다. 현재 화면 수에서는 새 Router 라이브러리를 설치하지 않는다.
- 공개 경로를 `frontend/src/routes.js` 한 곳에 명시하고 `/diagnosis`·`/proposal`만 과거 주소 별칭으로 유지한다. trailing slash를 정규화하며 알 수 없는 주소는 대시보드로 잘못 표시하지 않고 404 안내를 보여 준다.
- WorkspaceShell도 canonical 경로를 사용해 과거 주소와 trailing slash에서 활성 메뉴가 일치한다. 직접 URL 진입은 Vite SPA fallback으로 확인하며, 운영 배포 시 Nginx의 `try_files $uri /index.html` 검증은 별도 배포 단계에서 수행한다.
- 검증: 프런트 Node 계약 11개, oxlint, production build를 통과했다. Vite 개발 서버에서 공개 경로·별칭·trailing slash와 알 수 없는 경로까지 14개 직접 URL이 SPA 진입점을 반환했고, 알 수 없는 경로는 클라이언트 404 계약으로 분기된다. React 학습 카탈로그도 `routes.js`를 읽도록 갱신했다.

### D-113 완료 작업 복구와 화면 무한 로딩 차단 (2026-09-09)

- 최종 생성 흐름 점검에서 서버 재시작 후 작업 상태는 `completed`지만 저장 보고서가 없거나 현재 Schema로 검증되지 않는 경우, React가 `completed + report 없음`을 처리하지 못해 계속 폴링하는 경로를 확인했다.
- 저장 보고서를 복구할 수 없는 완료 작업은 서버에서 재시도 가능한 `failed` 상태와 사용자 안내로 정규화한다. 브라우저의 오래된 작업 ID가 404이면 해당 ID를 지우고 같은 입력으로 다시 생성하도록 안내한다.
- 생성 본문을 받은 뒤 브라우저 localStorage 저장만 실패한 경우에는 서버 결과를 화면 메모리에 유지해 검토·서버 저장·문서 다운로드를 계속할 수 있게 한다. 일시적인 네트워크 조회 실패는 기존처럼 작업 ID를 보존하고 재조회한다.
- 검증: AI 서버 전체 unittest 344개, 전국 데이터 파이프라인 unittest 39개, 프런트 Node 계약 3개와 production build, 작업 상태 API의 404 전달 계약, Python 구문 및 diff 검사를 통과했다. 실행 중 Backend·AI·Frontend health와 원주 생성 준비 상태 `ready`도 읽기 전용으로 확인했다. lint는 성공했으며 기존 effect 경고 2건이 남아 있다. 현재 `local_first`에서 OpenAI 연결만 active이고 Qwen·Gemma용 Ollama는 inactive라 실제 새 생성은 실행하지 않았다. LLM 연결 장애 자체를 성공으로 바꾸거나 미완성 결과를 승인하지 않는다.

### D-112 후보 오류의 서버 보정과 검토용 본문 보존 (2026-09-09)

- 사용자 요청에 따라 기획서 생성의 기술 실패와 내용 보완 상태를 분리한다. D-098의 모든 후보 작성 문제를 HTTP 422로 끝내던 범위를 좁히고, 미등록 출처 ID·타지역 운영 범위 복사·확정처럼 보이는 총예산·측정 분모 누락처럼 규칙으로 안전하게 고칠 수 있는 값은 서버 계약으로 한 번 보정한다.
- 서버 보정은 새 사례·성과·확정 예산을 만들지 않는다. 예산은 수량×미확정 비교견적 산식, 측정은 승인 참여 ID 대비 취소·중복 제외 완료 ID와 수집 원장으로 바꾸고 `selection_status=needs_evidence` 및 보정 기록을 유지한다. 지역 적합성·후보 차별성처럼 의미 판단이 필요한 문제는 Planner에 전달해 검토용 본문을 보존하되 major 검수와 82점 승인 기준을 완화하지 않는다.
- 기계적으로 복구할 수 없는 critical 근거 오류만 본문 작성을 차단한다. 화면은 자동 보정 수를 먼저 알리고 전체 상세·준비 자료는 접힌 상태로 제공한다. 오류를 숨기는 변경이 아니라 생성 실패와 담당자 확인 사항을 구분하는 변경이다.
- 학생 절약/로컬 경로는 OpenAI 키가 없다는 이유만으로 오케스트레이션 시작 전에 차단하지 않는다. 실제 OpenAI 단계가 필요할 때 Provider가 상태를 판정하며, 작성된 초안이 있으면 기존 정책대로 미승인 상태로 보존한다. 새 라이브러리·유료 호출·보고서 승인·기존 저장 보고서 변경은 없다.
- 검증: 후보·오케스트레이터 중심 64개와 환경 의존 진단을 제외한 확장 회귀 235개가 모두 통과했다. 별도 candidate replay 2개는 현재 테스트 Python에 FastAPI가 없어 `ai_server.app.main`을 로드하지 못했다. 프런트 production build와 diff 검사 통과, lint는 기존 effect 경고 2건만 남았다. 실제 Qwen/Gemma 생성 품질은 로컬 서버 복구 후 별도로 확인한다.

### D-111 다지역 사례 적용 진단의 지역 근거와 적용 차이 보존 (2026-09-09)

- 사용자 방향에 따라 사업 필요성 단정보다 유사 타지역 사례에서 어떤 운영 장치를 가져오고, 선택 지역에서 무엇을 바꾸거나 제외하며, 시범으로 어느 범위까지 발전 가능성을 확인할지를 우선한다. 임시 예산·측정 계약은 D-110과 같이 서버가 보존한다.
- 강남 저장 보고서 `81a10a4c42b84fd89410acb73e984fec`의 실제 본문과 검수를 읽기 전용으로 대조했다. 이 보고서는 hybrid 실행에서 로컬 Qwen/Gemma가 문맥 한도를 넘겨 OpenAI fallback으로 작성됐고 최종 74점·미승인이었다. 인천 사례의 야시장·공연·상권쿠폰·투어 중 강남에서 적용·축소·제외할 요소가 최종안에 충분히 남지 않았고 KPI 비교 분모도 불명확했다.
- 별도 지역 반복 입력은 원주 자료의 지역명만 바꾸지 않는다. `regional_context`에 선택 지역의 등록 dataset 요약과 source ID, 데이터랩 인기장소, 관광공사 Open API 관광자원을 축소 전달한다. 관측값은 필요성·인과효과가 아니라 적용 방향과 추가 확인 범위에만 쓴다. 장소 자료는 등록명·주소·순위만 보증하고 야간 운영·협약·예약 가능성은 제안 조건으로 둔다.
- 강남처럼 저장된 공식 야간사례 요약이 있으면 공통 야간관광 특화도시 카드와 함께 사용하고, 없으면 검수된 밤밤페스타 카드로 대체한다. 요약 카드 밖의 세부 절차·성과 산식은 보충하지 않는다. 출력은 사례별 적용 장치·지역 변경·제외/미확인·시범 학습 범위를 분리하고, 요청에 있던 사례 ID와 지역 dataset/장소 ID만 허용한다.
- 결정적 계약 결합의 해석 한계에 남아 있던 `원주시 전체` 고정 문구도 계약의 실제 지역명으로 바꿨다. 운영 Agent·보고서·ML·승인 상태와 유료 호출 경로는 변경하지 않는다.
- 강남 독립 입력 `candidate_comparison_input_b750ae3144f644e9a7879e88491e9c03.json`에서 새 준비 요청을 만들었고, `candidate_pilot_contract_9db7528779e0446392435fcf2659766b.json`에 보존했다. Qwen 연결은 승인된 읽기 전용 상태 확인에서도 10초 타임아웃이므로 새 응답의 내용 품질과 다지역 반복성은 아직 통과로 판단하지 않는다.
- 지역 입력·사례 선택·출처 대조·기존 회귀를 합친 unittest 129개와 Python 컴파일·diff 공백 검사가 통과했다. 테스트 통과는 새 Qwen 응답의 내용 품질 승인이 아니다.

### D-110 사례 기반 발전 가능성과 임시 추정 예산 우선 (2026-09-08)

- 사용자 방향: 지역 수치로 사업 필요성을 증명하기보다 유사 타지역 사례를 비교해 원주에서 더 기획할 내용과 발전 가능한 범위를 제시한다. 예산은 가정 수량·단가·소계가 보이는 임시 추정을 허용한다.
- 발전 가능성은 보장된 방문/매출 증가율이 아니라 참여업체·실제 이용완료·운영일·상품권 재사용액처럼 시범에서 관리 가능한 목표로 작성한다. 타지역 성과율과 총예산은 복사하지 않는다.
- 실제 비교: 두 방향 통합 요청은5,600토큰 미완료. 한 시범 방향으로 줄이자 야간 사례 비교, 2개 콘텐츠·5개 업체·10일, 임시550만원, 업체5개·이용100건 목표까지 생성했다.
- 판단: 방향과 산식은 부분 개선됐지만 미확인 장소, 관광객 이용 흐름 누락, 기타 비용, 불완전 원장이 남았다. 운영/Gemma에 반영하지 않고 `CANDIDATE_FEASIBILITY_20260908.md`에 증빙한다.
- 후속 분할 검증: 평가용 계약(콘텐츠2개·10일·업체5개·유효이용100건, 임시1,750만원)을 서버가 보존하고 LLM은 사례 비교/설명만 작성하게 했다. Gemma 본문 연결과 Qwen 검토까지 완료했지만 Gemma의 총액 누락·실시간/시스템 구축 확대를 Qwen이 승인했다. 결정적 재검수가 최종 미승인으로 판정했다.
- 결론: 사례 비교 중심의 분할 구조와 서버 소유 비용/측정 계약은 유효한 방향이나, 이번 결과만 운영에 반영하지 않는다. 다지역 독립 입력 반복성과 운영 통합은 별도 검증한다. 상세 증빙은 `CANDIDATE_FEASIBILITY_20260908.md`.

### D-109 후보 지역 적합성·예산·측정의 독립 비교 진단 (2026-09-08)

- 사용자 요청으로 남은 다섯 품질 항목 검증을 재개한다. 우선 D-107 원주 동결 근거를 보존한 채 두 후보의 지역 적합성·이용 절차·수량/단가/소계·측정 설계를 구조화해 로컬 단회 진단한다.
- 기존 승인 작성 예시 유무만 비교한다. 운영 분할 호출·프롬프트·검사·모델·저장/승인 상태는 바꾸지 않고 유료 호출·설치·파인튜닝은 하지 않는다.
- 숫자 산식은 Python Decimal로 확인하지만 운영량·단가 근거·지역 적합성·측정 의미를 실제 답변에서 별도로 검토한다. 산식/Schema 통과가 승인으로 바뀌지 않는다.
- 첫 응답은 타지역 자료 제목/예산 항목을 후보와 담당자로 복사하고, 예산서를 이용자 수 원장으로 잘못 지정했다. 4억원 합계가 맞아도 운영량10억원과 모순이므로 품질 미달이다.
- 후보가 미달인 상태에서 Gemma 본문·최종 승인으로 진행하지 않는다. 전체 연결·별도 지역 반복성은 미검증으로 남기며, 실행 상세와 비교 결과는 `CANDIDATE_FEASIBILITY_20260908.md`에 기록한다.
- 비교 완료: 예시 없음111.049초, 포함154.084초. 예시 포함은 원주 출처/환급 이용 절차를 보완했으나 예시 모방, null 비용, 부정확한 분자·분모와 원장, 사업 효과 자료 선요구가 남았다. 두 응답 모두 품질 미달이며 운영 반영 없음. 진단 관련 unittest12개 통과는 의미 품질 승인이 아니다.

### D-108 후보 이용 흐름 단일 과제와 작성 예시 검토 (2026-09-08)

- 후속 진행 요청으로 명시 실행 진단에서만 과제를 후보 하나의 이용 흐름으로 좁힌다. D-107의 근거 본문은 유지하고 지시·출력 Schema를 바꾼다. 운영 분할 호출·검사·프롬프트 변경은 아니다.
- 예시 없는 짧은 요청은 31.413초에 응답했지만 예산 편성형 후보와 추상적 실행 흐름을 반복했다. Schema 통과·짧은 시간은 품질 개선을 뜻하지 않는다.
- 작성 예시 초안을 별도로 마련했으며 현재 사람 검토 대기다. 검토 전 예시 입력은 진단에서 거절한다. 사용자의 예시 내용 확인 전에는 사람 검토 완료 표시나 예시 포함 GPU 비교를 하지 않는다.
- 기록: `CANDIDATE_FLOW_EXPERIMENT_20260908.md`. 보고서·ML·유료 경로·운영 호출 상한은 유지한다.
- 후속: 사용자가 작성 예시 사용을 승인해 같은 요청에 예시 메시지만 추가한 GPU 비교를 1회 실행했다. 53.163초, 입력 16,411/출력 727토큰. 원주 출처 연결과 역할별 단계는 보완됐지만 관측→필요성 단정, 모호한 이용 절차, 예산서의 운영 근거 오용이 남아 품질 미달로 기록한다. 운영에는 반영하지 않는다.

### D-107 동일 최종 입력의 Qwen 추론 모드 비교 진단 (2026-09-08)

- 사용자 요청: 이전 실측과 현재 코드를 대조하고 기존 범위에서 가장 작은 비교 실험을 진행한다. 파인튜닝·설치·유료 호출·운영 호출 증가는 승인 범위에 포함하지 않는다.
- 결정: 명시 실행 CLI에만 `--compare-thinking`을 추가한다. 필수 근거 조회 후 첫 최종 메시지·Schema·모델·출력 상한을 고정하고 `think=false/true`를 각 1회 비교한다. 도구 선택은 기존 false이며 두 실험군이 공유한다. 최종 응답 이후 자동 교정·추가 원문 조회·본문 생성은 하지 않는다.
- 증빙: 입력 전체와 SHA-256, 준비 단계/실험군별 시간·토큰, 응답 본문, Schema·출처 조회·작성 검사를 보존한다. 사고 과정 원문은 저장하지 않는다. 작성 검사 통과와 의미 품질/보고서 승인을 구분한다.
- 경계: 운영 provider와 프롬프트·검사 기준·호출 한도·ML·저장 보고서·승인은 변경하지 않는다. 새 예시 학습이나 분할 생성은 이번 실험에 포함하지 않는다. 원주 한 입력·각 1회이므로 반복성·다지역 일반화는 검증하지 않는다.
- 결과·한계는 `LOCAL_CANDIDATE_REPLAY_20260908.md`의 후속 실측에 기록한다.
- 실측 완료: false 252.527초/true 194.286초, 둘 다 완전 JSON이나 실제 예산·측정·사업 설계 오류가 남았다. true는 needs_evidence를 표시했지만 품질 개선을 입증하지 못해 운영 think=False를 유지한다. 관련 unittest 104개와 후속 진단 4개 통과. 저장 보고서·ML·출처·여건은 보존했다.

### D-106 발표용 전체 구조 탐색기와 관리자 마지막 탭 (2026-09-08)

- 사용자 요청에 따라 기존 D-039의 `별도 Streamlit 서버를 추가하지 않는다` 결정을 대체한다.
- `project_tree_explorer/`를 별도 폴더로 만들고, 읽기 전용 CLI 트리 출력기와 Streamlit 탐색기를 둔다. 기본적으로 `.git`, `node_modules`, Python 가상환경, 캐시·빌드 폴더는 접고 원본·문서·모델·소스 경로는 보여 준다.
- 탐색기는 전체 트리 검색, 파일 역할 설명, 앱 시작·대시보드·AI 전략 생성·챗봇·관리자 학습 흐름을 단계별 화살표와 제한된 코드 발췌로 제공한다. `.env` 값·API 키·소스 전문은 표시하지 않는다.
- React 관리자 학습 탭의 마지막에 `/project-tree`를 추가하고, 해당 페이지에서 별도 Streamlit 포트 `8501`을 iframe으로 표시한다. 기존 React·Backend·AI Server 데이터 경로와 기능은 변경하지 않는다.
- 실행 명령과 별도 의존성은 `project_tree_explorer/README.md`에 기록한다.

### D-105 기존 후보 없는 최초 설계 비교 진단 (2026-09-08)

- 이어서 품질 개선하라는 요청에 따라 명시 실행 진단에만 --fresh를 추가한다. 같은 관측·출처·사례·사용자 여건·저장 ML로, 이전 선택안과 수정 피드백을 제외해 초안 복사 영향을 비교한다. 운영 경로의 입력·호출 횟수는 바꾸지 않는다.
- 결과의 candidate_contract_passed와 report_approved=false를 분리한다. 유료 호출·저장·승인은 계속 차단한다. 진단 성공을 전체 기획안 승인으로 표시하지 않는다.
- 실제 응답의 분기별 주기를 누락으로 오판한 검사를 수정한다. 이는 분기 집계의 적절성·자료 범위·분모 검증을 면제하지 않는다.
- 관련 unittest 101개 통과. 실제 모델 비교 결과는 LOCAL_CANDIDATE_REPLAY_20260908.md에 기록한다.
- 실제 최초 설계도 통과하지 못해 운영 경로를 fresh로 변경하지 않는다. 실제 응답에서 발견한 예산 편성 모델 도입 제목·미허용 유형·근거 없는 수치 문턱을 추가 검사한다. 기존 프롬프트의 허용 후보 6개 유형을 Schema enum과 코드 검사에 공유한다. 최종 관련 unittest 102개 통과, 새 enum GPU 실측은 후속 검증 대상이다.

### D-104 후보 재비교의 이전 선택 편향과 지역 출처 전달 (2026-09-08)

- 품질 개선 후속 요청에 따라, 오류 피드백이 있는 로컬 transferability에서 이전 선정 사례의 우선 조회를 해제한다. 현재 benchmark_cases 순서에서 서로 다른 운영 방식의 원문을 읽는다. 이전 sources 순서가 선정 우선순위를 다시 만들지 않도록 한다.
- 이전 선택 후보 원문·비교 후보·피드백은 그대로 보존하고 get_planning_decision에 rejected_candidate_draft 맥락을 표시한다. 본문 작성/검수는 이전처럼 실제 선정 사례를 우선 조회한다.
- get_region_metrics에 요청의 등록 dataset 출처 원문을 함께 전달한다. 기존에는 관측의 출처 이름만 있고 후보 검사에 필요한 source_id는 별도 검색해야 했다. 지표·값·기간 대조를 요구하며 순서나 지역명으로 ID를 임의 결합하지 않는다.
- 기존 도구 크기/문맥/출력 상한과 호출 횟수, 유료 차단 진단, 후보 오류 차단 기준을 유지한다. 원주 관측 7개·출처 7개 도구 응답은 5,132바이트다. 실모델 결과는 LOCAL_CANDIDATE_REPLAY_20260908.md에 별도로 기록한다.
- 실제 응답의 `수량×단가 산식:` 표제만으로 금액 나열이 통과한 오류를 확인해 해당 표제를 제외한 본문에서 산식을 검사한다. 후보와 본문 공통 적용이며 변수식은 허용한다. 관련 unittest 98개 통과. 오류 수 감소는 의미·실행 가능성 개선을 증명하지 않는다.

### D-103 후보 필드별 작성 지시와 보완 전달 회귀 (2026-09-08)

- 사용자 요청의 로컬 LLM 품질 개선 후속 작업. 기존 후보 Schema의 예산·측정·지역 적합성·출처·시범 범위에 필드 설명을 추가한다. 기존 `schema_field_guidance`가 로컬 시스템 메시지에도 전달한다.
- 직접 provider 호출도 Router 경로와 같은 후보 보완 지시를 사용한다. 전체 피드백 12건과 600자 초과 수정 지시가 도구 조회 이후 최종 JSON 입력까지 보존되는지 검사한다.
- 실제 재시험에서 선택안에 타지역의 `총 10억 원`을 복사했다. 본문에만 있던 가정 표시 없는 확정 총액 검사를 후보와 strategy_brief에도 적용하며 critical로 차단한다. 수량×단가가 있거나 needs_evidence 상태여도 면제하지 않는다. 명시된 참고 견적은 허용하되 산술·의미 검증을 대신하지 않는다.
- JSON 필드·호출 횟수·출력 상한·승인 점수·저장 보고서는 유지한다. 설명 추가는 모델의 의미 정확성이나 기획 품질 향상을 입증하지 않는다.
- 관련 unittest 96개 통과. pytest는 프로젝트 가상환경에 없어 실행하지 못했다. 실제 모델 실행 결과는 `LOCAL_CANDIDATE_REPLAY_20260908.md`에 별도 기록한다.

### D-102 로컬 후보 보완 실측과 사례 분류 통일 (2026-09-08)

- 사용자 승인으로 저장 근거의 Qwen 보완을 시험한다. 유료 API와 저장 보고서 변경은 시험에서 차단한다.
- 사례 Scout/로컬 도구의 분류 기준을 통일하고, 실제 사용하는 로컬 지시문에 후보 보완 규칙을 반영한다. 새 라이브러리·토큰 상한 확대·검증 완화는 없다.
- 1차 실측은 JSON 성공이나 품질 실패였다. 상세 범위·수치는 LOCAL_CANDIDATE_REPLAY_20260908.md를 참고한다. 현재 모든 지역 생성이 검증됐다는 의미가 아니다.

### D-101 실제 PPT 견적의 별도 예시 금액 제거 (2026-09-08)

- 실제 보고서의 견적 슬라이드와 계산 노트는 strategies[0].budget을 사용한다. 문서만의 고정 운영량/가정 단가로 별도 총액을 만들지 않는다.
- 명시된 단일 총액은 그대로 전사하며 산식 검증 완료로 간주하지 않는다. 총액이 없거나 여러 개이면 총액 미확정으로 표시한다. 원문과 기존 저장 보고서는 변경하지 않는다.
- 고정 예시 견적은 offline_sample에만 유지한다. 실제 출력의 출처 목록에 견적 함수가 임의로 추가하던 강진 환급 출처를 제거한다. 보고서가 실제 인용한 출처는 유지한다.
- 캐시 렌더 버전을 올려 다시 다운로드할 때 변경을 반영한다. 구조화 예산 항목의 생성·검증, 실제 LLM 전체 기획 품질과 시각 검수는 후속 작업이다.

### D-100 명시적 목표 KPI 입력 연결 (2026-09-08)

- 생성 입력 화면에 방문자/관광소비 목표율을 추가한다. 두 값 모두 입력하거나 모두 미정으로 유지한다. 방문자 0~20%, 소비 0~30%의 기존 문서 계약을 유지한다.
- 지역별 planning_brief에 보관하고 생성 결과 execution_scenario에는 사용자 값만 복사한다. LLM이 생성한 임의 목표는 채택하지 않는다.
- 문서 공통 projection은 저장 execution_scenario를 우선하고 없을 때 명시적 planning_brief 값을 읽는다. 마지막 사업 월 목표율까지 균등하게 접근하는 계획값이며 인과효과 예측이 아니다.
- 관련 테스트 14개와 frontend build 통과. lint는 기존 effect 내 setState 경고 1건이며 새 입력 오류는 없었다. 목표율 입력이 타당한 효과나 예산을 입증하지는 않는다. 웹/PPT 견적 단일화는 아직 미완료다.

### D-099 전체 검수 지적을 자동 보완에 전달 (2026-09-07)

- 사용자 요청: 반복되는 기획 품질 경고의 원인 개선. GPU 노트북 종료 상태이므로 실생성·유료 호출 없이 검증한다.
- 기존 상위 8개 요약과 별개로 자동 보완에는 코드 검사 전체를 전달한다. 문제 설명과 수정 지시를 보존하며 지시문 600자 절단을 제거한다. 호출 횟수·토큰 상한은 늘리지 않는다.
- 최종 승인에도 전체 검사를 병합한다. 화면/문서는 validation_findings의 중대 문제와 명시적인 최종 검수 미완료를 통과로 표시하지 않는다.
- 사례의 case_region/intervention/operating_model을 출처에 보존하고, 다른 지역의 운영 범위를 그대로 복사한 후보를 차단한다. 타지역 사례 참고 자체는 허용한다.
- 66개 오프라인 테스트, 수정 JSX lint 및 frontend build 통과. 실제 Qwen/Gemma 재생성, 문서 예산 단일화·목표 KPI 개선은 미완료이며 기존 보고서의 승인을 변경하지 않는다.

### D-098 후보 보완 실패 시 본문 작성 차단 (2026-09-07)

- 원주 결과의 미등록 사례 ID·예산 편성형 후보를 확인하고 사용자가 수정을 승인했다.
- 후보 보완 후에도 작성 계약 위반이 남으면 본문·후속 유료 검수를 실행하지 않는다. 검증 오류를 품질 승인으로 바꾸지 않는다.
- 상세 검증 및 미완료 항목: [후보 검증 수정 기록](CANDIDATE_GATE_FIX_20260907.md).

### D-097 기획안 수정 챗봇의 범위·실행 추적·문맥 안전성 (2026-09-07)

- 사용자 요청: 생성·수정·입력 여건의 실제 연결과 품질을 점검·개선. 기존 데이터·승인 여부를 허위로 바꾸지 않는다.
- 구현: 단순 수정의 검색 강제 라우팅과 두 번째 대화의 answer/content 불일치를 수정한다. 모델·웹 사용 여부를 실제 trace로 반환한다. 빈 필드/임의 키 덮어쓰기를 차단하고 수정 후 재검수가 필요함을 유지한다.
- 긴 수정: 기존 기획안 보완에는 저장 시점 근거를 사용하고 실행 로그를 제외한다. 반복 원문을 무손실 사전·표로 표현한다. 미인용 RAG는 원문 대신 미열람 출처 목록으로 분리하며 새 근거로 인용하지 못한다. 전체 저장 보고서·원문은 변경하지 않는다. 신규 사업 선정·사례 검증은 별도 생성·검수 범위다.
- 결과: Qwen 실제 원주 설명과 Gemma 실제 원주 수정안 HTTP 200 확인. 유료 호출 없음. 시험 수정안 미저장. 연결·구조화 성공이 전문적인 기획 품질 승인이라는 뜻은 아니다.
- 문서: `QUALITY_INTEGRATION_AUDIT_20260907.md`. 지역 초록색 기준은 유지하고 오래된 승인·오프라인 샘플을 제외한다. 목표값 미저장·미검수 상태를 임의로 해제하지 않는다.

아래의 확정된 결정은 임의로 바꾸지 않는다. 사용자가 승인한 새로운 결정은 같은 형식으로 추가한다.

## 확정

### D-096 지역 선택 검증 상태

- 사용자 요청에 따라 시군구 선택에서 전체 확인을 통과한 지역명만 초록색으로 표시한다. 모델 파일 존재만으로는 통과시키지 않는다.
- 원자료·7개 ML·최신성·SQL 전국 비교/peer·공식 사례와 저장 기획안 목표/검수/기간/문서 출력을 구분한다. 미검증·만료는 기본색, 실패 이유는 선택창 아래에 표시한다.
- 모든 사업의 필요한 자료를 100% 정의할 수 없고 미래 LLM 성공도 보장할 수 없으므로 화면에 확인 범위를 명시한다. 가정 목표를 자동으로 넣어 통과 상태로 만들지 않는다.
- 예상 숙박일 헤딩과 ‘평균 2.64일’ 형태의 값 표기를 분리한다.

### D-095 실행 개요와 세부 실행내역 분리

- 사용자 요청으로 6장 표의 단계·기간 열을 분리하고 헤더 가운데 정렬, 작은 본문 폰트와 연결형 마일스톤을 적용한다.
- 바로 다음에 ‘세부 실행내역’ 1장을 추가한다. 신청·확인·운영·집계 방법을 쉬운 문장으로 설명하며 환급 사업과 일반 운영 절차를 구분한다. 절차는 제안이지 확정 지침이 아니다.
- v9 출력 총 11~12장. v8 파일·기존 보고서·데이터·모델·LLM 정책은 변경하지 않는다.

### D-094 월별 비교·실행 업무 중심 PPT v8

- 날짜: 2026-09-07. 사용자 추가 수정 요청 반영.
- v6 원본과 v7 샘플은 보존하고 v8 출력 모듈을 적용. 4장 상단은 사업내용·사업기간·목표 KPI 3개 동일 폰트, 월별 차트는 적응형 축과 만/억 단위 레이블을 사용한다.
- ‘사업 미진행시 예측값’은 과거 추세를 이용한 참고값이며 인과적 반사실 추정이 아니다. 목표는 명시적 입력률에 따른 계획값이다. 누락 목표는 생성하지 않는다.
- 방문·소비 표를 분리하고 3개월 추가 목표 규모와 증가율 표시. 월별 방문자 합계는 중복 포함이라고 표시한다. 예산은 수량×단가 참고 견적이며 실제 계약 가격으로 주장하지 않는다.
- 기존 지속 운영 장 제거, 출처 1~2장 유지, 마지막 FINAL/TOUR INSIGHT/검토용 문구 제거. 총 10~11장.
- 사례 전체에서 숙박 단어를 먼저 찾는 대신 사업명·운영방식 중심으로 분류. 선정 사례 고정 후 다른 방식 우선 배치하며 새로운 공식 근거를 임의 생성하지 않는다.

### D-093 사업 설명 중심 PPT v7 레이아웃

- 날짜: 2026-09-07
- 상태: 사용자 요청 반영
- 결정: 승인된 v6 원본을 보존하고 `proposal_layout_v7.py`에서 수정 레이아웃을 적용한다. 사업 설명에 집중하는 3장, 설계와 비교 차트를 합친 4장, 4개 공식 사례, 4단계 업무표, 수치 KPI 표를 사용한다. 기존 5장은 제거하고 전체 출처를 1~2장으로 정리한다.
- 데이터 원칙: ML예측치와 사업 목표는 별도 계열이다. 미입력 목표를 임의 생성하지 않으며, 제시된 목표는 효과 예측이 아니라 계획값임을 표시한다. 사례 선정 ID가 확인된 카드만 선정 색상으로 강조한다.
- 편집 원칙: 단어 단위 줄바꿈, 두 줄은 가능한 범위에서 70/30 비율, 고립된 마지막 단어 최소화. 본문을 생략 부호로 잘라내지 않는다. 표·차트·파이프라인은 편집 가능하다.
- 검증: 저장 보고서 재출력 및 오프라인 문서·예측 계약 검사. 샘플 목표율은 원 보고서와 분리하고 예시로 명시한다. 다운로드에는 유료 생성 호출이 없다.

### D-034 강남구 수요 예측의 1차 ML 범위

- 날짜: 2026-08-28
- 상태: 승인됨
- 결정: 강남구 2024.01~2026.07 공식 월별 원자료로 다음 달 순 방문자 수와 외지인 관광소비액을 각각 예측한다. 최근 3개월 관측과 이후 3개월 예측을 지역선택 대시보드에 함께 표시한다.
- 이유: 원자료만 보여주는 화면을 넘어, 담당자가 단기 수요 흐름을 읽을 수 있게 하기 위함이다. 두 목표값의 검증 성능이 달라 단일 모델로 묶지 않는다.
- 결과: 학습은 `ai_server/ml`의 명시적 CLI에서만 수행하고, 저장 artifact만 API가 읽는다. 최근 4개월 시간순 테스트에서 seasonal-naive 기준선보다 MAE가 낮지 않으면 artifact를 저장하지 않는다. 업종별 소비 패턴은 최신 관측 비중을 적용한 가정이지 업종별 ML 결과가 아니다.

### D-001 서비스 방향

- 결정: 일반 여행자 챗봇이 아닌 관광정책·소상공인 전략 서비스
- 이유: 관광데이터랩 활용도와 지역 경제 효과를 더 분명히 보여 줄 수 있음

### D-002 AI 역할 분리

- 결정: ML은 수요 숫자 예측, MySQL은 정확한 수치 조회, RAG는 공식 문서 검색, LLM은 설명·전략 생성
- 이유: 환각을 낮추고 발표에서 각 기술의 역할이 분명함

### D-003 앱 구조

- 결정: React/Vite + FastAPI Backend + FastAPI AI Server + MySQL + ChromaDB + OpenAI API + Nginx + AWS EC2
- 이유: 강사 제공 구조와 맞고 AI 로직을 분리할 수 있음

### D-004 예측 과제

- 상태: 잠정
- 결정: 지역별 다음 달 방문객 수를 회귀로 예측
- 조건: 실제 데이터의 기간·일관성이 확인되어야 함

### D-005 벡터 DB

- 결정: MVP에는 영속형 ChromaDB 사용. Qdrant는 확장 단계에서만 검토

### D-006 Fine-tuning

- 결정: MVP에서는 하지 않음
- 이유: 변하는 공식 지식은 재임베딩 가능한 RAG가 적합하며, 숫자 근거 문제도 해결하지 못함

### D-007 강남구 단기 시연 대시보드

- 상태: 승인됨
- 결정: 최종 React/Vite MVP와 분리해, 강남구 2025-08~2026-07 데이터로 Streamlit 시연 화면을 만든다.
- 이유: 공무원 사용 관점의 대시보드와 AI 전략 보고서 흐름을 빠르게 검증하기 위해서다.
- 결과: 12개월 표본의 ML은 기준선·후보 모델을 보여 주는 교육용 시연으로만 사용하며, 정식 예측 성능이나 정책 판단 근거로 주장하지 않는다. OpenAI 키는 서버 측 `.env`에서만 읽는다.

### D-008 React/Vite UI 골격

- 상태: 승인됨
- 결정: `frontend/`에 React + Vite 기반의 시군구 관광 대시보드 UI를 먼저 만든다. 지도는 React Leaflet, 그래프는 Recharts를 사용하며, 시연 데이터는 `src/data/`에 격리한다.
- 이유: 실제 데이터셋이 취합되기 전에도 공무원 사용 흐름과 반응형 레이아웃을 검토할 수 있고, 이후 FastAPI `/api`, `/ai` 응답으로 안전하게 교체할 수 있다.
- 주의: 화면의 모든 수치·예측·전략 문구는 시연값임을 표시한다. 실제 시군구 경계는 검증된 GeoJSON과 지역코드가 준비된 뒤에 연결한다.

### D-009 시군구 경계 지도

- 상태: 승인됨
- 결정: VWorld WFS 행정구역 경계를 일반 FastAPI Backend가 서버 측에서 조회해 React에 GeoJSON으로 전달한다. React는 React Leaflet으로 면 경계를 표시하고, 키를 직접 읽지 않는다.
- 이유: 참고 화면과 같은 클릭형 시군구 지도를 만들면서 VWorld 인증키 노출을 막고, 지도 응답을 캐시해 외부 API 호출을 줄일 수 있다.
- 주의: VWorld 현행 코드와 과거 관광 원자료 코드는 다를 수 있다. 예를 들어 전북·강원의 변경 전 코드는 지역코드 매핑표 검증 전에는 직접 조인하지 않는다.

### D-010 2단계 지역 선택 지도

- 상태: 승인됨
- 결정: 전국 지도는 고정하고, `시도 선택 → 해당 시도의 시군구 선택` 순서의 두 선택창을 지도 위에 둔다. 시도는 청록색, 선택한 시군구는 주황색으로 같은 지도 위에 겹쳐 표시한다.
- 추가: 첫 화면 검색창에서 도·시·군·구 이름을 입력하면 같은 시도·시군구 선택 상태로 연결한다.
- 이유: 지자체 담당자가 본인 지역을 빠르게 찾되, 시군구 선택 때 전국 지도 위치·맥락이 바뀌지 않게 하기 위해서다.
- 주의: VWorld 현행 경계에서 광주·전남 통합처럼 행정구역이 변경된 경우에는 현재 공식 경계 기준을 표시한다. 데이터 원자료의 기준 시점과 다르면 매핑·표기 기준을 별도로 검증한다.

### D-011 강남구 OpenAI 전략 보고서 테스트

- 상태: 승인됨
- 결정: 강남구 원본 ZIP의 최신 방문·소비·체류·관심도 관측값을 별도 AI 서버가 읽고, OpenAI가 구조화된 종합 전략기획서를 생성한다. 보고서에는 원본 ZIP에서 재계산한 월간 방문·관광소비 그래프, 기간별 전략 로드맵, 담당자 목표 입력형 시나리오와 `다음 달`·`3개월 이내`·`1년 이내` 전략을 함께 표시한다.
- 이유: 공무원·소상공인용 핵심 흐름을 실제 데이터 기반으로 먼저 검증하기 위해서다.
- 주의: 12개월 표본이므로 예측 성능·정책 효과를 주장하지 않는다. 관측 사실과 AI 제안을 표기에서 분리한다. 시나리오의 소비 목표액은 사용자 입력 목표율을 최근 월 관측값에 단순 환산한 값이며 예측·인과효과가 아니다. 단가·견적·유사사업 예산의 공식 근거가 없으므로 AI가 예산 금액을 만들지 않으며, 예산 산정 필요 항목만 제시한다.
- 추가: `GET /ai/v1/demo/11680/dashboard`는 OpenAI를 호출하지 않고 강남구 ZIP에서 최신월·전월 대비 상단 카드 3개를 계산한다. 이 테스트 경로는 다른 지역의 데이터 검증과 MySQL 이전 전까지만 사용한다.

### D-012 담당자용 4단계 의사결정 UI

- 상태: 승인됨
- 결정: React 화면에 `지역 선택 → 전국 진단 → 지역 결과 → 정책 시뮬레이션` 탭 흐름을 둔다. 정책 시뮬레이션은 목표값을 조절하는 UI를 먼저 제공하며, 검증된 전국 비교·예측 모델이 연결되기 전에는 금액·순위·정책 효과를 사실처럼 표시하지 않는다.
- 이유: 담당자가 데이터 확인부터 실행 검토까지의 순서를 쉽게 이해하고, 향후 ML·MySQL·RAG 결과를 같은 화면에 연결할 수 있게 하기 위함이다.
- 주의: 시뮬레이션 결과는 가정 기반 시나리오이며 인과효과·매출 증가를 보장하지 않는다.

### D-013 데이터 출처·의존성 변경 기록

- 상태: 승인됨
- 결정: 데이터랩 ZIP은 월별 사실·전처리·ML의 중심 데이터로, 관광 Open API는 관광자원·인프라·공식 맥락을 보강하는 데이터로 역할을 분리한다. 실제 연결 상태는 `docs/DATA_SOURCE_USAGE.md`에서 관리한다.
- 이유: 화면의 수치가 어떤 원본에서 나왔는지, 어떤 Open API가 실제 사용 중인지 팀원이 같은 기준으로 확인하기 위해서다.
- 결과: 새 ZIP 또는 API를 실제 화면·분석·보고서에 연결할 때 출처 문서와 필요한 의존성 파일(`requirements.txt` 또는 `frontend/package.json`)을 같은 변경에서 갱신한다. API 키는 서버 `.env`에만 보관한다.

### D-014 AI 전략 화면과 Word 기획서 분리

- 상태: 승인됨
- 결정: 화면의 AI 리포트는 핵심 요약, 원자료 월간 변화, 기간별 실행 전략, 담당자 입력형 정책 시나리오만 보여 준다. 예산 검토, 성과 확인 기준, 세부 실행 방법, 근거와 데이터 한계는 `POST /ai/v1/demo/11680/strategy-proposal.docx` Word 기획서에 정리한다.
- 이유: 담당자는 화면에서 빠르게 전략과 규모를 판단하고, 회의·결재용 상세 내용은 다운로드 문서로 검토할 수 있어야 한다.
- 주의: 시나리오의 방문객·관광소비액은 최근 월 실측값에 사용자가 입력한 목표율을 단순 환산한 값이다. ML 검증 전에는 정책 인과효과·매출 증가·관광객 증가 예측으로 주장하지 않는다. OpenAI는 구조화된 JSON 안에서 관측 사실과 제안을 구분하며, 예산 금액은 공식 단가 근거가 없으면 만들지 않는다.

### D-015 독립 기획 추천안과 실행 비교 시뮬레이션

- 상태: D-019로 대체됨
- 결정: AI 전략은 `1개월 단기 전략`·`3개월 단기 전략`·`1년 장기 전략`의 독립 기획 추천안 3개로 표시한다. 각 추천안에는 구체적 실행 설명과 2~3개 착수 항목을 함께 제공하며, 선택한 추천안은 오른쪽 시뮬레이션과 연결한다.
- 이유: 담당자가 시간 순서의 파이프라인이 아닌, 예산·여건에 맞춰 검토할 수 있는 서로 다른 실행 대안을 빠르게 비교하도록 하기 위함이다.
- 주의: `전략 미실행`과 `전략 실행 목표` 비교는 담당자가 조절한 방문객·관광소비 목표율을 최근 월 실측값에 단순 환산한 것이다. 이는 AI·ML 예측이나 정책 인과효과가 아니다. 유사 지역 비교는 검증된 공통 지역·기간 원자료가 확보된 뒤에만 AI 근거로 사용한다.

### D-016 실행 산출물 중심 AI 기획안

- 상태: 승인됨
- 결정: AI 전략의 각 항목은 해결할 데이터 문제, 실행 설명, 2~3개 작업과 산출물, 기대 변화, 확인 지표, 근거를 구조화해 생성한다. 화면과 문서에서는 역할 호칭 대신 기획안 자체를 표시한다.
- 이유: 수치 근거 없이 "강화"·"활성화"만 반복하는 일반론을 줄이고, 실행 후 무엇을 검토할 수 있는지 명확히 하기 위함이다.
- 주의: 현재 AI 입력은 강남구 원본 ZIP에서 계산한 snapshot과 최신 소비 업종대분류다. 공식 정책·교통·관광자원 문서를 수집·출처 등록하기 전에는 RAG나 유사 지역 사례를 실제 보고서 근거로 사용하지 않는다.

### D-017 지역 원본 공통 대시보드·전략 보고서

- 상태: 승인됨
- 결정: `data/raw/{시도}/{시군구}`에 있는 공식 원본에서 방문자·외지인 관광소비·숙박/체류·내비게이션·SNS 표가 검증된 지역은 하나의 AI 대시보드·전략 보고서 경로로 처리한다. 최초 지원 지역은 서울특별시 강남구, 인천광역시 계양구·서구·옹진군이다.
- 이유: 지역별 파일명이 연도·다운로드 시점에 따라 달라도 표 이름을 기준으로 실제 원본을 읽어, 신규 지역을 같은 사용자 흐름에 추가하기 위해서다.
- 주의: 현재 전략 근거는 해당 지역의 관광데이터랩 원본 관측값과 OpenAI 구조화 출력이다. RAG, ML 예측, 외부 관광 Open API를 추가했다는 뜻이 아니며, 새 지역은 원본 표·지역명·지역 코드 검증 뒤에만 지원한다.

### D-018 시도 내 시군구 최근 3개월 비교

- 상태: 승인됨
- 결정: 시도를 선택하면 `data/raw/{시도}/{시군구}`에 실제 원본이 있는 시군구만 대상으로, 공통 최근 3개월의 순 방문자 수와 외지인 관광소비액 평균을 비교 차트에 표시한다.
- 이유: 선택 지역의 규모를 같은 기간·같은 집계 기준으로 간결하게 비교하기 위해서다.
- 주의: 현재 인천광역시는 계양구·서구·옹진군 원본만 있으므로, 다른 군·구의 수치를 추정하거나 표시하지 않는다. 원본을 추가하면 같은 계산으로 자동 포함한다.

### D-019 단일 3~6개월 관광 기획안

- 상태: 승인됨
- 결정: 기존 `1개월`·`3개월`·`1년` 독립 추천안 3개를 폐기하고, 원칙적으로 향후 3~6개월 동안 방문자 수 또는 관광소비액 개선을 목표로 하는 단일 기획안을 생성한다. 화면과 Word 문서는 `문제 확인 → 솔루션 → 집행 방법` 순서로 동일하게 구성한다.
- 이유: 여러 선택지를 나열하는 것보다 선택 지역의 핵심 문제를 먼저 특정하고, 하나의 해결 방향을 일정·작업·산출물까지 연결하는 편이 실제 사업 검토에 유용하다.
- 비교 기준: 동일 시도의 `data/raw`에 원본이 있는 시군구만 공통 최근 3개월의 순 방문자 수·외지인 관광소비액·숙박 방문 비율·평균 숙박일수로 비교한다. 전국 또는 시도 전체 평균으로 표현하지 않는다.
- 예산 기준: 현재 원본에는 사업 단가와 견적이 없으므로 금액을 만들지 않는다. 비용 항목, 수량 기준, 공식 단가·비교견적 확보처, 계산식을 제시하고 견적 확보 후 확정한다. 실제 원화 범위는 근거 문서를 RAG에 연결한 뒤에만 계산한다.

### D-020 근거 수집·기획 작성·품질 검토 Multi-Agent

- 상태: D-030으로 확장됨
- 결정: AI 전략 보고서는 Evidence Agent, Planner Agent, Reviewer Agent의 고정 순서로 생성한다. Reviewer가 82점 미만 또는 critical 오류를 판단하면 Planner가 한 번 수정하고 최종 검토한다.
- 근거 범위: 정확한 수치는 데이터랩 원본, 관광자원은 허용 Open API, 공식 정책·사업 문서는 영속형 ChromaDB RAG와 허용 공식 도메인 조사만 사용한다.
- 이유: 자료 조사, 작성, 검토 책임을 분리해 일반론·근거 누락·장문 반복을 줄이고 실행 단계와 산출물을 강화하기 위해서다.
- 주의: Agent 수가 사실성을 보장하지 않는다. 출처가 없는 주장은 버리고, RAG 문서가 없으면 자료 공백을 표시한다. 재작성은 비용·지연을 통제하기 위해 최대 1회로 제한한다.
- 운영: Sol 기반 작성·검수는 오래 걸릴 수 있어 Agent 한 단계의 기본 대기시간은 300초로 두고 `AI_AGENT_TIMEOUT_SECONDS`로 조정한다.

### D-021 Agent 코드·프롬프트 분리

- 상태: 승인됨
- 결정: AI Server의 Agent 코드는 `ai_server/app/agents/`에서 Evidence·Case Scout·Transferability·Planner·Reviewer·Orchestrator 파일로 분리한다. 다섯 Agent의 프롬프트와 페르소나는 `agents/prompts.py` 한 파일에서 관리한다.
- 이유: 팀원이 Agent별 책임과 조정 위치를 쉽게 찾고, 한 Agent의 변경이 다른 Agent 역할과 섞이지 않게 하기 위해서다.
- 주의: 프롬프트를 조정해도 원자료 수치·출처·기간 규칙, 공식 도메인 제한, JSON Schema 검증은 완화하지 않는다.

### D-022 간결한 실행 기획안·목표 비교 시각화

- 상태: 승인됨
- 결정: AI 전략 팝업은 긴 본문 대신 `핵심 판단 → 문제·해결 방향 → 단계별 집행 → 실행/미실행 목표 비교` 순서로 표시한다. Word 기획서는 같은 내용을 도표·표 중심으로 최대 5쪽에 정리한다.
- 이유: 담당자가 읽기 전에 핵심 문제와 실행 방법, 검토할 목표 규모를 빠르게 판단할 수 있게 하기 위함이다.
- 주의: 목표 비교는 최근 월 원자료에 선택 목표율을 단순 환산한 검토값이다. 정책 인과효과·ML 예측·매출 증가 보장이 아니며, 1인당 소비는 집계 기준 검증 전에는 표시하지 않는다.

### D-023 계획 기간별 실행 목표 추이

- 상태: 승인됨
- 결정: 실행 목표 비교는 3개월 또는 6개월의 월별 방문자 수·관광소비액 추이를 표시하며, `미실행`과 `실행 목표` 선을 개별 또는 겹쳐서 검토할 수 있게 한다. 문제 확인, 솔루션, 집행 방법은 각각 독립된 화면 영역으로 표시한다.
- 이유: 목표 총액만 보는 것보다 계획 기간의 월별 변화와 실행 순서를 한 화면에서 검토하기 위함이다.
- 주의: 미실행 선은 최근 월 원자료 유지선이고 실행 선은 선택 목표율을 기간에 균등 배분한 검토선이다. 인과효과·정책 성과 예측이 아니다. 조사 공백은 Agent 내부 품질검토에 보관하며 팝업과 Word에는 긴 한계 문단을 표시하지 않는다.

### D-024 페이지별 프론트엔드 관리

- 상태: 승인됨
- 결정: 상단 공통 UI를 유지하면서 기능별 페이지를 `frontend/src/pages/`에 분리한다. `최근 뜨고 있는 관광지`는 `/trending` 경로의 빈 페이지로 먼저 만들고, 팀원이 해당 페이지에 데이터·차트 기능을 추가한다. 반복 배경은 `frontend/src/components/HeroShapeField.jsx`에서 공통 관리한다.
- 이유: 대시보드와 신규 기능을 한 파일에 섞지 않고 팀원별로 독립 개발하기 위함이다.

### D-025 쉬운 문장·근거 중심 5단계 기획안

- 상태: 승인됨
- 결정: AI 기획안은 쉬운 한국어로 `문제/제안 → 이 제안을 한 이유 → 해결 방법 → 5단계 실행 → 기대 변화` 순서로 작성한다. 판단 근거에는 실제 보유 기간의 월별 변화, 같은 기준의 지역 비교, 날짜가 확인된 공식 행사·정책 자료만 사용한다.
- 이유: 행정용어와 내부 역할 나열을 줄이고, 사용자가 왜 이 제안이 나왔으며 다음에 무엇을 해야 하는지 바로 이해하게 하기 위함이다.
- 표시 기준: 화면과 Word에는 내부 source_id·상세 KPI 문장을 노출하지 않고 `관광데이터랩`, `관광 Open API`, `공식 문서`와 자료명만 표시한다. Word에는 5단계 표와 프로젝트 타임라인을 함께 넣는다.
- 주의: 1년 자료를 3년 추세로 표현하거나, 행사 시점과 방문 변화가 확인되지 않은 상태에서 인과관계로 단정하지 않는다.

### D-026 근거 조사 병렬화·단기 캐시

- 상태: 승인됨
- 결정: Evidence Agent의 관광 Open API, ChromaDB RAG, 공식 웹 조사를 동시에 실행하고, Evidence Agent와 Case Scout Agent도 병렬로 실행한다. 동일 지역·동일 원자료·동일 조사 설정의 지역 근거는 기본 1시간, 공식 사례 근거는 기본 6시간 메모리에 재사용한다.
- 이유: Planner·Reviewer의 Sol 고추론과 품질 미달 시 1회 재작성은 유지하면서, 서로 의존하지 않는 외부 조회의 직렬 대기와 반복 조사를 줄이기 위해서다.
- 주의: 캐시는 기획안 완성본이 아니라 확인된 근거 묶음에만 적용한다. 원자료 또는 조사 설정이 바뀌면 캐시 키가 달라지며, 서버 재시작 시 사라진다. Planner 작성과 Reviewer 검수는 매번 새로 수행한다.

### D-027 전 지역 공통 최근 12개월 차트

- 상태: 승인됨
- 결정: 첫 대시보드와 AI 기획안의 방문자 수·관광소비액 월간 추세는 선택 지역의 가장 최근 12개월만 표시한다. 지원 지역을 바꿔도 카드·차트·관광소비 진단·지도 배치 구조는 동일하게 유지하고 값과 분석 내용만 바꾼다.
- 이유: 지역마다 1~3년 원본이 섞여 있어도 한 화면의 기간과 밀도를 같게 유지하고, 최근 변화가 3년치 축에 눌려 보이지 않게 하기 위해서다.
- 주의: 12개월보다 오래된 원본은 삭제하지 않는다. 향후 계절성 비교와 ML 시간순 검증에 사용하며, 현재 화면 응답에서만 제외한다.

### D-028 대시보드 시군구 전체 비교 차트 제거

- 상태: 승인됨
- 결정: 메인 대시보드 하단의 시군구 전체 비교 그래프와 이를 위한 프론트엔드 요청을 제거한다.
- 이유: 첫 화면에서는 선택 지역의 최근 12개월 방문자 수·관광소비액과 소비 진단에 집중하기 위해서다.
- 주의: AI 전략기획서가 근거로 사용하는 동일 시도 원본 보유 지역 비교 계산은 유지한다. 화면에서 비교 그래프만 표시하지 않는다.

### D-029 API 크레딧 소진 시 오프라인 화면 테스트

- 상태: 승인됨
- 결정: 개발 환경에서 OpenAI API가 크레딧·결제 한도로 실패하면 선택 지역의 실제 snapshot과 고정된 문장 규칙으로 오프라인 샘플 기획안을 표시한다.
- 이유: API 결제 상태와 무관하게 팝업·그래프·5단계 실행안·Word 다운로드 흐름을 검토하기 위해서다.
- 주의: 화면에 `오프라인 테스트 결과`를 명확히 표시하며 공식 웹 조사, RAG, Case Scout, Transferability, Planner, Reviewer가 실행되었다고 주장하지 않는다. `APP_ENV=production`에서는 샘플 endpoint를 비활성화한다.

### D-030 공식 성공사례 탐색·지역 적합성 평가 Agent

- 상태: 승인됨
- 결정: 기존 Evidence·Planner·Reviewer 흐름에 `Case Scout Agent`와 `Transferability Agent`를 추가한다. Case Scout는 공식 성과평가·결산서·예산서·보도자료에서 실제 관광사업의 대상, 운영 방식, 공개 예산, 관측 성과와 적용 조건을 수집한다. Transferability는 선택 지역 원자료와 사례 조건을 비교해 적합한 사례 1~3개와 3~6개월 시범사업 구조를 Planner에 전달한다.
- 사례 RAG: 검수한 공식 사례는 `data/rag/official_case_studies.jsonl`과 ChromaDB에 저장한다. 라이브 웹 검색은 최신 사례 보강용이며, URL과 기간이 없는 사례는 사용하지 않는다.
- 이유: `동선을 묶는다`, `홍보한다` 같은 일반론 대신 쿠폰·환급·숙박·행사·교통 결합 등 실제 집행 방식과 측정 가능한 중단·확대 기준이 있는 기획안을 만들기 위해서다.
- 주의: 타 지역에서 관측된 성과를 선택 지역의 보장 효과로 바꾸어 쓰지 않는다. 공식 평가가 인과효과를 검증하지 않았다면 참고 사례로만 표시하며, 예산은 공식 금액 또는 항목×수량×단가 산식으로만 제시한다.
- 표시: AI 팝업에는 근거 강도·제목·요약·원문 링크를 갖춘 공식 사례 카드를 최대 3건 보여 준다. Word 기획서는 일반 데이터 출처와 분리한 `참고한 공식 사례` 표로 정리한다.

### D-031 bid3 업무공간 디자인과 관광 AI 도우미 통합

- 상태: 승인됨
- 결정: TP2-3은 React + Vite, Backend FastAPI, AI FastAPI, MySQL 구조를 유지한다. bid3의 224px 사이드바, 64px 상단바, 1360px 콘텐츠 폭, 흰색 surface와 얇은 테두리 디자인을 관광 분석 화면에 이식한다. Django·SQLite·결제·입찰공고 기능은 가져오지 않는다.
- 지도: 메인 대시보드의 첫 분석 영역에서 가장 넓게 표시한다. 기존 VWorld 경계·Leaflet 선택 로직은 수정하지 않고 레이아웃 위치와 폭만 바꾼다.
- 챗봇: 선택 지역 원자료 snapshot, 현재 구조화 기획안, 허용된 공식 웹 자료를 입력으로 사용한다. `설명`, `공식 사례 조사`, `기획안 수정안 제안`을 지원하며 수정안은 자동 저장하지 않고 사용자가 적용해야 한다.
- 출력: 화면은 그래프·카드·지도 중심으로 유지하고, AI 문장은 짧고 쉬운 한국어로 제한한다. 기획서의 사실 수치와 예측·제안은 계속 분리한다.
- 이유: bid3의 균형 잡힌 업무공간 UX를 재사용하면서 TP2의 데이터·ML·RAG·5-Agent 전문성을 잃지 않기 위해서다.

### D-032 TP2-3 개발 포트 분리와 반응형 소개 화면

- 상태: 승인됨
- 결정: TP2-2와 동시에 실행할 수 있도록 TP2-3의 로컬 개발 실행은 Backend `8100`, AI Server `8111`, Frontend `5176`을 사용한다. Windows에서 `8101`이 예약되어 AI 서버가 시작하지 않는 환경과 다른 프로그램이 `5175`를 점유한 환경을 피하기 위한 변경이다. 배포 시에는 기존 Nginx `/`, `/api`, `/ai` 구조를 유지한다.
- 메인 화면: `/`는 bid3의 상단바·중앙 소개 문구·4단계 입체 카드 전환을 관광 업무 흐름으로 바꾼 반응형 소개 화면으로 사용한다. `/dashboard`는 지도 중심 분석 업무공간으로 사용한다.
- 이유: 같은 PC에서 TP2-2와 TP2-3 포트가 충돌해 다른 프로젝트 서버가 응답하는 문제를 막고, 소개 화면과 실제 업무화면의 목적을 명확히 분리하기 위해서다.

### D-033 실제 데이터 안전 표시와 로딩 성능 개선

- 상태: 승인됨
- 결정: `/`와 `/dashboard`는 React 지연 로딩으로 분리한다. 원본 ZIP·CSV 파싱은 `ai_server/app/raw_data_repository.py` 한 곳에서 담당하고, 상대경로·크기·수정시각 지문이 같은 동안만 LRU 캐시를 재사용한다. Backend·AI Server의 큰 JSON 응답에는 GZip을 적용한다. 키를 제거한 VWorld GeoJSON은 지도 표시용으로 단순화해 24시간 로컬 디스크 캐시를 재사용하고, 브라우저에는 선택 시도의 시군구만 반환한다.
- 표시 기준: 지원 지역의 FastAPI 응답이 성공한 경우에만 실제 수치를 표시한다. 미지원·오류 지역은 개발용 예시 숫자로 대체하지 않고 빈 상태와 원자료 연결 안내를 표시한다.
- 이유: 첫 화면의 JavaScript 전송량, 반복 ZIP 파싱, 전국 경계 전송량을 줄이면서 데이터 출처가 불명확한 숫자가 실제 값처럼 보이는 문제를 방지하기 위해서다.
- 주의: 이 개선은 MySQL 적재나 ML 예측 완료를 뜻하지 않는다. 현재 구현·부분 구현·미구현 범위와 측정값은 `docs/PROJECT_AUDIT.md`에서 관리한다.

### D-034 분석 업무 메뉴의 독립 페이지 연결

- 상태: 승인됨
- 결정: 사이드바의 `관광 진단`, `AI 전략기획`, `기획서 제작`을 버튼이나 빈 앵커로 두지 않고 `/diagnosis`, `/strategy`, `/proposal` 독립 경로로 제공한다. 각 페이지는 해당 업무의 3단계 흐름과 지원 지역을 보여 주고, 실제 분석·생성은 `/dashboard`의 지역 선택과 원자료 검증 흐름으로 연결한다.
- 이유: 기존에는 관광 진단이 같은 화면의 앵커였고 나머지 두 메뉴가 동일한 보고서 함수에 연결되어 별도 페이지처럼 열리지 않았다. 업무 목적과 현재 위치를 명확히 하고, 이후 팀원이 기능별 페이지를 독립적으로 확장할 수 있게 한다.
- 주의: 별도 페이지를 추가했다고 원자료·AI 기능을 복제하지 않는다. 실제 지표 계산과 보고서 생성은 기존의 단일 검증 경로를 재사용한다.

### D-035 지역 선택의 Open API 상세보기 분리

- 상태: 승인됨
- 결정: 지역 선택 화면의 `AI 전략기획서 생성` 버튼은 제거하고, 해당 위치에 `지역 정보 상세보기` 버튼을 둔다. 버튼은 팝업에서 관광데이터랩 원자료 요약과 한국관광공사 국문 관광정보 Open API의 관광자원·분류·주소·제공 이미지를 출처별로 나누어 표시한다.
- 이유: 지역 선택은 현황 확인에 집중하고, 유료·시간이 걸릴 수 있는 AI 기획안 생성은 전용 `AI 전략기획` 업무 페이지에서만 수행하게 하기 위해서다.
- 주의: Open API 관광자원 목록은 방문자·관광소비 수치가 아니며, API 실패·빈 결과를 임의 정보로 대체하지 않는다. 인증키는 AI Server `.env`에서만 읽고 React에는 노출하지 않는다.

### D-036 페이지 이동에도 유지되는 AI 전략기획 작업

- 상태: 승인됨
- 결정: AI 전략기획 생성은 동기 HTTP 응답 대기가 아니라 AI Server의 백그라운드 작업으로 등록한다. 브라우저는 지역별 작업 ID를 localStorage에 저장하고 3초 간격으로 상태를 조회한다.
- 이유: Agent 조사·작성·검수는 수십 초 이상 걸릴 수 있으므로, 사용자가 다른 업무 페이지나 탭으로 이동해도 서버 작업과 진행 상태를 유지하기 위해서다.
- 주의: 현재 개발 단계는 AI Server 프로세스 메모리 작업 저장소다. 서버 재시작·배포 중단까지 복구하는 영속 작업 큐는 MySQL/Redis 운영 단계에서 별도로 구현한다. 완료된 결과는 브라우저의 저장 기획서 목록에도 보관한다.

### D-037 AI 주도 전략 설계를 위한 사업 여건 입력 (2026-08-28)

- 상태: 사용자 승인 및 구현.
- 결정: 지역선택 다음에 `/planning` 전략기획 페이지를 둔다. 예산·시기·자원·필수 제약을 중심으로 묻고 현장 정보·선호·문서는 선택으로 둔다. 사용자가 해결책·타깃·사업 내용을 미리 작성해야 하는 서비스로 만들지 않는다.
- AI 역할: 공식 수치와 사례에서 문제·기회를 발견하고 대안을 검토해 목표·방법·실행안을 제안한다. 확정 조건과 선호를 분리하며, 현실적으로 불가능한 조건은 대안/확인 사항으로 설명한다.
- 전달: 조건은 Pydantic으로 검증하고 공식 snapshot과 분리하여 Evidence, Case Scout, Transferability, Planner, Reviewer와 챗봇에 전달한다. 조사 캐시에 조건을 반영하고 생성 당시 조건을 결과에 고정한다.
- 자료: 한글(HWPX)·Word(DOCX)·PDF·TXT/MD·Excel(XLSX)의 텍스트만 최대 3개 읽는다. 첨부 원본·텍스트는 서버 DB와 공용 RAG에 등록하지 않고, 문서 속 명령은 시스템 지시로 취급하지 않는다. 기존 HWP는 안전한 텍스트 추출을 위해 HWPX/PDF 변환을 안내한다.
- 범위: TP2-3만 변경, 지도와 원자료 유지. 모델·토큰 설정, DB 구조, ML 학습은 변경하지 않는다. PDF·Excel 텍스트 추출을 위해 `pypdf`, `openpyxl`을 추가한다.
- 저장 한계: 지역별 초안·보고서는 브라우저 저장, 서버 작업은 프로세스 메모리다. 다중 사용자 공유·서버 재시작 복구는 이번 범위가 아니다.

### D-038 ML 전망을 공식 조사와 5-Agent 기획에 연결 (2026-08-28)

- 상태: 사용자 승인 및 구현.
- 결정: 지역 모델은 시간순 Train/Validation/Test로 평가하며 모델 선택에는 Validation만 사용한다. 마지막 Test는 선택 후 보고하고, 화면 사용과 같은 1~3개월 재귀 예측도 별도로 점검한다.
- 기획 연결: OpenAI 호출 전에 저장 모델의 전망과 검증 기록을 `snapshot.ml_analysis`로 만든다. 전망 기간은 D-041의 일정 정책을 따르며, Evidence와 Case Scout는 전망 신호로 공식 자료·비교사례 질문을 좁히고, Transferability·Planner·Reviewer는 같은 근거를 사용한다.
- 안전 기준: 예측은 기존 이력 기반 자연 추세이며 정책 미실행 반사실·사업 효과·추가 매출이 아니다. 미지원·오래된 모델은 수치를 만들지 않고 관측 자료로만 진행한다. 지역코드·월 연속성·데이터 hash가 다르면 모델을 거절한다.
- 범위: 현재 등록·검증 지역은 강남구 11680이다. 다른 시군구는 같은 공통 인터페이스를 사용하되 공식 원자료 정의와 개별 성능 검증 후 등록한다.

### D-039 등록 모델 기반 내부 ML 학습 페이지 (2026-08-29)

- 상태: 사용자 승인 및 구현.
- 결정: 로고의 작은 점으로만 들어가는 `/ml-test` 페이지를 React/Vite 안에 만든다. 별도 Streamlit 서버는 추가하지 않는다.
- 자동 확장: AI Server가 `region_registry`와 저장 메타데이터의 `target`을 순회해 지역과 결과 모듈을 생성한다. 새 지역·Target이 같은 계약으로 등록되면 화면 카드도 자동으로 늘어난다.
- 표시 내용: 공식 학습 데이터, Open API 사용 여부, 선택 모델, 입력 Feature, 실제 Python 함수, 시계열 기법, Train/Validation/Test, MAE·RMSE·MAPE, 전년 동월과 3개월 전망을 표시한다.
- 범위: 학습 전용 읽기 화면이며 온라인 재학습·OpenAI 호출·지도 코드는 변경하지 않는다. 현재 실제 검증 지역은 강남구이고 D-040 확장 뒤 결과 모듈은 7개다.

### D-040 체류·검색 ML Target 확장 (2026-08-29)

- 상태: 사용자 승인 및 구현.
- 결정: 강남구 2024.01~2026.07 공식 월별 원자료에서 숙박방문자 비율, 평균 체류시간, 내비게이션 목적지 검색량, 숙박 목적지 검색량을 추가한다. 기존 3개와 합쳐 7개 Target을 같은 시간순 Train/Validation/Test와 전년 동월 기준선으로 평가한다.
- 기획 연결: Agent에는 7개 전망을 그대로 전달하되 공식 조사 질문은 `관심→방문`, `숙박 관심→실제 체류`, `관광소비` 세 묶음으로 압축한다. 검색량은 방문·예약 관측값으로 표현하지 않는다.
- 전국 확장: 새 지역도 같은 열 정의를 검증한 전처리 어댑터와 지역코드 등록 후에만 학습한다. 강남 모델을 다른 시군구에 복사하지 않는다.
- 제외: 업종별 소비예측은 전국 업종 분류 일관성 검증 뒤 별도 다중 Target으로 검토한다. SNS 언급량은 관광 수요와의 관계가 약하므로 이번 Target에서 제외한다.

### D-041 기획 일정 기반 동적 ML 전망 범위 (2026-08-29)

- 상태: 사용자 승인 및 구현.
- 결정: 기획안 일정이 미정이면 향후 3개월·6개월 전망을 각각 집계해 공식 사례와 함께 비교하고, Planner가 실행 가능한 기간 하나를 선택한다. 희망 시작일·종료일이 있으면 원자료 최신월 다음 달부터 종료월까지 예측해 실제 사업기간과 겹치는 월만 기획 근거로 사용한다.
- 검증 경계: 현재 강남구 모델은 1~3개월 재귀 백테스트만 수행했다. 4~12개월 계산값은 탐색 전망으로 표시하며, 12개월 밖 수치는 생성하지 않는다. 입력 일정 전체를 포함하지 못하면 `coverage_complete=false`로 전달한다.
- 품질 규칙: 전망은 자연 추세이며 정책 미실행 반사실·사업 효과·추가 매출이 아니다. Reviewer는 기획안 기간과 ML 구간 불일치, 탐색 전망의 과도한 확신, 범위 밖 수치 생성을 중대 오류로 판정한다.

### D-042 학습 페이지 ML 전용 챗봇 (2026-08-29)

- 상태: 사용자 승인 및 구현.
- 결정: `/ml-test`의 본문을 왼쪽으로 배치하고 오른쪽에 스크롤을 따라오는 `ML 챗봇`을 둔다. 모바일·좁은 화면에서는 본문 아래의 일반 패널로 전환한다.
- 근거: 챗봇은 선택 지역의 등록 모델 카탈로그, 함수·파일 역할, 평가 결과, 동적 전망기간 규칙만 OpenAI Responses API에 전달한다. 구조화 JSON으로 답하고 브라우저에 키·프롬프트를 노출하지 않는다.
- RAG 범위: 현재 정보는 짧고 구조화되어 있어 RAG와 웹 검색을 사용하지 않는다. 향후 수업자료·논문·긴 기술문서를 추가할 때만 출처 메타데이터가 있는 별도 학습 문서 컬렉션을 검토한다.

### D-043 자동 탐색형 OpenAI·React 관리자 학습 페이지 (2026-08-29)

- 상태: 사용자 승인 및 구현.
- 결정: 기존 `/ml-test` 옆에 `/openai-test`, `/react-test`를 추가하고 공통 탭으로 이동한다. 두 페이지는 파이프라인 다이어그램, 파일 역할, route·기술 목록과 전용 학습 챗봇을 제공한다.
- 자동 갱신: AI Server가 요청 시 Python AST와 안전한 정규식으로 Agent·FastAPI route·React src·App route·fetch endpoint·package.json을 다시 읽는다. 새 파일이나 route가 같은 구조로 추가되면 새로고침 후 목록에 반영한다.
- 보안: `.env` 실제 값, API 키, 소스 전문, 로컬 절대경로는 반환하지 않는다. 환경변수는 `.env.example`의 이름만 보여 준다.
- 수업자료: 저장소에서 선생님이 준 별도 과제 원문은 확인되지 않았다. 현재 페이지에는 프로젝트 문서에 확정된 React/Vite·컴포넌트 재사용·API 분리·lint/build 기준만 표시하며, 과제 원문을 받으면 검증된 학습자료로 추가한다.

### D-044 강사 구조도 기반 학습 페이지 시각화 (2026-08-29)

- 상태: 사용자 승인 및 구현.
- 결정: 강사 제공 시스템 구조 자료를 `/react-test`의 학습 기준으로 반영한다. 현재 로컬 개발은 React/Vite·Backend FastAPI·AI FastAPI를 분리하고, MySQL·Joblib·ChromaDB·OpenAI 연결을 표시한다. AWS EC2·Nginx는 `추후 배포 예정`으로 별도 표시한다.
- 자동 반영: 로컬 포트는 `start-dev.ps1`, 폴더 수와 대표 파일은 `frontend/src`, route와 의존성은 `App.jsx`와 `package.json`에서 읽는다. 세부 파일·route는 기본 화면에서 접고 필요할 때만 펼친다.
- OpenAI·ML: OpenAI 페이지는 5-Agent의 병렬 조사와 순차 검수 관계를 중심으로 표시하고, ML 페이지는 오프라인 학습과 온라인 추론을 분리해 웹 요청마다 재학습하지 않는 원칙을 표시한다.
- 제외: 강사 예시의 Gemini·Fine-tuning 등 현재 TP2-3에서 사용하지 않는 기술은 사용 중인 기술처럼 표시하지 않는다.

### D-045 관리자 학습 챗봇의 짧은 응답·실제 연결 상태 (2026-08-31)

- 상태: 사용자 요청 및 구현.
- 결정: ML·OpenAI·React 학습 챗봇은 보고서 Agent와 별도 설정으로 `low` 추론·낮은 상세도·출력 상한 800을 사용한다. 답변은 4문장, 핵심 포인트·관련 파일은 각각 최대 3개로 제한한다.
- 상태 표시: 브라우저는 AI Server의 상태 endpoint를 30초마다 확인한다. API 키 없음·모델/네트워크 오류·실제 채팅 실패는 빨간 `Inactive`로, 정상 연결은 초록 `Active`로 표시한다.
- 비용: 연결 확인은 Models API 조회만 하므로 답변 토큰을 생성하지 않는다. 실제 질문에만 OpenAI 토큰이 사용된다.

### D-046 팀원용 최초 설치 자동화 (2026-08-31)

- 상태: 사용자 요청 및 구현.
- 결정: Git clone 뒤 PC별로 필요한 `backend/.venv`, Python 패키지, `frontend/node_modules`, `.env` 예시 파일을 `setup-dev.ps1`에서 준비한다. 실행 서버는 기존 `start-dev.ps1`가 계속 담당한다.
- 보안·데이터: 실제 `.env`의 API 키·DB 비밀번호, 라이선스·용량 이슈가 있는 `data/raw` 원본, 저장된 문서는 Git에 포함하지 않는다. 설치 스크립트는 비밀값을 만들거나 복사하지 않고, 원본 파일 누락만 경고한다.
- 재현성: Python은 루트 `requirements.txt`, 프런트엔드는 `package-lock.json` 기반 `npm ci`로 설치한다. 이미 설치된 패키지를 다시 맞출 때는 `-Refresh` 옵션을 사용한다.

### D-047 공통 지역 ML 어댑터와 기획안 결정적 품질 게이트 (2026-08-31)

- 상태: 사용자 요청 및 구현.
- 결정: 동일한 관광데이터랩 직접 다운로드 CSV는 `regional_datalab_data.py` 하나에서 7개 공통 Target으로 변환한다. 지역별 파일은 원본 경로·지역 코드·예외 열 정의만 둔다. 예측 알고리즘은 `RegionForecastSettings`로 공유하되, 원자료·전처리 결과·Joblib·메타데이터는 지역 코드별로 분리한다.
- 등록: 계양구(`28245`)의 2024.01~2026.06 직접 CSV는 월 연속성·공통 Target 30개월 검증 뒤 시간순 학습으로 등록했다. Target별 모델은 Validation에서만 선택하며, Test 또는 1~3개월 재귀 평가가 기준선보다 나쁜 Target은 우수하다고 주장하지 않는다.
- 품질: Planner 출력 뒤 `plan_quality_gate.py`가 실제 source_id, 5단계 일정·행동·산출물, 운영 방식, 비용 산식, KPI 측정 규칙을 확인한다. critical·major 누락은 Reviewer의 승인 상태를 막고 Planner에 한 번 보완 요청한다.
- 이유: 50개 이상 시군구로 확장할 때 코드 복사와 타 지역 모델 오적용을 막고, Agent가 만든 문장이 그럴듯해도 근거와 집행 방법이 비어 있으면 기획안으로 통과하지 않게 하기 위해서다.
- 주의: 원본 파일의 제공기관 URL·다운로드 일시·이용 조건은 원본과 분리된 `data/catalog/region_data_registry.csv`에 각 데이터 묶음별로 팀이 보완해야 한다. 원본 파일 또는 원본 레지스트리의 값을 코드가 추정·수정하지 않는다.

### D-048 카탈로그 기반 지역 ML 사전점검 (2026-08-31)

- 상태: 사용자 요청 및 구현.
- 결정: `data/raw/` 원본과 분리된 `data/catalog/region_data_registry.csv`를 지역 확장 등록표로 사용한다. 표준 관광데이터랩 CSV는 지역 코드·원본 상대 경로·출처 상태 한 줄만 추가하면 `standard_region_pipeline.py`가 공통 7개 Target 학습/예측 함수를 만들고 `region_registry.py`가 자동 등록한다.
- 사전점검: `python -m ai_server.ml.scripts.check_regions --region-code <code>`가 원본 위치, 7개 Target, 월 연속성, 관측 기간과 출처 메타데이터를 읽기 전용으로 검사한다. 통과 후에만 `train_regions.py`로 Joblib을 만든다.
- 출처: 현재 강남구·계양구 카탈로그는 공식 포털 URL과 다운로드 날짜를 기록했지만, 파일별 정확한 다운로드 상세 URL·이용 조건을 확인하지 않아 `needs_exact_download_url` 상태다. 이를 `verified`로 임의 변경하지 않는다.
- 이유: 50개 이상 시군구의 코드 복사와 원본 훼손을 막고, 어떤 지역이 왜 학습·미등록인지 팀원이 CLI 결과만으로 판단할 수 있게 한다.

### D-049 공식 사례 레지스트리 검증과 전국 저장 분리 (2026-08-31)

- 상태: 사용자 요청 및 구현.
- 결정: 전국 월별 수치, 지역별 Joblib, 공식 사례 카드, 긴 RAG 문서의 저장 위치와 역할을 분리한다. 현재 공식 사례 4건은 Pydantic 스키마로 필수 필드·HTTPS URL·중복 source_id를 검증한다.
- 확장: 전국 수치는 `region_code + year_month` 기준으로 전처리·MySQL 조회·지역별 모델에 사용하고, 공식 정책 사례는 사람이 원문을 검수한 뒤 JSONL/ChromaDB에 별도로 추가한다.
- 이유: 전국 수치가 늘었다고 사례 문서가 자동으로 늘어난 것으로 오해하지 않고, LLM에는 선택 지역 요약과 관련 Top-K 사례만 전달해 정확도·비용을 관리하기 위함이다.

### D-050 Reviewer 승인 기준의 코드 강제 (2026-08-31)

- 상태: 사용자 요청 및 구현.
- 결정: Reviewer가 `approved=true`를 반환해도 82점 미만이거나 critical 이슈가 있으면 Python 품질 게이트가 최종 승인을 차단한다. 기존 source_id·5단계·운영 방식·예산 산식·KPI 점검과 함께 적용한다.
- 이유: 프롬프트 규칙은 모델이 항상 지킨다고 보장할 수 없으므로 코드로 판정 가능한 승인 조건을 결정적으로 적용하기 위함이다.

### D-051 Word의 ML 자연추세와 사용자 목표 분리 (2026-08-31)

- 상태: 사용자 요청 및 구현.
- 결정: Word 문서에서 최근 월 고정선을 `미실행 예측`으로 부르거나 임의 +5%/+8%를 기본 생성하지 않는다. 저장 ML 전망이 있으면 자연추세를 표시하고, 사용자가 직접 목표율을 입력했을 때만 별도 목표선과 필요한 수준을 산술 계산한다.
- 이유: 기존 이력 모델이 학습하지 않은 정책 인과효과·추가 매출을 예측한 것처럼 보이지 않게 하기 위함이다.

### D-052 MySQL 전략기획 작업 상태와 재시작 복구 (2026-08-31)

- 상태: 사용자 요청 및 구현.
- 결정: `strategy_report_jobs`에 작업 상태와 첨부 본문을 제거한 요청 조건을 저장한다. 첨부가 없는 queued/running 작업은 AI Server 시작 시 다시 예약한다. 첨부가 있던 작업은 문서 본문을 저장하지 않으므로 자동 재개하지 않고 안전한 실패 상태와 재요청 안내를 남긴다.
- 완료 결과: `strategy_reports`와 서버 Word/PPT 저장소를 계속 사용하며, 상태 API는 메모리에 없을 때 MySQL을 조회한다.
- 이유: 페이지 이동뿐 아니라 서버 재시작 이후에도 작업 상태와 완료 결과를 복구하면서 참고문서 비저장 약속을 지키기 위함이다.

### D-053 기획안 재작성 입력 축소와 관측·ML 기준월 일치 (2026-08-31)

- 상태: 실제 강남구 5-Agent 생성 검증 후 구현.
- 재작성: 최초 검수에 실패한 Planner에는 `previous_draft`를 필수로 전달한다. 기존 초안과 무관한 근거를 모두 다시 보내지 않고 관측값·ML·사용자 조건, 인용/추천 사례, 최대 16개 관련 출처만 전달한다. 재작성 추론은 `medium`으로 실행하고 지적받지 않은 내용은 유지한다.
- 병목 확인: 각 OpenAI 단계의 시작·완료·실패 코드와 시간을 서버 로그에 남기고, 사용자 오류에도 `evidence`, `case_scout`, `transferability`, `planner_draft`, `reviewer_first` 중 실패 단계를 포함한다. 이미 수집된 사례를 정형 비교하는 Transferability는 `medium` 추론·7,000 출력 상한으로 조정한다.
- 기준월: 등록 ML 지역의 AI snapshot은 같은 지역 `load_history()`의 최신 확정월과 방문·소비·체류·검색 수치를 사용한다. 강남구의 일반 원본 묶음이 2026-06까지이고 최신 보완 원본이 2026-07까지인 경우에도 관측 근거와 ML이 모두 2026-07을 기준으로 한다.
- 지표별 날짜: SNS처럼 보완 원본에 포함되지 않은 지표는 2026-07로 바꾸지 않고 실제 최신월 2026-06을 별도 표시한다.
- 이유: 실제 생성에서 기존 초안을 받지 않은 재작성 호출이 214초 뒤 출력 미완료로 끝났고, 관측 snapshot과 ML의 마지막 월도 서로 달랐다. 같은 사실을 다시 조사하는 비용을 줄이고 기준월 오류를 차단하기 위함이다.

### D-054 전국 원본 보관소와 오프라인 파이프라인의 TP2-3 통합 (2026-09-01)

- 상태: 사용자 요청 및 구현.
- 결정: 전국 지역 자료의 inventory·중첩 ZIP 병합·표준화·MySQL 적재 후보 생성·패널 ML 자산은 `data_pipeline/`으로 분리한다. 정리된 결과는 `data/processed/nationwide/`, 모델 산출물은 `artifacts/nationwide/`, MySQL 스키마는 `database/mysql/`에 둔다.
- 원본: 팀 공유폴더의 공식 ZIP/CSV는 원본 보관소로 유지하며 TP2-3 Git에 복사하지 않는다. 서비스 요청은 원본을 다시 읽지 않고, 검증·적재된 MySQL 수치와 저장된 모델만 사용한다.
- 안전: 기존 `ai_server/ml/`의 강남·계양 지역 모델과 화면 동작은 변경하지 않는다. 전국 결과는 MySQL 적재·지역별 평가·AI 입력 연결을 완료하기 전까지 자동으로 기획안 근거로 사용하지 않는다.
- 이유: 실제 프로젝트 폴더를 TP2-3 하나로 통일하면서도 기존 팀원 기능을 덮어쓰지 않고, 전국 데이터 확장 과정과 근거를 재현 가능하게 보관하기 위해서다.

### D-055 전국 비교 입력·지역별 ML 품질 게이트·기획안 사후 측정 분리 (2026-09-01)

- 상태: 구현 완료, 실제 MySQL 적재는 DB 서비스·계정 설정 대기.
- 전국 비교: `regional_planning_context`와 `regional_peer_comparison`의 검증된 12개월 관측값만 AI snapshot에 읽기 전용으로 넣는다. DB가 없거나 해당 지역이 없으면 기존 지역 원자료 흐름을 유지하며, peer는 전국 평균이나 정책 효과로 표현하지 않는다.
- ML: 166개 지역의 4개 핵심 target은 지역별 seasonal-naive 시간순 validation/test 기준선을 평가한다. 새 후보 모델은 같은 지역·target에서 두 구간 모두 기준선을 이겨야만 `decision_usable`로 등록한다. 기준선 평가표 자체는 예측 모델이나 정책 효과가 아니다.
- 사례: 사례 레지스트리는 공식 원문 URL·운영방식·기간·성과 해석 한계가 확인된 카드만 보관한다. 2022 숙박할인쿠폰과 2025 야간관광 순회 운영 사례를 추가하되, 전국 사업 평가를 특정 지역의 인과효과로 쓰지 않는다.
- 사후 측정: 보고서 저장 시 실제 원자료의 방문·소비·숙박·체류 기준값만 `strategy_measurement_baseline`에 저장한다. 목표·ML 자연추세·LLM 기대효과는 저장하지 않으며, 이후 공식 원자료 실측치는 별도 follow-up API로 기록한다.
- 이유: 비교·예측·사례·성과평가의 역할을 섞지 않아 기획서의 근거성은 높이고, 근거 없는 효과 수치와 불필요한 LLM 토큰 사용은 줄이기 위함이다.

### D-056 공식 이미지·ML 목표 비교를 포함한 8장 PowerPoint 실행기획서 (2026-09-01)

- 상태: 사용자 제공 디자인 참고자료를 기준으로 구현 완료.
- 구성: PowerPoint를 `표지 → 핵심 판단 → 데이터 진단 → 추천 전략 → ML 자연추세/실행 목표 → 예산/KPI → 5단계 로드맵 → 출처/ML 방법`의 8장으로 고정한다. 긴 서론은 추가하지 않는다.
- 디자인: 밝은 회색 배경, 네이비·시안·오렌지 강조, 큰 제목, 얇은 선, 넓은 여백, 직사각형 정보면과 지역 사진을 사용한다. 공식 관광 Open API 이미지가 없으면 같은 레이아웃의 추상 배경으로 대체한다.
- 수치 기준: `ML 자연추세`는 저장 모델의 기존 이력 전망이고, `실행 목표`는 사용자가 입력한 목표율에 도달하려면 필요한 월별 수준이다. 두 선의 차이는 목표 규모이며 정책 인과효과·보장 매출로 표현하지 않는다.
- 기간: 기본 3개월, 사용자가 확정한 희망 일정과 ML 전망 창이 있으면 최대 6개월까지 사용한다. 모델이 없는 지역은 수치를 만들지 않고 목표 비교 장에 미지원 상태를 표시한다.
- 코드: endpoint 호환 진입점은 `proposal_presentation.py`, 슬라이드 구성은 `proposal_presentation_v2.py`, 공통 시각요소는 `presentation_theme.py`로 분리한다.

### D-057 편집 가능한 12장 PowerPoint 템플릿과 버전 캐시 (2026-09-02)

- 상태: 사용자 승인 및 구현. 이 결정은 D-056의 8장 PowerPoint 구성을 대체한다.
- 템플릿: 승인된 편집형 원본 `ai_server/app/templates/tourism_strategy_12_slide_template.pptx`를 사용한다. 배경을 한 장의 이미지로 붙이지 않고 PowerPoint의 텍스트·도형·표·차트 요소를 유지해 사용자가 직접 수정할 수 있게 한다.
- 구성: 12장은 `표지 → 목차 → 데이터 근거·기획 배경 → 문제·시범 목표 → 참여자·이용 시나리오 → 핵심 사업 구성 7가지 → 실행 Flow·산출물 → 유사 지역 비교·차별점 → ML 자연추세·사용자 목표 → 예산 산식·KPI → 5-Agent·공식 근거 파이프라인 → 기대효과·확대 판단` 순서로 만든다. 긴 서론보다 지역 진단, 실행안, 예산·KPI·로드맵과 근거를 우선한다.
- 지역 이미지: 템플릿의 여섯 개 사진 프레임에는 해당 지역의 공식 관광 Open API 이미지를 우선 사용한다. 공식 이미지가 없으면 다른 지역 사진을 남기지 않고 중립 이미지를 사용한다.
- 수치 기준: PowerPoint의 네이티브 차트에는 저장 ML의 자연추세와 사용자가 직접 입력한 실행 목표만 표시한다. 두 선의 차이는 목표 수준이며 정책 미실행 반사실·인과효과·추가 매출 보장으로 표현하지 않는다. 등록 ML이 없는 지역은 수치를 만들지 않고 미지원 상태를 명시한다.
- 코드: 기존 endpoint 호환 진입점 `proposal_presentation.py`는 유지하고, 12장 템플릿 매핑은 `proposal_presentation_v3.py`가 담당한다. 생성 결과에는 슬라이드별 출처와 방법 메모를 남긴다.
- 캐시: 저장 보고서가 수정되면 기존 Word/PPT 경로를 무효화한다. 문서 내용 지문과 렌더 버전이 모두 일치할 때만 캐시를 재사용하여 템플릿·생성기 변경 뒤 이전 8장 문서가 다시 내려가지 않게 한다.

### D-058 Hybrid Multi-LLM Router와 관리자 Control Center (2026-09-02)

- 상태: 사용자 요청 및 구현.
- 결정: Provider 선택은 `ai_server/app/llm/`의 Router가 단독으로 담당한다. Evidence·Case Study의 공식 Web Search와 Reviewer는 OpenAI에 고정하고, Transferability·Planner는 개인 노트북 Ollama의 Qwen·Gemma를 기본으로 사용한다. 로컬 구조화 출력·연결이 실패하면 Web Search가 필요하지 않은 작업만 OpenAI로 한 번 폴백한다.
- 안전: OpenAI Web Search가 실패한 경우 로컬 모델이 조사한 것처럼 대체하지 않는다. 원자료/ML/RAG의 역할은 바꾸지 않고, Agent는 Provider별 HTTP 형식이나 IP를 알지 않는다.
- 운영: `/llm-control`은 실제 Provider 상태, 모델 목록, 라우팅 스냅샷, 완료된 호출 trace를 표시한다. 설정 변경은 서버 `.env`의 `LLM_ADMIN_TOKEN`이 있어야 하며, 모델·키·IP·토큰은 프런트엔드에 노출하지 않는다.

### D-059 개발 PC·개인 GPU PC의 LAN Ollama 연결 안정화 (2026-09-02)

- 상태: 사용자 요청 및 구현.
- 결정: 개인 GPU 노트북은 Ollama 추론 서버만 실행하고, 학원 개발 PC의 React·Backend·AI Server는 프로젝트 루트 `.env`의 `LOCAL_LLM_BASE_URL`을 통해 호출한다. `start-dev.ps1`는 `.env`를 자식 서버 창에 전달한다.
- 안정성: 빈 Windows 환경변수는 `.env`의 실제 LAN 주소를 덮어쓰지 않는다. OpenAI Web Search·최종 검수와 Ollama 구조화 생성의 역할 분리는 D-058을 유지한다.
- 보안: Ollama 포트 11434는 사설망 개발 PC만 허용하며, API 키·관리자 토큰·개인 노트북 IP는 React 코드에 넣지 않는다.

### D-060 생성 전 후보 선정·근거·Hybrid 검토 (2026-09-02)

- 상태: 사용자 요청(생성 전 전반 검토 및 필요한 개선)에 따라 구현. 기존 팀원 코드·12장 템플릿·지역 레지스트리는 유지한다.
- 기획: 서로 다른 후보 2~3개, 지역 적합성, 기존 사업과 차이, 확보 조건, 비용·측정·중단 기준과 선택 이유를 구조화한다. `planning_decision`을 보고서에 보존하고 미리보기에서 확인한다. 독창성·실행 가능성을 자동으로 보장하는 것은 아니다.
- 검수: 정확한 출처 ID·미등록 인용·단계 1~5·후보 참조를 검사한다. 82점 이상이어도 critical/major가 남으면 미승인이다. Hybrid 재작성/검수 오류는 미승인 초안을 보존한다. 챗봇 수정 후 과거 승인 배지를 유지하지 않는다.
- 검색: 필수 Web Search 실행과 조회 URL을 확인한다. URL 일치만으로 주장의 진실성을 보장하지 않아 독립 검수를 유지한다. 통신 실패 자료는 정상 캐시로 장기 재사용하지 않는다.
- 기간: 일정 미정은 현재 월 이후 3·6개월 실행 후보를 비교한다. 관측 공백 월은 재귀 예측하되 실행 합계에서 제외한다. 신뢰도는 마지막 관측월로부터의 거리로 판단하고 12개월 밖은 외삽하지 않는다.
- Hybrid: Ollama 출력 상한·문맥 크기·토큰 통계를 반영한다. 근거 원문을 자르지 않고 한국어·영문 혼합 JSON의 보수적 토큰 추정과 실제 Ollama 입력 토큰을 함께 사용한다. 실제 모델 문맥을 넘으면 설정된 폴백/실패로 처리하며, local_only는 유료 폴백을 금지한다. active는 정확한 모델 태그 연결 확인이지 품질 보장이 아니다.
- 후속 승인: 사용자 승인 후 `jeonnam.go.kr`, `evaluation.go.kr`, `jeonju.go.kr`만 기존 허용 목록에 추가했다. `.env.example`도 동일한 개별 목록으로 맞추었고 전체 `go.kr` 허용 확대는 적용하지 않았다. 실제 설정으로 사례 6/6건 통과를 확인하고 진행 중 기획안이 없는 상태에서 AI 서버를 재시작해 반영했다. Chroma 문서는 현재 0건이며, 검수 JSONL·공식 웹 조사가 실제 텍스트 근거다.
- 재현 명령과 검증 결과: `docs/GENERATION_READINESS_20260902.md` 참조.

### D-061 단계별 출력 예산과 제한된 토큰 부족 재시도 (2026-09-02)

- 상태: 사용자가 토큰 설정 조정을 요청하여 구현. D-053의 Transferability 7,000은 OpenAI 경로에서 대체하고 Qwen의 로컬 상한으로만 유지한다.
- 배경: 실제 생성에서 Qwen은 보수적 문맥 예산 검사로 OpenAI에 폴백했고, 적용 검토는 7,000 출력 상한에서 중단됐다. 공식 사례 조사에도 미완료 기록이 있어 5-Agent 전체 출력 여유를 검토했다.
- 결정: OpenAI 최초/재시도 상한은 Evidence 10,000/16,000, Case Scout 16,000/24,000, Transferability 16,000/24,000, Planner 24,000/32,000, Planner 수정 20,000/28,000, Reviewer(재검수 포함) 12,000/20,000이다. 모델·추론 강도·품질 승인 기준은 유지한다.
- 회복: `incomplete + max_output_tokens`일 때만 해당 요청을 1회 새로 생성한다. 근거·출처·스키마를 자르거나 부분 JSON을 합치지 않는다. 인증·결제·통신·콘텐츠 필터 오류는 재시도하지 않는다. 기존 기획안 품질 미달 시 1회 수정과는 별도다.
- 시간·비용: 기획 Agent의 OpenAI HTTP 한 번 대기는 최대 600초, 토큰 부족 시 최대 2회다. 무한 재시도하지 않으며 실제 사용량만 기록한다. 로컬 출력 상한·문맥 크기와 학습 챗봇 800 상한은 유지한다.
- 추적: OpenAI 성공·실패·재시도 사용량과 폴백 실패를 보존한다. API가 사용량을 알려주지 않으면 미확인으로 표시한다. 프롬프트·응답 원문·키는 trace에 저장하지 않는다.
- 검증: 외부 LLM 응답을 대역으로 재현하는 테스트를 추가했다. 실제 유료 기획안의 성공률·품질 개선 완료를 주장하지 않으며 실행 결과는 `GENERATION_TOKEN_BUDGETS.md`에 기록한다.

### D-062 Qwen·Gemma 역할별 지시와 요청 범위 근거 도구 (2026-09-02)

- 상태: 사용자의 로컬 LLM 페르소나·명령·도구 구현 요청을 반영했다. 현재 Hybrid 모드와 유료 폴백 정책은 임의로 변경하지 않았다.
- 결정: Qwen 적용성 비교, Gemma 초안·보완에 Ollama 네이티브 함수 호출을 연결한다. 공식 관측값, 동일 기간 지역 비교, 저장 ML·검증 한계, 기존 수집 사례, 선정 판단, 요청 내 세부 근거를 읽는다. 실제 도구 실행은 Python이 허용 목록과 JSON Schema를 검사한 뒤 담당한다.
- 품질: 출처/숫자/기간은 원본 그대로 보존하고 긴 파생 해석은 별도 경로로 조회한다. 필수 ML·비교·사례·선정/기존 초안 조회가 누락되면 결과를 채택하지 않는다. 최종 JSON 스키마, 82점·critical/major 검수 기준과 OpenAI 독립 검수를 완화하지 않는다.
- 문맥: 최초 입력은 한글을 UTF-8 바이트 세 개로 과대 계산하지 않는 보수적 혼합 언어 토큰 추정을 사용하고, 이후 동일 메시지 접두부는 Ollama가 실제 보고한 입력 토큰 수를 활용한다. 새 근거·스키마에는 여유를 다시 확보한다. 근거 원문은 줄이지 않으며, 실제 모델 문맥을 넘으면 명시적 실패/설정된 폴백으로 처리한다.
- 비용/권한: 최대 도구 선택 2회×3함수, JSON 작성 1회, 형식 교정 최대 1회. 이번 구현에 새 검색 서비스·키·라이브러리·DB 권한은 추가하지 않았다. local_only의 실시간 웹검색은 미지원 상태이며 별도 검색 수단 승인/구현이 필요하다.
- 검증: 외부 호출 없는 단위·통합 대역 테스트를 추가했다. 개인 Ollama에 대한 최소 검증 요청은 연결 오류로 완료하지 못했고, 개발 PC Wi-Fi 주소와 별도로 원격 `/api/tags`도 시간 초과했다. 실제 생성 성공·GPU 사용·기획 품질 향상을 검증 완료라고 주장하지 않는다. OpenAI 유료 생성은 수행하지 않았다.

### D-063 로컬 우선 비용 제어와 근거 검수 (2026-09-02)

- 사용자 요청: Qwen/Gemma를 적극 활용하고 OpenAI는 품질 차이가 큰 공식 근거·최종 검수에 제한. 구현 대상은 TP2-3만이다.
- 새 `local_first` 모드: Qwen 적용성·사전/재검수, Gemma 작성/수정, 통과 후 OpenAI 최종 검수 1회. 생성 전 개인 모델 연결 확인, 자동 유료 대체 차단, 생성당 OpenAI 요청 최대 3회(실패 포함). 이 모드에서는 D-061 자동 토큰 부족 재호출도 비활성화한다. 기존 hybrid/openai_only/local_only는 유지한다.
- 검수 사례 재사용 후 부족하면 공식 보강. 문서 중복/다른 지역/미래 일자는 최신 지역 정책 근거로 세지 않는다. 로컬 RAG 조회는 키워드 방식이며 새 유료 임베딩 호출을 하지 않는다. 의미 검색과의 품질 동등성은 아직 검증하지 않았다.
- Python 비교 방향/전망 사실표, 숙박비율 격차 방향 점검, 원문 보존 도구와 긴 초안 전체 페이지 확인을 추가한다. 82점·major/critical 차단·동일 JSON 계약·ML 자연추세/사업 인과효과 구분은 유지한다.
- 작성 이후 검수 실패는 초안을 보존하되 승인하지 않는다. 최대 3요청은 금액·내부 웹검색 횟수의 상한이 아니며, 모든 학습 챗봇이 로컬화된 것은 아니다.
- 실제 GPU 품질/비용 비교와 PPT 재편집은 후속 단계다. 본 구현에서 유료 기획 생성은 하지 않는다. 계획·검증은 `LOCAL_FIRST_QUALITY_PLAN.md` 참조.

### D-064 기획 기간·출력 실패·ML 해석 경계 (2026-09-02)

- 사용자 요청에 따라 TP2-3 생성 전반과 추가 ML/기획 보강을 검토했다.
- Word·PPT는 `report_projection.py`에서 기획 월·목표 산식을 공유한다. 관측 공백 제외, 부분 전망 표시, 미입력 목표 생성 금지. 렌더 버전을 갱신한다.
- 스레드 직렬 렌더링과 형식별 실패 격리, 실제 입력 지문 검증, 표지 검수 상태 표시를 추가한다. production 생성 작업의 결제 오류를 샘플 성공으로 바꾸지 않는다.
- 모델 변경 없이 Test/재귀 열세·계절 기준선을 model_reliability로 전달한다. 소비/방문 집계 비율은 실제 객단가로 단정하지 않는다.
- 새 학습·인과효과 추정·유료 생성은 하지 않았다. 검증과 후속 우선순위는 `GENERATION_REVIEW_20260902.md` 참조.

### D-065 공식 PDF 참고문서의 로컬 RAG 등록 (2026-09-02)

- 상태: 사용자 제공 자료를 검수해 구현.
- 결정: 한국관광공사 「요즘, 한국관광 2호 - 지방공항 연계 지역관광 활성화 방안」과 「2026 요즘, 한국관광 데이터 세미나(2차) 자료집」의 원문 사본은 `data/rag/source_documents/`에 로컬 보관한다. 발행기관·공식 공지 URL·SHA-256·쪽수·활용 한계가 확인된 정책·운영·지표 해석 요약만 `official_reference_documents.jsonl`에 `ALL` 공통 문서로 등록한다.
- 검색: `OfficialTourismRagStore`가 ChromaDB가 비어 있거나 local_first 모드여도 이 레지스트리를 무료 키워드로 조회한다. 임베딩 API를 호출하지 않으며, Chroma 의미 검색과 동등하다고 주장하지 않는다.

### D-067 제공 공식 PDF의 페이지 청크 RAG 확장 (2026-09-03)

- 결정: 사용자 제공 한국관광공사 PDF 2종은 원본 사본·SHA-256을 유지한 채, 이미 검수한 쪽수 범위만 700~1,000자 내외의 페이지 청크로 `data/rag/official_reference_chunks.jsonl`에 생성한다. 각 청크는 원문 파일명·공식 URL·발행일·쪽수·문서유형·공통 지역코드(`ALL`)를 보존한다.
- 비용·안전: `local_first`는 청크 JSONL을 무료 키워드로 검색한다. Chroma 의미 검색은 필요할 때 한 번만 색인하고, 생성 요청마다 임베딩 API를 호출하지 않는다. 월별 수치표·향후 공개 예정·가상 화면은 청크 대상에서 제외하며, 문서 내용은 시스템 명령이 아닌 참고 근거로만 취급한다.
- 안전: 자료의 월별 수치표는 MySQL/검증된 데이터랩 원자료를 대체하지 않는다. 세미나의 향후 공개 예정 기능·가상 화면, 지방공항 사례의 관찰 결과는 타 시군구의 성과 보장·인과효과·예산 근거로 사용하지 않는다.

### D-066 지역 맞춤 사례 다양성·로컬 Agent 응답 안정화 (2026-09-02)

- 상태: 사용자 요청에 따라 구현 및 외부 호출 없는 회귀 테스트 완료. 실제 유료 기획 생성 품질은 별도 평가 대상이다.
- 사례: `local_first`에서도 전국 공통 사례만 있고 선택 지역 또는 검증된 peer와 맞닿는 카드가 없으면 OpenAI 공식 Web Search의 사례 조사 1회를 사용한다. 검색 관점은 방문→소비, 기존 자원 활용, 측정 설계에 더해 실제 데이터에 있는 체류·검색·교통·계절 신호로 정하며, 숙박·야간을 모든 지역의 출발 질의로 고정하지 않는다.
- Qwen: 후보 비교 전 `get_case_comparison_matrix`로 운영 원리·제약을 비교하고 서로 다른 유형의 공식 사례 두 건을 읽어야 한다. `candidate_type`이 다른 2개 이상 후보를 요구하며, 이름만 다른 숙박/야간/쿠폰 후보는 결정적 품질 점검에서 major로 처리한다.
- Gemma·Ollama: 최종 구조화 JSON은 `think=false`, 낮은 temperature, 15분 `keep_alive`로 호출해 숨은 사고 출력·반복 모델 로드로 인한 지연과 미완료를 줄인다. 기본 timeout은 미설정 시 420초다. 이는 모델 가중치 fine-tuning이 아니라 프롬프트·도구·출력 계약 최적화이며, 지역 원자료로 모델을 재학습한 사실은 아니다.
- 비용·권한: OpenAI 호출 상한(공식 지역 조사 1회, 지역 맞춤 사례 조사 또는 보강 1회, 로컬 통과본 독립 최종 검수 1회)은 그대로 최대 3회다. 로컬 모델에는 웹·SQL·파일·임의 코드 권한을 주지 않으며, 별도 승인된 검색 서비스 없이는 로컬 실시간 웹검색을 주장하지 않는다.

### D-068 서울·강원 공유 원본의 불변 snapshot 보관소 (2026-09-03)

- 상태: 사용자 승인으로 구현 진행.
- 결정: 팀 공유폴더의 서울 25개 자치구 중첩 ZIP과 강원 18개 시군구 category ZIP은 `data/raw/`를 변경하지 않고 `data/source_snapshots/{시도}/{시군구}/`에 hash 검증 복제본으로 보관한다. 부모 서울 묶음 ZIP과 내부 ZIP의 SHA-256 관계는 materialization manifest에 기록한다.
- 이유: 학원 네트워크·공유폴더 접속 상태와 무관하게 같은 공식 입력으로 대시보드·ML·기획안을 재현하면서, 수동 원본 보관소인 `data/raw/`의 읽기 전용 원칙을 지키기 위함이다.
- 안전: 같은 경로의 hash가 다르면 덮어쓰지 않고 중단한다. 지역코드 내부 후보와 행정안전부 시군구 코드표가 일치하고 7개 target·24개월 이상 연속성 점검을 통과한 지역만 ML 카탈로그에 활성화한다. 정확한 다운로드 화면 URL은 미확인 상태로 남겨 출처 검증 완료로 표시하지 않는다.

### D-069 지역 카탈로그 기반 선택·ML 활성화 (2026-09-03)

- 상태: 서울·강원 snapshot을 실제 사전점검하고, 준비 완료 지역의 공통 ML 모델을 생성했다.
- 결정: React는 더 이상 지역 배열을 별도 관리하지 않고 `GET /ai/v1/regions/catalog`에서 활성 카탈로그와 저장된 Joblib·metadata 존재를 함께 확인한 목록만 읽는다. 신규 지역은 원본 검증→카탈로그 등록→시간순 학습만 마치면 선택 화면·대시보드·기획안 입력에 자동으로 나타난다.
- 현황: 서울 25개 자치구·강원 17개 시군구·기존 계양구를 포함해 활성 모델은 43개다. 강원 양양군은 2025년 방문자 ZIP이 없어 `enabled=false`로 남긴다. 임의 보간·강남 모델 재사용·예시 수치 표시로 우회하지 않는다.
- 비교층: MySQL에는 171개 지역, 5,010개 월별 사실, 166개 계획 컨텍스트, 830개 peer 비교가 적재돼 있다. 현재 비교 profile은 2025-07~2026-06, 전년 동기 변화 기준은 2024-07~2025-06이며, 지역 ML 관측 기간은 2024-01~2026-06이다. Agent는 각 근거의 기간을 함께 표시하며 서로 같은 지표·기간이라고 단정하지 않는다.
- 출처: 공유 원본의 정확한 다운로드 화면 URL은 아직 미확인이다. `needs_exact_download_url` 경고를 유지하며, 링크를 받은 뒤에만 provenance를 verified로 바꾼다.

### D-070 지역별 관광 현황 ZIP의 사실표·Agent 근거 연결 (2026-09-03)

- 상태: 사용자 요청에 따라 원본 보존·전수 검증·MySQL 적재·AI Server 연결까지 완료했다.
- 범위: 2024-01~2026-06 지역별 관광 현황 묶음의 유입·유출, 인기 관광지·음식점, 향후 30일 집중 신호를 snapshot에서 정규화했다. 공식 코드와 매핑된 254개 지역을 MySQL의 별도 지역 현황 테이블에 저장했고, `data_load_run` 3번은 862,917행 전체를 거절 0건으로 검증했다.
- 역할 분리: 유입권역·인기장소는 타깃/연계권역과 현장 조사 범위의 후보 근거, 30일 집중률은 혼잡·분산 운영 참고로만 쓴다. 이 자료는 월별 ML 학습 피처·target, 정책 미실행 반사실, 사업 인과효과에 사용하지 않는다.
- Agent/LLM: AI Server는 원본 ZIP/대용량 SQL을 요청마다 열지 않는다. 지역별 compact context만 snapshot에 붙이고, Evidence Agent와 Qwen·Gemma에 `get_regional_tourism_status` 읽기 전용 Tool로 제공한다. 출처·관측 기간·해석 한계를 prompt와 source record에 보존한다.
- 활성화 경계: 이 ZIP만으로는 7개 월별 핵심 target의 시간순 검증이 충족되지 않으므로 254개 지역을 자동 ML 활성화하지 않는다. 현재 43개 활성 ML 지역은 새 보조 근거를 즉시 사용할 수 있다. 행정구역 명칭이 바뀐 인천 4개 지역은 공식 코드·원자료 확인 전 제외한다.

### D-071 학생용 절약 Multi-LLM 모드 (2026-09-03)

- 상태: 사용자 요청에 따라 구현·기본 적용.
- 결정: `student_budget`을 기본 LLM 모드로 둔다. Qwen은 타 지역 사례의 적용성 비교와 1차/재검수, Gemma는 기획안 초안·수정을 맡는다. 지역 사실은 MySQL·저장 ML·공식 Open API, 문서 근거는 검수 JSONL/PDF RAG만 사용한다. OpenAI는 로컬 검수 통과본의 독립 최종 검수 1회에만 허용한다.
- 비용 경계: 생성 중 OpenAI Web Search, 사례 보강, 로컬 실패 유료 폴백, OpenAI 토큰 부족 자동 재시도는 모두 자동 실행하지 않는다. `student_budget`의 OpenAI 요청 상한은 실패 포함 1회다. 최신 공식 웹 조사나 지역 맞춤 사례 보강이 필요하면 사용자가 `local_first`로 전환하며, 이때만 기존 최대 3회 정책을 사용한다.
- 품질 경계: 저장된 공식 사례·정책 근거가 부족하면 이를 `research_gaps`와 trace에 남긴다. Qwen이 근거 부족으로 `needs_evidence`를 반환하거나 코드 품질 게이트의 major/critical이 남은 결과는 최종 승인·문서 출력 대상이 아니다. 비용 절감을 이유로 웹 조사를 했다고 표시하거나, 사례·수치·성과를 만들어 내지 않는다.
- 결과: 실제 유료 사용량·품질 향상은 개별 실행 trace와 담당자 검토로만 평가한다. 이 설정 자체가 무료·고품질·무오류를 보장한다는 뜻은 아니다.

### D-072 로컬 LLM의 제한형 무료 공식 웹 검색 도구 (2026-09-03)

- 배경: Qwen·Gemma가 OpenAI Web Search처럼 최신 공식 자료를 전혀 찾지 못하면, 저장된 사례가 부족한 지역에서 기획 후보 비교가 약해진다. 반대로 모델에 브라우저·파일·SQL·임의 HTTP 권한을 직접 주면 프롬프트 인젝션, 비공식 출처 혼입, 비용·할당량 통제가 불가능해진다.
- 결정: `student_budget`에서만 Qwen의 `local_web_query_planner`가 최대 3개 검색 질문을 구조화 JSON으로 설계하고, `OfficialWebSearchClient`가 보고서당 최대 2개 질문을 Tavily로 실행한다. Brave는 팀이 별도 키·계정 조건을 확인해 `LOCAL_WEB_SEARCH_PROVIDERS=tavily,brave`로 지정한 경우에만 앞선 결과가 부족할 때 보조 호출한다. Naver 검색 API는 결과 독립 노출 약관 때문에 LLM 내부 입력 경로에서 제외한다. 서버는 HTTPS 및 `TOURISM_ALLOWED_RESEARCH_DOMAINS`를 다시 검증하고, 제목·URL·짧은 API 요약만 반환한다.
- 안전·품질: 로컬 모델에는 인터넷·브라우저·파일·쉘·DB 직접 권한을 주지 않는다. 검색 후보는 `official_web_search_candidate`와 `source_verification=search_snippet_only`로 표시하며, Planner의 확정 인용·사례카드·자동 RAG 저장에서 제외한다. 원문을 사람이 검수한 뒤에만 기존 JSONL/PDF RAG 등록 절차로 승격한다. 키·응답 원문은 관리자 상태·trace에 남기지 않는다.
- 비용: 실제 키가 있는 공급자만 호출하며, 키가 없거나 무료 할당량·네트워크 오류가 발생해도 OpenAI로 자동 대체하지 않는다. `student_budget`의 OpenAI 최종 검수 1회 제한은 유지한다. Brave의 무료 조건은 계정별 정책으로 확인해야 하며 유료 업그레이드는 자동으로 하지 않는다.

### D-073 한국어 근거 문맥 추정 보정 (2026-09-03)

- 배경: `OLLAMA_CONTEXT_BUDGET_EXCEEDED`가 Qwen 적용성 검토에서 발생했다. UTF-8 바이트 수를 그대로 토큰 수로 쓰면 한글 한 글자가 세 토큰으로 계산되어, 실제 40,960 토큰 문맥 안에 들어가는 공식 근거도 실행 전에 차단됐다.
- 결정: `OllamaProvider`는 비ASCII 문자 1.5토큰·ASCII 3.2문자당 1토큰과 채팅·스키마 여유를 쓰는 보수적 혼합 언어 추정으로 변경하고, 첫 Ollama 호출 뒤에는 실제 `prompt_eval_count`를 접두부 기준으로 재사용한다. Transferability의 로컬 출력 상한은 7,000에서 5,600으로만 조정해 사례 원문 입력 여유를 확보한다.
- 품질 경계: 근거 원문, 필수 사례 수, citation 검증, 최종 JSON 계약은 줄이지 않는다. 실제 추정도 문맥을 넘으면 여전히 명시적으로 중단한다. 한국어 실제 생성 성공률·품질 향상은 별도 실행 기록으로 검증한다.

### D-074 지역 관광 현황 도구의 명시적 결정용 요약 (2026-09-03)

- 상태: 지역 적용 가능성 검토에서 `get_regional_tourism_status`가 결과 크기 제한을 넘어 필수 조회 미완료로 기록되는 오류를 수정했다.
- 결정: 도구는 원본 snapshot을 변경하지 않고 상위 유입·유출/업종·인기장소 3건의 이름·분류·비율/값·단위·순위·source_id와 집중 운영 신호만 반환한다. 반복되는 지역 코드·기간·장소 ID 같은 식별 열은 도구 요약에서 제외하고, 필요하면 요청 내 `full_detail_path`를 `read_task_section`으로 읽는다.
- 이유: 254개 지역 중 긴 인기장소·유입/유출 원본을 가진 지역도 동일한 필수 근거 검증을 통과하게 하되, 원문을 조용히 자르거나 ML/정책 효과로 과장하지 않기 위해서다.
- 검증: 실제 context 254건의 도구 반환을 전수 점검해 `EVIDENCE_RESULT_TOO_LARGE` 0건, 최대 7,024 byte를 확인했다. 외부 LLM 호출 없이 도구/로컬 Agent 단위 테스트도 통과했다.

### D-075 승인된 11장 기획서 PowerPoint 템플릿 적용 (2026-09-03)

- 상태: 사용자 승인으로 구현.
- 결정: 사용자가 최종 확인한 11장 편집형 기획서 양식을 `tourism_strategy_11_slide_template.pptx`로 등록하고, 공개 PowerPoint 생성 진입점은 `proposal_presentation_v4.py`를 사용한다. 기존 12장 템플릿 파일과 v3 생성기는 삭제하지 않고 과거 문서 재현용으로 보존한다.
- 구성: 표지 → 목차 → 프로젝트 개요 → ML 자연추세 → 유사 지역·사례 적용 논리 → 4단계 실행 → 시범 KPI → 견적표·성과 측정 → 확대 판단 → 사실·전망·검증 구분 → 월별 성과관리 순서다.
- 데이터 경계: 실제 생성에서는 보고서 JSON의 관측값·저장 ML·사용자 목표·5단계 실행안만 채운다. 예산 총액과 세부 금액은 원자료·공식 단가·비교견적 또는 사용자가 제공한 근거가 없으면 만들지 않으며, 표에는 `확정 전`으로 표시한다. 오프라인 디자인 샘플만 명시적으로 샘플 견적을 표시할 수 있다.
- 결과: 사진·텍스트·차트·견적표는 모두 PowerPoint에서 수정 가능하다. 각 슬라이드에는 사용 근거와 방법을 발표자 노트로 기록하고, 템플릿·생성기 버전이 바뀌면 기존 문서 캐시가 무효화되어 다음 다운로드에서 새 양식으로 생성된다.

### D-076 최종 9장 PowerPoint 출력과 지역 대표 사진 우선순위 (2026-09-04)

- 상태: 사용자 승인 및 구현. 공개 출력 장수에 대해서는 D-075를 대체하며, D-075의 편집형 디자인 원칙은 유지한다.
- 결정: 승인 레이아웃 원본은 보존하고 앞 9장만 최종 기획안으로 생성한다. 공개 구성은 `표지 → 목차 → 프로젝트 개요 → ML 전망 → 유사 지역·사례 비교 → 4단계 실행 → KPI → 예산 → 기대 효과·확대 판단`이다.
- 가독성: 목차 항목과 작은 본문 글꼴을 소폭 확대하고, 마지막 장 제목은 한 줄로 고정하며 주요 콘텐츠를 화면 중앙 쪽으로 이동한다.
- 이미지: 3장 대표 사진은 선택 지역의 관광 Open API 결과만 사용한다. 관광지·문화시설·축제·레포츠를 우선하고, 지역 이미지가 없으면 타 지역 이미지를 대신 쓰지 않는다.
- 캐시: 렌더 버전은 `pptx-9-approved-template-v1`로 올려 기존 11장 출력과 구분한다.

### D-077 사용자 최신 첨부본 기준 최종 9장 템플릿 재지정 (2026-09-04)

- 상태: 사용자 재지정에 따라 구현. D-076의 9장 구성은 유지하되 레이아웃 원본 선택을 이 결정으로 대체한다.
- 원본: 사용자가 직접 첨부한 `35강남구_관광전략기획안_최종_샘플.pptx`를 바탕으로 글꼴, 5장 비교 정보 배치, 8장 견적표, 9장 의사결정 구성을 다듬어 `tourism_strategy_9_slide_template.pptx`로 등록한다. 과거 11장 원본에서 앞 9장을 다시 추출하지 않는다.
- 매핑: 실제 기획 제목·해결방식, 비교 서술, 선정한 공식 사례와 적용 조건을 사용한다. 사례 출처 유형 `benchmark_case`를 포함하며, 수치가 없는 타 지역 비교 막대를 남기지 않는다. 5장의 전년동월 지수는 같은 지역의 관측 방문 증감률이 제공된 경우에만 `100 → 100+증감률`로 표시한다.
- 가독성: 폰트·좌표는 완성된 템플릿에서 관리하며 생성 시 일괄 확대·재이동을 하지 않는다. 9장의 확대·보완·중단 기준은 정적으로 보존하고 해당 기획의 기대효과와 측정지표를 짧게 채운다. 견적 비중은 1% 이상 정수, 1% 미만 소수 둘째 자리까지 표시한다.
- 데이터·캐시: 실제 생성에서 더미 금액·가짜 비교·보장 효과를 만들지 않는 원칙은 유지한다. 렌더 버전은 `pptx-9-user35-template-v1`이며 새 템플릿으로 기존 PPT 캐시를 갱신한다.

### D-078 핵심 실행·지속 운영·출처를 포함한 최종 12장 PowerPoint (2026-09-04)

- 상태: 사용자가 최신 9장 디자인을 승인한 뒤, 같은 시각 체계 안에서 설명력을 보강하도록 요청해 구현.
- 템플릿: `tourism_strategy_12_slide_template.pptx`를 공개 출력에 사용한다. 이전 9장 템플릿과 생성 코드는 삭제하지 않고, 구형 v3의 12장 원본도 `tourism_strategy_12_slide_template_legacy_v1.pptx`로 분리해 과거 결과 재현용으로 보존한다.
- 구성: `표지 → 목차 → 프로젝트 개요 → 핵심 기획 실행 모델 → ML 전망 → 유사 지역·사례 비교 → 4단계 실행 → KPI → 견적 → 지속 운영 방안 → 데이터·ML·공식 근거·AI 이력 → 감사합니다` 순서다.
- 실행 설명: 4장은 내부 후보 ID를 노출하지 않고 선정 후보의 작동 방식·대상·시범 범위·필요 자원과 실제 산출물을 보여 준다. 10장은 단순 확대 판단 대신 시범 후 정례 운영·월별 개선·단계적 확장을 설명한다.
- 근거 경계: 9장은 성과 문구를 섞지 않고 수량×단가·공식 단가·비교견적 근거만 표시한다. 11장은 `evidence_sources`, 저장 ML metadata, `agent_trace`에 실제로 있는 정보만 사용하며 관측값·자연추세·공식 근거·AI 실행을 구분한다. 근거가 없는 예산 총액·정책 인과효과·모델 사용 여부는 만들지 않는다.
- 캐시: 렌더 버전은 `pptx-12-user35-template-v3`로 올려 기존 9장·이전 12장 캐시와 구분한다.

### D-079 실행 설명과 실행 순서를 시각적으로 분리한 로드맵 (2026-09-04)

- 상태: 사용자 승인으로 구현.
- 결정: 4장은 기획의 네 가지 구성요소를 병렬로 설명하는 구조로 유지하고, 7장은 왼쪽 최종 산출물 레일과 오른쪽의 우상향 4단계 실행 여정으로 변경한다. 두 장이 같은 4열 카드처럼 보이지 않도록 정보 구조와 실루엣을 분리한다.
- 편집성: 7장의 `step-0~3-label/title/body`, `value-panel`, `value-title`, `value-body` 도형 이름을 유지해 실제 보고서의 일정·작업·산출물이 계속 네이티브 PowerPoint 개체로 치환되게 한다.
- 검증: 12장 렌더 전수 시각 검수, 오버플로 검사, 템플릿 충실도 검사를 통과한 파일만 공개 템플릿으로 사용한다.
- 캐시: 새 로드맵이 기존 저장 PPT에도 반영되도록 렌더 버전을 `pptx-12-user35-template-v4`로 올린다.

### D-080 비교 슬라이드 정렬 및 실행 로드맵 겹침 보정 (2026-09-04)

- 상태: 사용자 최종 확인 요청에 따라 구현.
- 6장: 2×2 비교 프레임을 동일한 686×282px 셀로 맞추고 좌우 제목·값·보조 설명 기준선을 통일한다. 방문 지수 차트 높이를 줄여 증감 문구와 본문 사이의 충돌을 제거하며, 하단 사례·검증 흐름의 내용과 위치는 유지한다.
- 7장: `STEP 03`의 제목과 본문만 왼쪽으로 이동해 대각선 실행 경로가 `안내 페이지·운영 매뉴얼` 문구를 통과하지 않게 한다. 단계 문구·기간·산출물과 나머지 로드맵 도형은 변경하지 않는다.
- 편집성·검증: 모든 요소는 네이티브 PowerPoint 개체로 유지하며 12장 전수 렌더 검수, 오버플로 검사, 템플릿 충실도 검사를 통과한 파일을 공개 템플릿으로 사용한다.
- 캐시: 기존 저장 PPT에도 정렬 보정본이 반영되도록 렌더 버전을 `pptx-12-user35-template-v5`로 올린다.

### D-081 전국 빅데이터 보조 근거 계층 (2026-09-04)

- 상태: 사용자 제공 공유폴더 `빅데이타`의 20개 한국관광 데이터랩 ZIP을 hash-검증 snapshot으로 보관하고, MySQL·AI snapshot 연결까지 완료했다.
- 범위: 228개 공식 코드 매핑 시군구의 2023~2025 연간 및 2026-01~06 반기 집계(내·외국인 방문, 관광소비 비중, 내비게이션 검색) 4,332행과 전국 월별 추이 2,511행을 각각 별도 테이블에 적재했다. 세종특별자치시는 기존 공식 시군구 코드 참조표에 행이 없어 19행을 명시적으로 보류했다.
- 역할 분리: 시군구 표는 기간합계·비중의 보조 비교 근거이며, 전국 월별 표는 거시 관광 흐름의 참고값이다. 두 표는 지역 월별 사실표, ML target/feature, 정책 미실행 반사실, 사업 인과효과, RAG numeric chunk로 사용하지 않는다.
- Agent/LLM: AI Server가 MySQL에서 선택 지역의 최대 12개 기간 지표와 최대 10개 전국 최신 월별 맥락만 읽어 `get_nationwide_bigdata_context` 도구로 Qwen·Gemma에 준다. 모델에는 SQL·파일·웹 직접 권한이 없고, 연간과 반기·전국과 지역을 섞어 표현하지 않도록 프롬프트 규칙을 추가했다.
- 출처 경계: 파일은 데이터랩 다운로드 형식이나 정확한 개별 다운로드 조건 URL은 팀 원본에 남아 있지 않다. 모든 source record는 `needs_exact_download_url`로 남기며, 그 전에는 보조 신호로만 사용한다.

### D-082 새 기획안의 월별 데이터 최신성 차단 기준 (2026-09-04)

- 결정: 새 전략기획 생성 직전에 지역 snapshot의 마지막 확정 관측월과 현재 기준 마지막 완료월을 비교한다. 공백이 **3개월 이상**이면 `DATA_REFRESH_REQUIRED`(HTTP 409)로 생성 요청과 백그라운드 작업 등록을 막고, 필요 원자료 기간과 적재·검증·재학습·계절 기준모델 비교 순서를 반환한다. 진행 중인 당월은 공백으로 세지 않는다.
- 이유: 과거 관측값을 현재 시점의 ML 전망·사업기간 근거로 보이게 하거나, 자료만 새로 적재한 뒤 이전 Joblib을 계속 쓰는 일을 막기 위함이다. 온라인 요청에서 즉시 재학습하지 않고, 검증된 오프라인 재학습 산출물만 다시 등록한다.
- 범위: 새 OpenAI/로컬 LLM 기획안과 개발용 오프라인 샘플 생성에만 적용한다. 대시보드·저장 기획안·기존 Word/PPT·성과 측정 기록은 당시 확정 사실을 열람하는 기능이므로 계속 제공한다.

### D-083 경기도·경상남도 원본 snapshot과 지역별 ML 확장 (2026-09-04)

- 상태: 사용자 승인 공유폴더 `전국데이타_20260831`을 원본 변경 없이 hash 검증 snapshot으로 보관하고, 경기도 43개·경상남도 3개 지역을 지역별 ML 카탈로그·artifact까지 확장했다.
- 결정: 직접 archive 1,717개와 서울 중첩 archive 599개를 대조해 동일 hash는 재사용하고, 새 경기도·경상남도 자료는 `data/source_snapshots/`에만 복제한다. 새 46개 지역은 원본 내부 지역코드·행정안전부 공식 명칭·7개 target·2024-01~2026-06 30개월 연속성 검사를 모두 통과한 뒤에만 `region_data_registry.csv`와 `artifacts/ml/<region_code>/`에 등록한다.
- 계층형 지역: 고양시 덕양구·수원시 장안구처럼 폴더의 마지막 행정구명과 공식 코드표의 전체 명칭이 다른 경우, `local_hierarchy_name`과 공식 전체 명칭을 대조한다. 원본 코드가 없는 시 단위 8개는 snapshot만 보관하고 추측으로 ML 활성화하지 않는다.
- MySQL 경계: 신규 ML target의 순 방문자수·외지인 관광소비·체류 지표는 기존 전국 비교층의 `unique_visitors`·외지인 소비·체류 지표와 같은 정의임을 표본 대조했다. 기존 MySQL 5,010 월간 사실행을 중복 삽입·덮어쓰기하지 않고, 새 snapshot과 ML artifact가 해당 사실층을 보강한다. 연인원 방문자와 순 방문자수는 교체하지 않는다.
- 동적 보조자료: 새 강원 `향후 30일 집중률` 17개는 기존 snapshot과 같은 경로·다른 hash였으므로 기존 파일을 덮어쓰지 않고 `data/source_snapshots/versioned_conflicts/20260903/`에 별도 보관한다. 이는 운영 참고 신호일 뿐 ML target/feature·사업 인과효과에 사용하지 않는다.

### D-084 강남구·인천 계양구 활성 ML 입력의 snapshot 경로 통일 (2026-09-04)

- 상태: 사용자 승인 및 구현.
- 결정: 수동 `data/raw/`는 원본 보관소로 그대로 두고, 강남구(`11680`)와 인천 계양구(`28245`)를 포함한 활성 ML 카탈로그는 hash 검증 `data/source_snapshots/` 경로만 읽는다. 인천 원본 CSV 631개는 `data/source_snapshots/인천광역시/`에 동일 hash로 복제하고 manifest를 남겼다.
- 강남구 최신성: 서울 category ZIP snapshot은 2026-06까지이므로, 기존 공식 2026-07 보완 ZIP 9개를 `서울특별시/서울특별시_강남구/supplemental_202607/`에 별도 snapshot으로 보관했다. 강남 전용 로더는 base archive 뒤에 이 보완본만 명시적으로 적용해 2024-01~2026-07, 31개월·7개 target을 유지한다.
- 검증: 강남구 base 30개월의 7개 target은 기존 raw bundle과 수치상 모두 일치하고, 최신 2026-07까지 포함한 새 학습표는 기존 저장표와 수치가 일치한다. 인천 계양구 raw·snapshot 학습표도 30개월·7개 target 전체가 동일하다. 두 지역의 `check_regions`, Joblib 재학습, 대시보드 API 200을 확인했다.
- 정리: 사용자의 명시적 승인 뒤 `data/raw/서울특별시`와 `data/raw/인천광역시`의 기존 복제본은 Windows 휴지통으로 이동했다. 이동 직후 두 snapshot 경로와 강남구·계양구 대시보드 API 200을 다시 확인했다. 다른 `data/raw` 항목은 건드리지 않았으며, snapshot 충돌은 계속 덮어쓰지 않고 별도 version snapshot으로만 보관한다.

### D-085 직접 CSV 계양구의 MySQL 전국 비교 맥락 보강 (2026-09-04)

- 상태: 데이터 정의·hash·ML 입력 전수 대조 후 구현.
- 문제: 계양구(`28245`)는 활성 ML 카탈로그·Joblib에는 있었지만 기존 전국 월별 MySQL bundle 생성 이후 등록되어, 전국 비교 context와 유사지역 5개가 없어 기획 근거가 축소될 수 있었다.
- 결정: `build_direct_csv_staging.py`가 hash 검증 snapshot과 materialization manifest를 함께 확인한 뒤, 공식 CSV의 9개 공통 지표를 기존 nationwide staging schema로 변환한다. ML 학습표의 `visitors`(순 방문자)를 전국 비교의 `visitors`(연인원)로 복사하지 않으며, 두 지표를 별도 컬럼으로 유지한다.
- 적재: delta bundle은 새 계양구 월별 사실 30건·원본 source 18개·metric source 270건만 transaction upsert한다. 기존 사실 5,010건은 삭제·덮어쓰지 않고, 전체 12개월 profile을 다시 계산한 167개 context와 835개 peer 관계만 갱신한다.
- 검증·한계: 순 방문자·외지인 소비·숙박 비율·평균 숙박일·체류시간은 ML 학습표와 30개월 전수 일치했다. 원본에는 개별 다운로드 조건 URL이 없어 `needs_exact_download_url` 상태를 그대로 보존하며, MySQL의 소비/방문 비율은 표본 일치 검증 전 객단가·정책 효과로 해석하지 않는다.

### D-086 로컬 최종안의 공식 사례 인용 복구 (2026-09-05)

- 상태: 반복 발생한 `LOCAL_EVIDENCE_NOT_READ` 원인을 확인하고 구현·테스트 완료.
- 문제: Qwen은 요청 개요의 모든 공식 사례 ID를 볼 수 있지만 서버는 문맥 보호를 위해 서로 다른 운영 방식의 대표 사례만 먼저 읽었다. 모델이 목록에서 본 다른 등록 사례를 최종안에 인용하면 네트워크·토큰과 무관하게 안전 검증이 즉시 차단했다.
- 결정: 최종 작성 직전에 원문 조회 완료 사례 ID만 허용 목록으로 전달한다. 그래도 다른 등록 사례를 처음 인용하면 서버가 요청 범위 안의 `read_collected_source`를 실행하고, 같은 Qwen·Gemma에 Schema 유지 재작성을 1회만 요청한다. 재작성에서 또 다른 미조회 사례를 인용하거나 원문이 안전한 문맥 크기를 넘으면 `LOCAL_EVIDENCE_NOT_READ`로 차단한다.
- 비용·권한: 사례 복구에는 OpenAI와 실시간 웹검색을 호출하지 않는다. 로컬 모델에 파일·MySQL·임의 URL 접근 권한도 주지 않으며, 기존 필수 근거·문맥·Schema·출처 검증을 완화하지 않는다.
- 검증: 로컬 Agent 도구·정책 unittest 60개 통과. 자동 원문 조회 후 성공, 두 번째 새 사례 차단, 기존 도구/Schema/Router 정책을 함께 확인했다.

### D-087 로컬 도구 선택 미완료와 최종 본문 출력 분리 (2026-09-05)

- 상태: 실제 Planner 실패 trace를 확인하고 구현·테스트 완료.
- 원인: Gemma 본문 상한은 16,000토큰이었지만 실패는 본문 전 `tool_selection`에서 발생했다. 필수 MySQL·지역 현황·전국 비교·ML·선정안·사례 원문은 사전조회가 완료됐는데도, 모델이 함수 호출 대신 서술을 생성해 선택 단계 1,024토큰을 소진하면서 전체 작업이 폐기됐다.
- 결정: 도구 선택은 함수명·짧은 인자만을 위한 512토큰으로 분리한다. `OLLAMA_INCOMPLETE_RESPONSE`가 발생해도 `missing_required_reads()`가 비어 있을 때만 사용량과 `continued_without_optional_tools`를 기록하고 최종 Schema 작성으로 진행한다. 필수 조회가 남아 있으면 기존 오류를 그대로 반환한다.
- 품질·비용: Gemma 본문 16,000토큰, 원문·Schema·출처 검증, 미조회 사례 차단은 유지한다. OpenAI 폴백이나 유료 호출을 추가하지 않고 선택 단계의 불필요한 로컬 출력·대기만 줄인다.
- 검증: 로컬 Agent 도구·정책 unittest 62개 통과. 선택 단계 미완료 후 최종 JSON 성공과 필수 근거 미완료 시 우회 금지를 각각 검증했다.

### D-088 로컬 최종 JSON 미완료 1회 재생성 (2026-09-05)

- 상태: 출력 단계별 원인을 분리해 추가 구현·테스트 완료.
- 결정: 최종 Schema JSON이 `OLLAMA_INCOMPLETE_RESPONSE`로 끝나면 부분 응답을 폐기하고 동일한 원문·Schema·출력 상한으로 처음부터 한 번만 다시 생성한다. 중복 서술만 줄이고 필수 필드·수치·기간·산식·출처는 유지하도록 지시한다.
- 경계: 두 번째 미완료는 실패한다. 잘린 JSON 결합, 필수 필드 삭제, 출력 상한 확대, OpenAI 자동 대체를 하지 않는다. 실패한 첫 시도의 로컬 사용량도 trace 합계에 포함한다.
- 검증: 로컬 Agent 도구·정책 unittest 64개 통과. 첫 미완료 후 완전 JSON 성공, 연속 두 번 미완료 시 제한 종료, 사용량 합산을 확인했다.

### D-089 실제 저장 수치에 연결한 PPT v6와 전량 출처 (2026-09-05)

- 사용자 요청: 월별 예측 그래프, 수치형 목표 KPI, 임시 참고 견적, 모든 데이터·모델·공식 근거 표시. 승인된 시각 체계와 앞부분 순서는 유지한다.
- 발견: 원주 10월 2,633,270명과 11월 2,632,315명이 기존 정수 만명 표기에서 모두 263만명으로 보였다. 방문 차트에만 관측월을 붙였고, KPI는 최종 월 대신 첫 월을 사용했다. 출처는 상위 제목만 노출했으며, 개요는 증감률을 방문자 수로 잘못 제목화할 수 있었다.
- 변경: `proposal_slide_content.py`가 승인 템플릿의 편집형 차트·표에 사업 월의 저장 수치를 그대로 결합한다. 관측값·예측값·운영 목표를 분리하고, 원자료가 없는 경우 임의 0이나 더미를 만들지 않는다. `proposal_evidence.py`는 MySQL source ID의 파일명을 읽기 전용으로 복원하고 전체 출처를 보존한다.
- 사용자가 임시 견적을 요청했으므로 D-075/078의 '실제 문서에는 가정 견적 미표시' 범위를 변경한다. 수량·가정 단가·예비비로 산정하며, 공식 강진 환급 조건은 제도 방식의 참고이지 인건비·시스템 구축 단가의 증거가 아니다. 다른 사업은 일반 소규모 운영 가정임을 명시한다. 신규 앱·키오스크 구축은 별도 견적 대상이다.
- 목표율을 입력한 경우 최종 월 자연추세와 목표를 비교한다. 미입력 보고서는 지역 전체 증가율을 추정하지 않고 참고 견적에 기반한 지원량·완료량 등 별도 운영 제안만 표시한다. 참여자 증빙 소비를 증분 매출로, 지원 건수를 신규 관광객으로 바꾸지 않는다.
- 현재 원주 방문·소비 모델은 저장된 시험 평가에서 계절 기준선보다 오차가 커 탐색 전망 경고를 표시한다. 이 작업은 모델 선택·재학습을 임의로 변경하지 않는다.
- 출처는 11장부터 분량에 따라 같은 템플릿의 부록으로 이어지고 감사 페이지가 마지막이다. 후보·보조 조회까지 포함한 출처와 실제 기획에 채택한 근거를 동일시하지 않는다. 전체 파일명·URL·원수치·검증값은 노트에 보존한다.
- 저장 보고서·검수 결과·기존 템플릿은 보존한다. 새 렌더 버전 `pptx-user35-v6-exact-ml-kpi-full-sources-r2`가 다운로드 캐시만 갱신하며 유료 생성 호출은 추가하지 않는다.
- 검증: 문서/기간/견적/출처/캐시 31개, 지역 현황·ML 기획 10개, 전국 빅데이터 1개 테스트 통과. 실제 원주 보고서의 월별 예측 3행·모델 7개·출처 56건을 대조했다. 전체 출처 부록을 포함한 18장 렌더와 화면 밖 잘림 검사, 저장 보고서 PPT 다운로드 API 200을 확인했다. 테스트 환경에는 개발 시작 스크립트와 같은 MySQL 환경변수를 적용했으며 유료 LLM 생성은 하지 않았다.

### D-090 전국 사례 범위와 지역 적용 경계 (2026-09-07)

- 사용자 요청: 같은 시도에 한정하지 않고 다른 시도의 유사 규모·운영 사례까지 활용한다.
- 구현: `case_scope.py`가 검증된 지역 코드/시도와 관광 지표 peer를 바탕으로 동일 시도·타시도·전국 사업 및 다른 운영 원리를 함께 최대 8개 카드로 선택한다. 범위·선택 수·부족 조건을 trace/근거 패키지에 남긴다.
- RAG 확장은 `case_study`, `case_study_budget`에만 적용하며, 일반 정책/시설 문서는 기존 지역 필터를 유지한다. 무료 검색은 기존 최대 2개 질문 안에서 지역·전국을 나누며 호출 상한을 늘리지 않는다.
- 경계: '전국 모든 사례를 학습했다'는 뜻이 아니다. 현재 peer는 관광 지표 기준이지 주민등록 인구 기준이 아니다. 단순 성과율을 선택 지역에 그대로 이식하지 않고 운영 자격·비용·인프라 차이를 명시한다.
- 안내: `docs/NATIONWIDE_CASE_SELECTION.md`.

### D-091 후보 보완·실행 근거 계약·구체적인 자료 요청 (2026-09-07)

- 사용자 요청: 원주 기획안의 후보·성과 기준·예산·일정 부족 원인을 수정하고 필요한 자료를 구체적으로 요청한다. 경고를 숨기거나 이전 보고서를 임의 승인하지 않는다.
- 원인: Gemma 본문 수정으로는 Qwen 후보 JSON을 보완할 수 없었다. 후보 수 부족과 미확인 상태가 혼재했고, 예산은 '단가' 단어만으로 통과했으며 상세 근거 요구와 180자 제한이 충돌했다.
- 구현: `execution-evidence-v1` 계약을 클라우드/로컬 작성·검수 지시에 공유한다. 로컬 우선 모드에서 보유 사례가 충분하고 후보 작성 누락이 있으면 Qwen 후보 보완을 최대 1회 실행한다. 이후 기존 Gemma 본문 수정·독립 검수를 유지한다. 실패하면 원래 판단/초안을 보존하고 근거 부족 상태는 유지한다.
- 검증: 지역 사실 ID 연결, 비용 산식, KPI 구성 및 잘못된 증가율/근거 없는 문턱, 담당 역할 누락을 검사한다. 이는 의미·인과·법적 검증의 대체가 아니다. 전체 검사와 `completion_checklist`를 저장하고 React에 준비할 자료를 표시한다.
- 출처: 강진 카드의 전체 상품권 증가율은 집계 범위가 불명확하므로 생성용 정량 인용을 보류했다. 원래 문구는 `historical_unverified_claim`으로 보존하고 최신 교정 카드와 중복되는 RAG 요약을 제외한다. 원자료·과거 보고서·모델 가중치는 변경하지 않는다.
- 입력 효율: 같은 열을 반복하는 원문 JSON 행을 `context_tables.py`의 열/행 표로 무손실 변환한다. 반복 관측·선택 후보 원문은 한 번만 전달한다. 원수치·출처·단위·null은 복원 비교로 검증하며, 원본 도구 저장소와 필수 조회·도구별 크기 제한은 유지한다. 실모델의 해석 능력까지 자동 증명되는 것은 아니다.
- 비용: 학생 절약 모드의 유료 요청 상한, 로컬 출력/문맥 상한, 필수 원문 조회·82점/major 차단 기준을 완화하지 않는다. 추가 보완은 로컬 연산과 지연을 늘릴 수 있다. 원주 실제 근거 4개 단계의 문맥 사전검사와 관련 회귀 156개를 통과했다. 전체 새 기획안의 실생성 품질 검증은 별도로 필요하다.
- 자료 목록과 당시 실데이터 점검: `docs/WONJU_PROPOSAL_QUALITY_AUDIT_20260907.md`.

### D-092 PDF 청크 보존과 지표 정의 누락 공개 (2026-09-07)

- 발견: 페이지별 PDF 청크는 RAG 수집 단계에서 보존했지만, 기획 Orchestrator와 로컬 근거 도구가 `source_id`만으로 합쳐 같은 문서의 다른 페이지를 덮어쓸 수 있었다. 따라서 검색 결과 5개가 있어도 작성 Agent에 1개만 전달될 위험이 있었다.
- 변경: 병합 키를 `(source_id, chunk_id)`로 통일하고, 로컬 Agent는 같은 문서의 페이지 청크를 `offset` 순서로 읽어야 해당 문서를 읽은 것으로 표시한다. Chroma 의미 검색도 chunk ID·쪽수·원문 전체를 보존한다. PDF의 원문 `source_id`는 인용 ID로 그대로 유지한다.
- 데이터 정의: `get_region_metrics`는 공식 관측값과 함께 업종별 관광소비 구성을 전달한다. 업종 비중은 원인·사업 효과·개별 방문객 객단가의 증거가 아니라는 해석 한계를 함께 고정한다.
- PDF 확인: 지방공항 자료 34~44쪽은 추출 가능했다. 데이터 세미나 75~99쪽은 대부분 이미지/특수 폰트라 텍스트 추출에 실패했다. 화면으로 직접 확인한 76쪽만 검수 요약으로 RAG에 넣고, 나머지 누락 쪽수는 재생성 CLI가 경고한다. 따라서 이 PDF 전체가 RAG에 색인됐다고 주장하지 않는다.
- 검증: PDF 3페이지가 합치기·재작성·로컬 페이지 조회를 모두 통과하는 회귀 테스트, RAG 청크 메타데이터 보존, 미래 월 변조가 과거 ML feature 행을 바꾸지 않는 검사, 기존 계획·ML·PPT 검증을 포함한 선택 회귀 171개를 통과했다. 유료 OpenAI·임베딩·재학습은 호출하지 않았다.

## 아직 결정하지 않은 것 (후속 확인)

- 시범지역: 데이터 확인 후 3~5개 후보와 1개 심층 시범지역 선정
- 사용할 데이터랩 파일·기간·지역코드 기준
- 최종 타깃 변수와 Feature 목록
- 관광 기회 점수 수식과 가중치
- 정확한 OpenAI 모델명·비용 한도
- 로그인 필요 여부와 외부 날씨·공휴일·축제 데이터 추가 여부

미정 사항은 데이터를 근거로 제안하고 사용자 승인 후 확정한다.


### D-138 생성 진행 단계 표시 (2026-09-10)

생성 대기 화면 제목은 ‘기획서 초안을 생성중입니다’. 작업 조회 응답에 `progress_step`을 추가한다: 0 데이터 분석(연결 확인 포함), 1 공식사례 확인(지역 근거 병행), 2 기획안 생성(후보 비교·초안), 3 품질검토(보완·재검수 포함), 4 검토 절차 종료 후 본문·문서 준비. null은 단계 미확인이다. 실제 실행 지점에서 요청별 ContextVar 콜백으로 메모리 작업 상태를 갱신하며 기존 3초 상태 조회로 표시한다. 경과 시간으로 단계를 추측하지 않는다. 진행 단계는 파란 음영 애니메이션, 완료 단계는 파란 채움, 대기 단계는 기존 외곽선으로 구분한다. 재접속 시 서버 상태를 다시 읽고 연결 오류에는 확인 지연을 표시한다. 단계 완료는 품질 승인과 다르며 모델/유료 호출 수는 변경하지 않는다.


### D-140 선택형 기획 입력과 다음 3개월 예측 (2026-09-11)

- 사용자 승인: 기획안 생성의 3개월 시범 운영/시작 월/제외할 운영 방식 입력을 제거하고 자원·현장 정보를 복수 선택형으로 변경한다. 소비·환급 버튼은 두 줄로 표시한다.
- 새 웹 입력은 `guided_v2`. 서버가 신규 동기/비동기 생성 요청을 접수할 때 한국 시간 기준 다음 달 1일~세 번째 달 말일을 확정한다. 9월→10~12월, 10월→11~1월. 브라우저도 월 변경과 요청 직전에 갱신한다.
- 저장 보고서와 복구 작업은 당시 날짜를 유지한다. Word/PPT 공통 전망 선택은 v2의 확정 날짜를 우선해 LLM 사업 일정이 예측 구간을 바꾸지 못하게 한다. 기존 모델 학습·관측값·예측 거리별 신뢰도 및 자료 최신성 제한은 유지한다.
- 자원 `information_center/merchants/lodging/events/cultural_spaces/promotion`, 현장 `families/young_adults/weekend/weekdays/event_link/experience`만 허용한다. 서버가 선택 코드에서 참고 문장을 재구성하고 임의 문구는 사용하지 않는다. 사용자 선택은 협약 확보·공식 사실의 증거가 아니다. 미선택도 가능하다.
- 참고 예산 배분과 사업 방향 필터는 v2에도 적용한다. 구형 자유 입력 초안은 별도 키로 보존하고 새로운 선택형 초안에는 이관하지 않는다. 유료 호출·학습·저장 보고서 변경은 없다.

- 검증: 관련 Python unittest 32개, 프론트 node 테스트 17개, lint·production build 통과. 요청 월/연도/윤년 경계, 신규 두 API의 날짜 고정·저장 복원, LLM 일정과 문서 예측 구간 분리, 선택 코드 검증 확인. 브라우저는 테스트 API 응답과 생성 POST 차단으로 데스크톱·모바일/예산·선택 전달을 검증했다. 실제 LLM 생성은 실행하지 않았다. 임시저장 콜백이 상태 갱신 함수로 실행되던 연결도 수정하고 버튼 동작을 확인했다.

- 추가 UI 요청: 왼쪽 기간 카드(기존 03)를 제거하고 오른쪽 요약에만 `사업기간`으로 표시한다. 활용할 자원/현장 정보는 03/04로 재번호 부여한다. 다음 달부터 3개월 자동 계산과 API 날짜 계약은 유지한다.


### D-141 철원 반복 추천 원인 수정 (2026-09-11)

- 사용자 요청에 따라 철원·원주 실제 저장 보고서와 첨부 문서를 비교했다. 공통 사례 5/8, 후보 비교 문장 반복, 지역 적합성 대신 운영 방식으로 인용을 재연결하며 선정 이유를 덮어쓰던 문제, 10~12월과 9~12월 불일치를 확인했다.
- 유효한 인용과 선정 이유를 보존하고, 누락 인용만 같은 방식 문서에서 지역 검색 맥락·문서 등급으로 보완한다. 자동 추천에는 공식 운영 근거가 있는 예약·교통·체험을 허용한다. 예산서가 비교 슬롯을 차지하지 않게 한다. 이는 새 ML 적합성 모델이 아니다.
- 별도 로컬 지침에 후보별 차이와 사업기간을 반영하고, 반복 설명은 기존 1회 보완 경로에 전달한다. 문서의 실제 비교 사례와 추가 참고 사례를 구분한다. v2 사업기간 및 단계 일정을 입력 날짜에 맞추고 보정 이력을 남긴다.
- 목표 +20%·견적 비중은 기존 계획 가정이며 인구 보정/실측 효과로 주장하지 않는다. 저장본·ML·승인 상태 유지, 유료 호출·운영 호출 횟수 확대 없음. 자세한 근거와 미완료 항목은 `CHEORWON_RECOMMENDATION_AUDIT_20260911.md`.

- 검증: 관련 unittest 158개 및 실제 철원 저장본 오프라인 재생 통과. 유효한 선정 이유만 유지하며 잘못된 인용 보완 시 원래 이유를 감사 필드에 보관한다. 로컬 LLM 연결 시간 초과로 실제 재생성 품질은 미검증.

### D-142 팀 최종 데이터의 로컬 통합과 지역 확장 (2026-09-11)

- 사용자 승인: 일시 접근 가능한 최종 공유폴더의 누락·신규 파일을 모두 프로젝트에 보관하고 데이터셋·ML·기획 입력·웹 지원 지역에 반영한다. 원본 ZIP 6,256개를 `data/source_snapshots/team_final_20260911`에 복제하고 전부 SHA-256 일치를 확인했다. 이후 실행은 공유폴더를 조회하지 않는다.
- 지역코드·월·7개 지표를 검증한 161개 지역을 별도 staging에서 시간순 학습·기준모델 비교하고 실제 원본으로 예측 재검증 후 활성화했다. 기존 모델은 보존하고, 동일 월별 값인 64개 지역은 재사용한다. 활성 지역 89→250. 모델 파일을 먼저 검증·배치한 뒤 카탈로그를 원자적으로 교체한다.
- MySQL 월별 통계는 267개 지역/7,910행, 비교 요약은 263개 지역으로 확장했다. 같은 관측값은 중복 집계하지 않는다. `--fill-missing`은 기존에 비어 있는 지표만 출처와 함께 보완하고 기존 수치 변경은 차단한다. 양양군 18개월 누락 지표를 보완했다. 저장 보고서·승인 상태·관측 원본은 변경하지 않았다.
- 카탈로그 변경을 ML 레지스트리가 다음 조회에 반영하며 요청 중 자동 학습은 없다. 시도 비교는 물리적 폴더 계층 대신 등록표의 지역명·로컬 경로를 사용한다. React는 기존 카탈로그 API로 250개 지역을 표시한다.
- 7월 개편 코드도 공식 첨부 ZIP으로 확인했다. 신·구 코드의 중복 및 소비액 수정본은 별도 감사 자료에 보관하며 6월 관측에 신 코드를 임의 대입하지 않는다. 불충분한 지표·5개월 이력 지역을 생성 가능으로 표시하지 않는다.
- 검증: Python 관련 49개, React 17개, lint·build 통과. 250개 지역 생성 입력 점검 통과, 대표 4개 새 지역 API·시도 비교 정상, 브라우저 실제 API 응답 재생 확인. Qwen·Gemma 연결 점검은 미통과이며 LLM 실생성·유료 호출·기획 품질 승인은 수행하지 않았다. 상세: `TEAM_DATA_INTEGRATION_20260911.md`.

### D-143 관리자 학습 화면 최신화 (2026-09-11)

- OpenAI·React·AI Router 탭에 현재 데이터 준비/생성/저장·재사용 흐름과 담당 파일을 공통 카드 형식으로 추가했다. 공통 ML·발전 기회 분석은 미구현 제안으로 구분한다.
- 세 탭의 학습 챗봇은 동일 컴포넌트를 사용하고 데스크톱 폭 340px, 좁은 화면은 본문 아래로 배치한다. Router 질문도 기존 OpenAI 구조 학습 API를 사용하며 운영 설정을 변경하는 도구가 아니다.
- 카탈로그 설명에 선택형 입력·다음 3개월·지역 모델 등록·Router·로컬 지침을 반영했다. React 자원 설명은 키워드 RAG와 Ollama/OpenAI 역할을 구분한다. Provider 설정과 유료 정책은 변경하지 않았다.
- 검증: 학습 API unittest 4개, 프론트 lint/build 통과. 실제 카탈로그 응답 재생과 Router 상태 fixture로 3개 탭 데스크톱/390px 모바일에서 공통 챗봇 폭·가로 넘침 없음·브라우저 오류 0 확인. 실제 유료 챗봇·설정 저장은 실행하지 않았다.

### D-144 제주시 출력 검토와 공통 산출 근거 (2026-09-11)

- 사용자 요청으로 웹 견적 글씨 크기 통일, 사진 설명 줄 수에 따른 배경 높이, 3번 슬라이드 방문·소비 목표, 견적 뒤 산출 근거 2페이지를 공통 출력에 반영했다. 모델명/사례 수/후보 수/목표 합계는 저장 보고서에서 도출한다. PPT v18/DOCX v6 캐시 버전 갱신.
- 제주시 실제 trace의 보완 문맥 초과를 확인하고 과거 판단 감사 복제본을 명시적 조회 경로로 이동했다. 현재 판단/공식 근거/감사 원문은 유지한다. 재구성한 제주시 Gemma 보완 진단은 48.384초에 완료, 품질 승인 아님. 기존 저장본·유료 정책·호출 횟수·모델 변경 없음.
- 관련64개 unittest 및 frontend lint/build, Word/PPT 렌더링 확인. 실제 내용의 검색량→소비전환 추론과 추천 우수성은 아직 검증되지 않았다. 자세한 결과는 `JEJU_OUTPUT_REVIEW_20260911.md`.

### D-145 산출 인포그래픽과 모델별 로컬 문맥 (2026-09-11)

- 사용자 요청으로 공통 PPT 산출 근거 2장을 편집 가능한 수식/흐름도로 재구성했다. 사례 증가율×계획 계수, ML 기준선→목표→참여량→견적 합계 및 2개 수치 기준선/5개 진단 입력→Qwen→Gemma 흐름을 구분한다. 세부 산식과 발표 설명은 노트에 유지하며 Word도 ML 역할 설명을 보완했다. 사진 설명은 줄 수별 높이와 세로 중앙 정렬을 함께 적용한다. PPT v19/Word v7.
- 로컬 문맥 확대 승인에 따라 모델별 환경변수를 추가했다. 설정 우선순위는 모델별 값→기존 공통값→40,960. 현재 노트북 `/api/show`에서 Qwen 최대40,960, Gemma 최대262,144를 확인하여 프로젝트 `.env`는 Qwen40,960/Gemma65,536으로 설정했다. 키나 주소는 출력하지 않는다. OpenAI 정책/모델과 출력 토큰 상한은 변경하지 않았다.
- 제주시 근거로 Gemma 보완 단계1회 진단:66.912초, 입력19,721/출력1,480토큰, 완전 JSON 반환. `/api/ps`의 로드된 context_length65,536 확인. 원문/저장 보고서/ML/승인 상태는 보존, 유료 호출0. 실제 최대65k 분량의 부하 시험이나 전체 기획 품질 승인은 아니다. 실행 중 서버에는 재시작 후 환경변수가 적용된다.
- Router/출력/캡션 회귀17개 통과. PowerPoint 렌더링으로3·11·12번 확인, Word7쪽 렌더링 확인. 외부 라이브러리 추가 없이 기존 공통 출력 도구를 사용했다.

### D-146 확정 PPT 유지와 Word 공통 출력 정합성 (2026-09-11)

- 사용자가 PPT v19 양식을 모든 지역에 적용하는 것으로 확정했다. PPT 생성 코드와 최종 파일은 이번 변경에서 수정하지 않았다.
- Word는 공통 A4 출력 모듈 `proposal_document_v2.py`로 연결했다. PPT와 같은 사업기간·월별 전망/목표 배열·공식 사례·4단계 실행 안내·목표 근거·견적을 사용한다. 과거 12개월 그래프를 사업기간 그래프로 대체하고 전체 출처를 유지한다. 저장 보고서와 ML 결과는 변경하지 않는다.
- Word 캐시만 `strategy-docx-v8-ppt-parity-a4`로 갱신했다. 기존 다운로드 API의 다음 렌더링부터 모든 지역에 적용된다.
- 관련 unittest 28개 통과. 제주시 실제 PPT 차트 수치와 공유 배열, Word 월별 표·견적 876,645,000원·전체 출처 ID 보존 확인.
- 시각 검수 미완료: Word COM 렌더링 요청이 자동 승인 검토의 계정 사용 한도로 거절됐다. 별도 render_docx.py는 LibreOffice 미설치로 실패했다. A4 디자인 파일은 생성했으나 페이지 넘침·겹침을 확인하기 전 최종 품질 승인/전달 완료로 보지 않는다.

### D-146 시각 검수 완료 보충 (2026-09-12)

- 사용자 승인에 따라 Word 페이지 수를 고정하지 않고 내용별로 분할한다. 방문·소비는 각각 그래프/월별 표/산식 한 페이지, 사례는 설명·사진·출처를 묶어 이동하며 긴 출처 목록은 자동으로 이어진다. 표 머리글 반복과 행 분할 방지를 유지했다.
- 이전 계정 한도 차단이 해소되어 Microsoft Word 숨김 COM + PDF/PNG로 실제 렌더링했다. 제주시 최종 16쪽 모두 확인했고 그래프/표 중간 잘림을 수정했다. 공통 Word v9(`strategy-docx-v9-paginated-a4`) 적용. PPT v19 유지.
- 테스트 28개 통과와 실제 제주시 수치 대조, 출처 ID 누락 0 확인. 최종 Word는 `output/jeju_revision_20260911/제주시_관광전략기획안_Word_최종.docx`. 문서 출력/레이아웃 검증이며 LLM 기획 품질 추가 승인은 아니다.

### D-147 소비 업종 설명과 글자 잘림 수정 (2026-09-12)

- 첫 대시보드 소비 업종명에 작은 ? 버튼을 추가했다. 클릭/Enter로 항목별 이용 예시를 펼치고 Escape로 닫는다. 긴 업종명과 도넛 범례는 줄바꿈하며 비중/금액은 다음 줄에 배치해 잘림을 방지한다.
- 설명은 이용 예시이며 실제 세부 업종별 금액이나 증가 원인으로 표시하지 않는다. 운송업은 육상/수상/항공/렌터카의 이동 관련 결제로 설명한다. 카드사/자료별 분류 및 결제 귀속 주의점을 포함한다.
- 현재 계산은 `ai_server/app/main.py`의 3개월 월평균 소비 ML 전망 × 최신 관측 업종 비중이다. 업종별 독립 예측이나 증가율로 오해하지 않도록 화면에 표시했다. 수치 계산/API 스키마 변경 없음.
- 분류 참고: 한국관광 데이터랩 데이터 설명 https://datalab.visitkorea.or.kr/datalab/portal/getMetaInfoList.do 및 Data & Tourism Vol.21 업종 하위분류(운송/렌터카) https://datalab.visitkorea.or.kr/common/board/Download.do?bcIdx=305184&cbIdx=1129&streFileNm=a71a26f3-32a1-4044-bfee-a21a45740d72.pdf . 소비 예시는 완전한 코드 분류표가 아니며 해당 지역 실제 구매 품목을 단정하지 않는다.
- frontend lint/build 통과. 실제 React 컴포넌트+공통 CSS를 사용한 570px 카드 검수에서 1440/1024/390 화면의 클릭·Enter·Escape, aria-expanded, 업종명/범례 잘림 없음·가로 넘침 없음 확인. 외부 API는 가로채고 그래프 영역은 검사 대역을 사용했으며 LLM/DB 생성 호출 없음. 검수 스크립트 `storage/previews/check_consumption_help.mjs`.

### D-148 지역 준비 상태 만료 원인 구분 (2026-09-12)

- 청주시 청원구(43114)의 수집 원본 폴더·저장 ML 7개 지표·직전 data_ready=true를 확인했다. 실제 API는 Qwen/Gemma 연결 정상이나 audit 24시간 만료로 목록이 비어 있었다. 데이터 누락이 아니라 점검 유효기간에 따른 표시였다.
- 24시간 안전 기준은 유지하고 만료 응답을 status=expired와 이전 checked_at으로 구분한다. 지역 선택기 2곳은 준비됨/자료 준비됨·모델 연결 확인/자료 확인 필요/자료 점검 갱신 필요/준비 상태 미확인을 구분한다. 자료가 없거나 점검되지 않은 지역을 임의로 초록색으로 바꾸지 않는다.
- 전체 등록 지역 --data-only 재점검 실행. LLM 호출·학습·SQL 쓰기 없음. 상태 unittest 3개, 프론트 라벨 3분기 검사, lint/build 통과. 실제 API expired 및 local_models_ready=true 확인.
- 재점검 완료: 등록 250/250지역 data_ready=true, 실생성 검증 VERIFIED=0(--data-only). 청원구 43114 실제 실행 API에서 data_ready=true/generation_ready=true, 로컬 모델 연결 정상 확인. 자료 점검과 기획 품질 승인은 구분한다.

### D-149 지역 자료 준비 상태 영속화 (2026-09-12)

- 사용자 승인으로 초록색의 의미를 지역의 원자료·저장 ML·SQL 비교·공식 사례 준비 상태(data_ready)로 통일했다. Qwen/Gemma의 현재 연결은 별도로 표시하며 generation_ready는 기존처럼 자료+현재 모델 연결을 모두 요구한다.
- 24시간 audit 만료를 제거했다. 점검 때 원본/모델/공식 사례 파일의 경로·크기·수정시간과 지역 카탈로그 행, SQL 지역 context/peer·지역명 행의 서명을 저장한다. 조회 때 실제 입력 변경/삭제와 SQL 읽기 가능 여부, 생성과 동일한 관측월 최신성 기준을 확인한다. 준비 안 된 지역을 임의 승격하지 않고 변경된 입력은 재점검을 요구한다.
- 전체 점검 중 입력/SQL 변경도 확인한다. 파일은 stat 기반 변경 감지이며 암호학적 원본 내용 무결성 검증과 동일하지 않다. LLM 응답 품질·생성 성공 보장을 표시하는 기능이 아니다.
- 일시 API 실패 때 마지막 성공 결과를 유지하되 '상태 조회 연결 끊김 · 마지막 확인 결과 표시'로 알린다. SQL 연결 실패는 초록색을 해제하고 복구 시 저장 점검 결과와 재대조해 복구한다. 모델 health 예외는 자료 목록을 지우지 않는다.
- 250/250 지역 재점검 통과. 실제 API에서 data_ready 250, Qwen/Gemma active, 청원구 generation_ready=true 확인(조회 약1.87초). 서버 unittest5개 및 frontend lint/build 통과. 입력 파일/SQL 변경·누락·관측월 노후화·30일 경과·LLM health 실패를 검사했다. 유료 호출·학습·SQL 쓰기 없음.
- 실제 React 지역 선택기 브라우저 검수 완료: 모델 offline에서도 초록색 유지, API 요청 실패 시 마지막 확인 안내+초록색 유지, SQL 실패 응답 시 초록색 해제, 정상 응답 복귀 시 자동 복구. `storage/previews/check_readiness_persistence.mjs`로 재현한다. 서버/SQL 장애를 실제로 유발하지 않고 응답 대역으로 검증했다.
### D-150 지역 데이터와 다양한 공식 사례의 연결 복구 (2026-09-12)

- 사용자 요청: 지역 관측·ML 결과로 유사 지역의 실제 운영·성과를 조사하고 여러 사례를 비교해 적용한다.
  완전 동일한 지역은 요구하지 않으며 일부 조건이 다른 지역의 유용한 사례도 포함한다.
- SQL peer와 ML의 역할을 분리한 지역별 조사 계획을 Case Scout·Qwen 비교표·Gemma 보완 입력에 연결했다.
  쿠폰·환급에 고정된 검색 질문을 제거하고, 후보 원문은 서로 다른 운영 원리 최대 3건을 사전조회한다.
- 공식 사례 검색의 URL 검증은 카드별로 적용하고 기각 카드의 자유 서술 요약을 제거한다.
  실패 원인 URL과 실제 검색 수행을 기록하며, 유효한 나머지 결과는 보존한다. 유료 호출 수는 늘리지 않는다.
- 자동 추천에서 2개 실제 지역·2개 운영 방식도 비교하지 못하면 같은 공통 사례로 본문을 만들지 않는다.
  이는 정확한 peer 일치 조건이 아니다. 부족하거나 일부 출처가 기각된 조사는 정상 캐시로 재사용하지 않는다.
- 지급액을 성공으로 판단한 기준을 코드와 로컬 검수에서 교정한다. 기존 저장 보고서·ML·승인 상태는 보존한다.
- 부평·제주·원주의 실제 SQL/ML 조사 입력 차이, 관련 unittest 155개, 진단 기본 실행(유료 0회)을 확인했다.
  승인된 실제 부평 조사 1회는 약 1.9초 후 OpenAI 오류로 실패했다. 다양성 부족으로 본문 전에 중단했으며
  신규 검색→Qwen→Gemma 내용 검증은 미완료다. 품질 개선 완료로 승인하지 않는다.
- 오류 응답의 HTTP 상태·알려진 code/type을 공급자 시도와 Case Scout 추적에 남기도록 보완했다.
  원문 오류 메시지·헤더·본문은 저장하지 않으며 429만으로 결제 문제라 단정하지 않는다.
  과거 실패의 정확한 원인은 기록 부재로 확정 불가하고, 추가 유료 재시도 없이 대역 테스트로 검증했다.
  [상세 기록 및 승인 후 사용할 1회 유료 진단](REGIONAL_CASE_RESEARCH_20260912.md).
- 추가 사용자 승인 후 16:30 재실측 1회에서 HTTP 429 / `credit_balance_exhausted` /
  `insufficient_quota`를 확인했다. 이번 실패는 API 선불 크레딧 소진이며 `OPENAI_QUOTA_ERROR`로 기록됐다.
  사례 부족으로 로컬 비교·작성 전에 중단했으며 추가 유료 재시도·저장 보고서 변경 없음.
  크레딧 복구 후 실제 내용 검증이 남아 있다. 이전 상세 기록 없는 요청의 원인은 소급 단정하지 않는다.
- 충전·Tailscale 연결 후 승인된 조사 1회 성공: 신규 검색카드 5건, 공통 카드 포함 최종 비교8건.
  Qwen 실제 처리에서 비교표12KB 초과와 반복 조회에 따른 문맥 초과를 각각 확인했다.
  비교표 전체 분할 읽기·정확히 같은 성공 결과 참조 재사용을 추가하고 관련157테스트 통과.
  마지막 수정 후 로컬 재생은 자동 승인 검토 사용 한도에 막혀 미실행. 최종 품질 미승인 유지.
  원문 대조상 서울 박람회 기간 혼합·일부 매출 출처 연결, 지역 문자열 중복 집계 문제가 남아 있다.

### D-151 모델 호출 없는 코드 점검과 브라우저 실패 복구 (2026-09-12)

- 사용자 요청으로 Qwen/Gemma/OpenAI 호출 없이 코드·모의 테스트·React production 화면을 점검했다.
- 외부 글꼴 CSS 의존을 분리하고 페이지 오류 복구 경계를 추가했다. 작업 ID/지역은 주소로
  전달하며, 브라우저 저장소 장애가 결과 표시·수정·MySQL 확정 저장을 막지 않게 했다.
- 사례의 자유서술 지역 문자열을 시·군/명시된 광역시 구·군 단위로 집계하고 포함 지역 중복을
  제거한다. 조사 캐시 계약 v6 적용. 출처 사실성 및 실제 생성 품질 승인은 별개다.
- 사용하지 않는 동기 생성/자동 샘플 우회 함수와 비어 있던 전역 CSS 진입 파일을 정리했다.
  현재 렌더러가 참조하는 구형 모듈과 기존 데이터·보고서·모델·미커밋 작업은 보존한다.
- Python 전체 462개 점검에서 구형 PPT 계약 3개 실패를 수정 후 관련 22개 통과,
  캐시/로컬 우선 정책 39개 통과. frontend 19개·lint/build, 저장 장애 브라우저 대역 검사 통과.
  [범위·근거·남은 실제 생성 검증](CODE_REVIEW_20260912.md).

### D-152 로컬 출력 Schema 설명 중복 제거 (2026-09-13)

- 사용자 승인으로 저장된 부평 사례를 재사용해 Qwen·Gemma 로컬 진단을 재개했다.
  Tailscale/Ollama 연결 정상. 모든 OpenAI 호출·보고서 저장을 차단한 진단이다.
- Qwen 실제 도구 입력 약 30.3k에서 최종 JSON 문맥 예산 초과가 남았다. system에 이미
  전달한 필드 description만 최종 format에서 제거하고 원본 Schema 검증은 유지한다.
  필수 필드·타입·enum·출처·응답/문맥 상한을 완화하지 않는다. 모의 66개 통과.
- 재실행에서 Qwen 후보 비교(920.7초)·Gemma 작성(129초)은 완료했지만 Qwen 검수 도구
  선택의 빈 응답으로 중단했다. 필수 근거 조회 완료 시에만 최종 검수 JSON으로 이어가도록
  보완했고 관련94개 모의 테스트 통과. 최종 JSON 빈 응답은 계속 실패로 처리한다.
- 이 마지막 수정 후 실재생 미완료. 실제 초안의 쿠폰 지급량 산식·00% 목표·타지역 규모
  혼합·전후 증가의 인과 표현은 품질 미달로 남긴다. 유료0회, 보고서/ML/승인 변경 없음.
  [실행 ID·수치·후속 상태](REGIONAL_CASE_RESEARCH_20260912.md).

### D-153 후보 보완 사항의 본문 전달과 운영별 산식 (2026-09-13)

- 사용자 요청에 따라 오늘은 LLM을 호출하지 않고 저장된 부평 응답과 코드로 점검했다.
  후보 오류 목록이 Planner의 장문 지시에만 언급되고 로컬 최종 입력에는 보장되지 않던 경로를 수정했다.
  최초 작성 직전에 `candidate_handoff` 피드백 원문을 전달하고 재작성에도 기존 검수 지시와 함께 보존한다.
- 기존 안전 보정에서 넓은 spend_conversion enum을 환급으로 간주하던 문제를 수정했다.
  실제 mechanism에 따라 쿠폰 지급 건수/사용률, 신청별 상한 환급/재사용액, 야간 이용 완료율을 구분한다.
  모호한 소비 사업에는 환급을 새로 만들지 않는다. 금액·성과를 생성하지 않는 미확정 변수식이다.
- 점포 수×방문객 지급 단가와 미완성 00%를 기존 보완 경로에 전달한다. 함수가 인용을 바꾼 후보는
  ID가 유효해졌다는 이유만으로 비교 설명까지 통과하지 않으며 지역·운영량·성과 해석을 다시 검토한다.
- 관련151개 오프라인 테스트 통과, 저장 부평 후보 재생의 산식/측정 문장 직접 확인.
  진단 원본 SHA-256 유지, LLM/유료 호출0, 저장 보고서·ML·승인 변경 없음.
  실제 Gemma 초안의 잘못된 단가식·00%·전후 매출 인과 표현은 재생성 전이므로 여전히 미승인이다.
  [세부 기록](REGIONAL_CASE_RESEARCH_20260912.md#2026-09-13-llm-없는-후보작성-전달-보완).

### D-154 실제 로컬 재검증과 측정 주기·담당 위치 오탐 수정 (2026-09-14)

- 개인 노트북 테스트 요청으로 저장 부평 사례의 Qwen→Gemma→Qwen 진단을 실행했다.
  전 단계 JSON 완료. Qwen 최초 출력8,000토큰 미완료 재생성으로 후보 단계20분 소요, 품질 미승인.
- 월간 주기 및 같은 실행 단계 deliverable에 명시한 담당자를 누락으로 판단하던 오탐을 수정했다.
  담당의 실재/권한을 인정하거나 측정 분모 요구를 완화하지 않는다. 관련101개 오프라인 테스트 통과.
- 저장 초안에 서버 보정과 코드/사람의 수정 지시를 적용한 Gemma 보완1회·Qwen 재검수1회 완료.
  실제 본문 산식·쿠폰 분모·월/분기 혼동·인과 단정 표현은 개선됐으나 후보 선정 이유와 비교 설계는
  미완료, 최종 approved=false/81점. 신규 사례 검색·운영 전체·전국 품질 검증으로 간주하지 않는다.
- 유료0회, 기존 보고서/ML/원본 보존. [단계별 시간·실제 응답 검토](REGIONAL_CASE_RESEARCH_20260912.md#2026-09-14-실제-로컬-작성보완검수).
- 추가 후보 보완 진단은 사전 추정 문맥42,797+출력8,000이 Qwen40,960을 넘어 추론 전 실패했다.
  시간 초과나 계정 차단으로 분류하지 않는다. 본문 보완 성공을 후보 재비교 성공으로 확대하지 않는다.
  유료 대체·원문 절단·상한 변경 없이 입력 구조 보완과 실검증을 남긴다.

### D-155 로컬 문맥 확장 요청과 실제 할당 확인 (2026-09-14)

- 사용자는 개인 GPU의 문맥·대기 여유 확대 및 동일 입력 테스트를 승인했다.
  현재 실행 엔진은 vLLM이 아니라 Ollama 0.34.0이다.
- Qwen 요청65,536/실제40,960을 확인했다. D-145의 '최대40,960'은 현재 배포본과
  Ollama의 제한을 뜻하며 Qwen 계열 자체의 절대 최대치가 아니다. 지원 모델/엔진 구성
  없이 환경값만 올리지 않는다. Qwen40,960 유지.
- Gemma 요청131,072/실제131,072 및 동일 저장 초안 보완·Qwen 검수 완료.
  개인 `.env`의 Gemma 문맥을131,072로 올렸다. 기존 출력 상한·유료 정책은 유지한다.
  생성 요청에는 자동 입력 절단/문맥 이동 금지 옵션을 명시했다. 관련70테스트 통과.
- 실제 결과는 후보 선정 근거·쿠폰 사용률 비교 집단 문제가 남아 미승인82점이다.
  128k 할당 확인을 128k 입력 품질 검증이나 전체 기획안 품질 승인으로 확대하지 않는다.
  [모델별 설정·제약](GENERATION_TOKEN_BUDGETS.md#2026-09-14-모델별-문맥-실측-d-155).
- Qwen 동일 원문 네이티브 진단에서 실제30,052입력/2,847출력으로 완료했다.
  사전 추정42,797이 실제보다 커서 차단됐음을 확인했다. 합성180,017입력은 HTTP400으로
  거절함을 확인하고 opt-in 실제 토큰 검증을 연결했다. Ollama0.34.0이상 확인 후에만
  자동 절단/문맥 이동 없이 호출한다. 출력 중 문맥 소진·미완료는 실패로 유지한다.
  개인 환경 true/공유 예시false, 관련75테스트 통과. 유료 정책/출력 상한 유지.
  원문 후보는 잘못된 비교위험·선정이유·중단조건 반복이 남아 내용상 미승인이다.

- D-155 운영 Provider 재생 완료: Qwen후보703.815초(기존 인용교정 포함)→Gemma53.051초→Qwen검수130.305초, 문맥 오류 없이 완료. 후보/본문 의미 문제와 검수 오탐은 남아 병합81점/미승인. 유료0, 기존 보고서 불변. 세부 기록 REGIONAL_CASE_RESEARCH_20260912.md.

### D-156 서버 기본 측정 설계의 비교 집단 오류 수정 (2026-09-14)

- 사용자는 직접 생성 여부와 품질 개선 진행을 문의했다. 저장된 부평 응답을 대조하여 서버의
  기본 측정 문장 자체가 미운영 집단과 이용률을 동일 분모로 비교하도록 안내한 문제를 수정했다.
- 이용률은 같은 자격·혜택·관찰기간의 운영 집단끼리 각 집단 분모로 계산한다. 미운영 집단을
  이용률0으로 처리하지 않는다. 매출은 동일 상점·업종·요일·집계범위의 취소 제외 결제액으로
  별도 비교하며 전후4주는 매출 비교 기간으로만 사용한다. 인과효과는 확정하지 않는다.
- 정확히 일치하는 이전 서버 문장만 요청 내에서 교체하고 선택 후보의 success_metrics에도
  동기화한다. 저장 보고서·원본은 변경하지 않는다. 일반 사용자 측정 문장은 일괄 교체하지 않는다.
- Gemma에 선택 후보 혜택 조건 일관성, Qwen검수에 실제 문구·필드 근거 및 미확정 견적 구분을
  명시했다. 새 호출이나 라이브러리 없음. 관련103테스트 통과, 실제 저장 입력 보정·원본 불변 확인.
- 실재생은 자동 승인 검토의 'Selected model is at capacity'로 실행 전 거절됐다. 개인 GPU 오류나
  기획 품질 개선 완료로 기록하지 않는다. 후보 우열 설명·다른 사례 위험 혼합은 여전히 후속이다.
- 후속16:47 실재생 완료: 네트워크 접근 승인 후 Gemma74.445초/Qwen148.848초,
  문맥 오류 없이 완료. 완주 혜택·운영 집단별 이용률/별도 매출 비교·담당 역할 표현 개선을
  본문에서 확인했다. 후보 비교는 재실행하지 않았고 Qwen 검수의 잘못된 필드·ML 효과 해석은
  남았다. 병합81점/미승인, 유료0·원본 불변, 관련103테스트 재통과. 상세 실측은
  REGIONAL_CASE_RESEARCH_20260912.md의 D-156 후속 기록을 따른다.

### D-157 후보 설명의 반복 덮어쓰기 수정과 사례별 비교 재검증 (2026-09-14)

- 사용자는 남은 품질 개선을 계속 진행하도록 요청했다. link_decision에서 현재 인용이 이미
  보정됐는데도 과거 original_case_source_ids를 기준으로 local_fit/differentiation을 다시
  일반 문장으로 바꾸던 경로를 수정했다. 현재 인용이 바뀔 때만 보정하며 감사 원문은 유지한다.
- 선택 이유는 실제 후보의 장단점, 사례별 위험은 해당 case_source_id의 운영/위험 원문과
  연결하도록 기존 JSON 필드 안내를 보완했다. 새로운 호출·유료 한도·라이브러리 추가 없음.
- 반복 적용/설명 보존/원본 불변 회귀 테스트를 포함해 관련118개 통과.
  저장된16:47 입력으로 로컬 후보 보완1회를 실행하며 모델 원응답과 서버 보정 결과를 별도
  보존한다. 실제 결과는 REGIONAL_CASE_RESEARCH_20260912.md에 기록하며 점수/검사만으로
  품질을 승인하지 않는다. 기존 보고서·ML·사용자 운영 설정은 변경하지 않는다.
- 실제 재비교1,099.423초 완료(기존 인용교정 포함). 서울 사례에 섞인 인천 위험은 교정됐으나
  local_fit 복제·빈약한 대안 비교와 근거 없는 중단율20~50%가 남았다. 모델/후처리 결과를
  분리 보존했고 미승인 유지. 추가 유료 호출이나 기존 보고서 반영 없음.

### D-158 제한된 후보 비교 실험과 운영 적용 보류 (2026-09-14)

- 사용자 요청으로 저장된 부평 사실·ML·사례에서 이전 후보를 제외하고, 선정 논리만 작성하는
  진단을 진행했다. 예산·중단율·본문은 작업 범위에서 제외했다. 실제 원문은 불변 보존했다.
- 동일 자료/지시/Schema에서 think=false/true 비교를 완료했다(86.080/85.464초).
  둘 다 문맥과 출력에 여유가 있었지만 실제 판단 오류가 남아 운영 추론 모드를 바꾸지 않았다.
- 세 번째 진단은 fact_id/risk_id를 선택한 뒤 Python이 원래 사실과 위험을 결합했다.
  133.622초 완료, 근거 연결은 정확했으나 대안 비교·행동 연결의 내용은 부족하여 미승인이다.
- 세 실험은 로컬 호출만 사용했고 운영 호출 횟수·유료 정책·기존 보고서·ML·설정을 바꾸지
  않았다. 후보 전체 품질 개선으로 보고하지 않는다. 상세 및 기간 해석 보완은
  REGIONAL_CASE_RESEARCH_20260912.md의 D-158 기록에 있다.

### D-159 비교 기준 고정과 모델별 후보 품질 진단 (2026-09-14)

- 사용자의 후속 진행 요청으로 이용 행동·준비·운영·성과 확인의 동일 기준 비교를 실시했다.
  저장된 두 사례와 사실14개를 사용하고 후보별 근거 ID를 서버가 결합한다. 진단 전용이다.
- 동일 입력에서 Qwen은 반복·전환 조건 모순이 남았고 Gemma는 항목별 구체 설명이 나아졌다.
  Gemma의 A/B 순서 반전에서는 선택안이 달라져 안정된 우열 판단은 확인되지 않았다.
- 기존 후보 JSON 전체를 Gemma로 작성하는 추가 진단에서 지역 신호와 대안 비교가 구체화됐으나
  소비 전환 사실 단정·야간/주간 측정 비교·미확인 ready가 남았다. 이 결과를 운영 승인하지 않는다.
- Router 메모리 안에서만 실험 모델을 선택했다. 운영 설정·기존 보고서·ML·유료 한도는
  변경하지 않았다. 시간·토큰·실제 내용은 REGIONAL_CASE_RESEARCH_20260912.md D-159에 기록했다.

### D-160 사실·가설 분리와 동일 기준의 후보 비교 전달 (2026-09-14)

- 사용자는 사실/가설 구분과 일관된 비교 기준 적용, 향후 기획서 PPT에서의 선정 이유 설명을 승인했다.
- 진단 opt-in 후보 reasoning에 사실 ID·적용 가설·이용 행동·운영 준비·성과 확인·감수할 조건을 분리한다. 지역 관측 출처와 ML signal의 지표/기간/집계별 ID를 요청 Schema에 제한하고, 서버가 원래 값·기간·신뢰도 기록을 결합한다. 새로운 추천 점수·효과율을 계산하지 않는다.
- 확대 계약이 지정된 Qwen 비교 입력→Gemma 선택 후보 조회→planning_decision에 같은 연결 기록을 유지한다. 일반 생성에서는 기본 Schema를 유지하고 진단 계약을 강제하지 않는다. 사실 연결을 의미 타당성·실행 가능성 승인으로 승격하지 않는다. 기존 후보·보고서·운영 모델 설정·유료 및 운영 호출 횟수는 보존한다.
- 기록이 있는 신규 후보는 기존 PPT 디자인을 유지한 추가 선정 근거 페이지에서 관측/전망·적용 가설·동일 세 기준·선정 이유를 설명한다. 전체 원문과 감수할 조건은 발표자 메모에 남긴다. 과거 보고서의 미기록 판단을 만들어 넣지 않으며 인용/설계 변경 시 과거 설명을 현재 근거로 표시하지 않는다.
- 검증 결과와 실제 응답의 잔여 오류는 REGIONAL_CASE_RESEARCH_20260912.md의 D-160에 별도 기록한다.

- D-160 실측: Gemma 후보/후보 보완/본문/본문 보완 완료. 관측·전망 연결과 적용 가설 표현 개선을 확인했으나 대안 근거·측정·실행 상태 및 Qwen 반복성은 미완료다. 새 필수 계약은 진단 opt-in으로 유지한다. 실제 운영 인력 주어의 담당 누락 오탐만 공통 검사에 교정했다. 실제 설명 PPT 검수본과 상세 기록은 D-160 실측 문서를 따른다.

- 최종 관련 unittest141개 통과. 실제 Gemma4회와 소스 불변·유료0을 확인했다. React/SQL/ML 학습 변경이 없어 frontend/DB 재점검은 이번 변경 범위에 포함하지 않았다. 검수 항목 감소를 전체 기획 품질 개선으로 대체하지 않는다.

### D-161 두 지역 실제 생성 점검과 후보 보완 진입 오류 수정 (2026-09-14)

- 사용자는 기존 운영 경로에서 부평구·제주시 생성→저장→문서 출력을 확인하는 제한된 최종 검증을 승인했다. D-160 확대 계약은 계속 opt-in이며 새 추천 모델·유료 한도·실험 호출을 추가하지 않는다.
- 부평구는 실제 생성·저장·재조회·PPT/Word 다운로드를 완료했다. 선택안은 새 인천 야간관광 사례를 연결한 야간 체류 프로그램이며 강진 고정 출력은 아니었다. PPT 16장/Word 16쪽의 실제 렌더 및 차트·목표·견적 수치 일치를 확인했다. 원래 검수는 81점/미승인이므로 승인으로 바꾸지 않았다.
- 제주시 지역 조사와 신규 사례 5건의 출처 확인, Qwen 응답까지 완료했으나 선정 ID가 유효 후보 목록에서 탈락해 본문 전에 중단됐다. 원래 후보 JSON이 실패 trace에 보존되지 않아 어떤 문구·ID가 탈락을 유발했는지는 확정하지 않는다. 중단 지점은 `constrain_decision`이며 기존 후보 보완 전에 예외를 내고 자동 입력 사용자에게 조건 변경을 요구한 흐름을 수정했다.
- 로컬 우선 경로의 실패 후보는 원래 후보·선정 요약을 그대로 보존한 `constraint_repair` 피드백으로 기존 보완 1회에 전달한다. 보유 사례 연결 문제만으로 새 웹 조사를 추가하지 않는다. 보완 이후에도 연결 실패가 남으면 일반 안정화 전에 차단하여 다른 후보의 제목·범위·예산을 섞지 않는다. 자동 방향/제외 없음의 실패는 사용자 입력 오류로 안내하지 않는다. 실제 수정 후 제주시 완주 및 품질 승인은 아직 미확인이다.
- “안전 관리자가/기획팀이”의 역할 누락 오탐과 근거 없는 수치 중단 규칙을 단순히 가정으로 접두하던 처리를 수정했다. 원래 수치 규칙은 감사 기록에 보존한다. 명시적 사용자 목표를 일괄 삭제하지 않는다.
- Word 진단 표와 PPT/Word 목표 근거의 공통 문구를 관측·전망·계획 가정에 맞게 정정했다. 캐시 버전은 Word v10, PPT v21이며 승인된 디자인과 원래 저장 보고서는 유지한다.
- 전체 AI 검사 453개 통과 중 TestClient startup이 실제 진행 job에 영향을 준 격리 결함을 발견했다. 이번 부평 검증 job 1건만 영향이 확인됐고 라이브 상태와 조건부 대조 후 복구했다. 관련 3개 테스트 모듈의 startup을 격리했으며 DB 접속 금지 상태에서 19개 통과. 마지막 변경 후 관련 106개 통과, 프런트엔드 19개/lint/build 통과. 실제 실행/수치/부작용 기록: `output/final_validation_20260914/README.md`.

### D-162 최소 최종 검증 종료와 실행 안내 일정 연결 (2026-09-15)

- 사용자는 불필요한 반복 검사를 생략하고 필요한 확인만으로 종료하도록 요청했다. 남아 있던 제주시 실제 생성 1회를 실행했고 후보 비교·보완 → 본문·보완 → 검수 → MySQL 저장 → PPT/Word 다운로드를 완료했다. OpenAI 2회, 로컬 문맥/연결 오류 없음. 부평구와 제주시의 대표 기능 검증은 종료한다.
- 선택안은 서울 문화재야행 문서를 연결한 제주시 야간 문화유산·시설 개방 프로그램이다. 해당 사례는 정량 성공사례가 아닌 운영·예산 참고 자료이고 최종월 +5%는 계획 가정이다. 상대적 선정 설명과 실제 운영 조건의 한계가 남아 81점/미승인을 보존한다. 기능 완주를 품질 승인으로 바꾸지 않는다.
- 5단계 본문을 4단계 안내로 묶을 때 첫 3단계를 고정 배정하여 준비 10월이 시범 운영 기간으로 표시되는 오류를 발견했다. 추가 준비 단계를 앞쪽에 합쳐 마지막 운영 준비·운영·평가 일정이 유지되게 공통 함수를 정정했다. 실제 본문의 운영 11~12월, 평가 12월과 PPT/Word가 일치한다. 원래 보고서·일정·수치·승인 상태는 변경하지 않는다.
- 해당 일정 오류의 PPT/Word 재현 및 원본 불변 검사 1개만 실행해 통과했다. 전체 자동검사·프런트엔드·ML 학습을 반복하지 않았다. 캐시 버전 Word v11/PPT v22, 진행 작업 없음 확인 후 이번 AI 서버만 재시작했다. LLM 추가 호출 없이 문서를 재출력했다.
- PPT 16장/Word 16쪽 실제 렌더 전체 확인, 저장값과 차트·표·견적 일치. 수정된 PPT 7·14장/Word 7쪽 재확인, 나머지 페이지는 이전 렌더와 동일. 최종 기록과 파일: `output/final_validation_20260915/README.md`.

### D-163 PPT 공통 시각 구성과 선택 사례의 실적 연결 (2026-09-15)

- 사용자 요청: 사진 캡션 중앙 정렬, 사업 범위 상향 배치, 사업 목표 제목/쉬운 설명, 적용·후보 사례와 체크 표시, 사례 실적을 사례 소개 바로 다음에 배치, 산출 설명 도식화, 출처 글자 축소. 전체 지역 공통 출력 함수에 반영하고 PPT 캐시 버전을 v23으로 올린다. Word는 이번 디자인 변경 대상이 아니다.
- 선택 서울 문화재야행의 기존 예산 문서 83쪽에 중구 정동야행이 포함됨을 확인했다. 중구청의 2023.10.15/2024.05.27 보도자료에 따른 행사 방문객 약 10만→13만, 약 3만 증가(+30%)를 출력 시 별도 연결한다. 2023년 10월 행사와 2024년 5월 행사 비교이며 서울 전체 실적·사업 단독 효과·ML 대비 증가로 표시하지 않는다. 소비 증가액은 만들지 않는다. 실제 사진은 서울문화포털 2024 정동야행 자료를 원형 비율로 사용하고 출처를 표시한다.
- 연결은 정확한 원문 첨부 식별자/파일 번호/지역·사업 내용으로 제한한다. 다른 지역·다른 야간 사업에 같은 실적을 붙이지 않는다. 강진 기존 실적도 선택 사례의 강진 환급 방식과 연결된 기존 목표 근거가 있을 때만 표시한다. 저장 목표 +5%, 전망, 견적 79,057,000원과 미승인 상태를 유지한다. 새 실적을 생성 당시 LLM 입력으로 소급하지 않는다.
- 산출 근거는 3개월 기준 전망→추가 목표→직접 운영 참여량, 견적 구성 비중, 2개 수치 기준선/5개 관광 흐름 지표→공식 사례→Qwen→Gemma→출력으로 구성한다. 각 설명과 값은 저장 보고서에서 가져오며 운영량·단가는 계획 가정임을 구분한다.
- 관련 오프라인 검사 4개 통과. 실제 제주시 PPT 15장 PowerPoint 렌더 확인, 2개 편집형 차트·내장 엑셀 일치, 기존 KPI/견적 표와 출처 URL 보존 확인. 공식 추가 출처를 포함해 부록을 3→2장으로 압축했다. 번들 finalizer/Artifact Tool import 통과. 표지 배경/목차 워터마크의 의도된 겹침은 화면으로 확인했다. LLM·유료 호출·ML 재학습 없음. 진행 job 0개 확인 후 이번 AI 서버만 재시작. 산출물·검수 기록: `output/ppt_redesign_20260915/`.

### D-164 목표 KPI 이유와 5-Agent 파이프라인 인포그래픽 (2026-09-15)

- 사용자는 11장의 예산·기준 전망 설명을 제거하고 목표율을 선택한 이유와 추가 방문·소비 규모만, 12장은 생성 클릭부터 OpenAI 조사·후보 비교·본문 작성·검수·출력의 아이콘 그림으로 구성하도록 요청했다. 두 공통 함수를 변경하고 PPT 버전을 v24로 올렸다.
- 제주시 5%를 통계적으로 최적화된 값이나 정동야행 30%에서 계산한 효과로 꾸미지 않는다. 기존의 조정 가능한 최종월 초기 목표를 명시하고 3개월 추가 방문 348,601명/소비 148.12억원을 그대로 사용한다. 사용자 조정 목표 및 기존 강진 목표 근거는 각각 구분한다.
- Evidence/Case Scout의 병렬 수집, Transferability/Planner/Reviewer의 역할을 ①~⑤로 표시한다. 지역 SQL·7개 ML 입력과 보완 재검수 연결을 포함한다. 모델은 보고서 completed provider 기록을 사용한다. OpenAI 독립 최종 검수가 생략된 제주시를 유료 검수 완료로 표시하지 않는다.
- Lucide 0.468.0 공식 아이콘을 로컬 보관하고 ISC/Feather MIT 라이선스와 원본 SVG를 유지한다. 기존 번들 sharp로 색상별 PNG를 준비했고 패키지 설치는 없다. 도식의 글자·연결선은 편집 가능하다.
- 관련 기존 문서 계약 검사 2개 통과, PPT 최종 패키지/내장 차트/Artifact Tool import 통과. PowerPoint 렌더에서 목차·11·12장만 변경되고 나머지 12장은 이전 화면과 해시가 동일함을 확인했다. 차트/KPI/견적 값 및 기존 출처 보존. LLM·유료 호출 없이 출력했고 진행 job 0개 확인 후 이번 AI 서버만 재시작했다. 산출물: `output/ppt_kpi_pipeline_20260915/`.


### D-165 PPT 최종 디자인과 목표 산출근거 순서 (2026-09-15)

- 사용자 요청에 따라 목표 KPI 산출근거를 사업 목표 바로 다음(기본 5장)으로 이동하고 목차를 동기화했다. 해당 페이지 글자 크기를 낮추고 3장 목표 영역을 30px 위로 이동했다.
- 제공 예시 PPT의 여백·파란 제목·청록 강조선·흰 정보 영역을 참고했다. 공통 `proposal_visual_finish`에서 제목 크기, 페이지 번호, 표 문단 간격, 사례/실행 안내 강조선과 숫자 크기를 통일한다. 데이터 내용과 Word 출력은 변경하지 않는다.
- 공통 출력 캐시는 `pptx-v25-editorial-polish`. 제주시 15장 PowerPoint 렌더를 모두 확인했다. 관련 검사 2개, 패키지/내장 차트/Artifact Tool import 통과. 차트 값, KPI·견적 표, 기존 출처 링크 보존 확인. 표지/목차의 의도된 배경 겹침 4건은 유지한다.
- 추가 LLM·유료 호출·ML 재학습 없이 저장 보고서로 출력했다. 결과와 검수 기록: `output/ppt_final_polish_20260915/`.


### D-166 사용자 편집 PPT 순서와 지역별 성장률 비교 (2026-09-15)

- 사용자가 직접 편집한 `1315제주시_기획서_최종.pptx`의 순서를 확인했다. 기획 의도 → 사례 → 실적 → 사업 목표 → KPI 근거 → 지역 적용/실행 → 월별 KPI → 견적 → 생성 과정으로 공통 배치를 변경한다. 메인/서브 헤딩 1./1.1과 목차를 맞춘다.
- 사용자는 인구·인구밀도 보정 모델은 추후로 미루고, 우선 동일 기간 전년 대비 증가율 비교를 승인했다. 주민 인구를 모델에 넣었다고 표시하지 않는다. 기존 지역별 모델을 재학습하지 않고 동일 202606 관측 종료월·데이터 지문·전년 동기 양수값을 충족하는 249개 지역을 비교했다. 강남구는 관측 종료월 차이로 제외했다. 시/산하구 중복 지역은 함께 집계하지 않는다.
- `proposal_growth_comparison`은 저장 모델과 processed CSV의 지문을 확인해 같은 기간을 추론하고, 지역별 3개월 합계의 전년 동기 증가율을 계산한다. 평균은 지역별 증가율 단순평균이며 전국 고유 방문자 총량 증가율이 아니다. 성장률 공동 순위를 사용한다. 입력 파일·기간 지문으로 결과를 로컬 캐시한다. 보고서 모델 지문/예측값이 다르거나 비교 지역이 적으면 임의 순위를 만들지 않고 지역 전망 페이지를 사용한다.
- 제주시 방문 -2.9970% / 비교평균 -0.3948%, 소비 -0.3442% / 비교평균 +6.6409%. 저장 목표 달성 가정 시 +0.1991% / +2.9735%, 성장률 순위 208→87 / 228→68. 다른 지역 전망을 고정한 조건부 순위이며 사업의 인과효과나 예측 정확성을 증명하지 않는다. 소비 평균은 일부 높은 예측 증가율에 민감하며 지역 추천 순위로 사용하지 않는다. 선택 사업/기존 목표/견적/ML 모델은 변경하지 않는다.
- PPT 캐시 `pptx-v26-regional-growth`. 기존 계약 검사 2개, 249개 지역 산식과 평균/순위 재계산, 기존 차트와 KPI·견적 표 보존 확인. PowerPoint 렌더/패키지/내장 차트 검수. 새 LLM·유료 호출·ML 학습 없음. 결과 `output/ppt_growth_20260915/`.
- Word 공통 디자인은 작업 중 사용자가 PPT를 우선하도록 요청해 최종 전달을 보류했다. 작업 파일과 렌더는 `output/word_final_polish_20260915/`에 보존하며 이번에 Word 완료로 보고하지 않는다.

### D-167 기획 의도 페이지 사진 복원과 비교 막대그래프 (2026-09-15)

- 사용자 요청으로 3장의 기존 왼쪽 전체 높이 지역 사진·중앙 캡션을 복원했다. 오른쪽에 향후 3개월 방문/소비 예측 합계와 전년 동기 대비 증가율의 지역/전국 평균 막대그래프를 배치했다. 순위와 조건부 목표 순위는 이 페이지에서 제거했다. D-166의 비교 집단·원자료·산식은 유지하며 총량 평균이나 인구 보정으로 바꾸지 않는다.
- 공통 생성 버전 pptx-v27-photo-growth-bars. 양수/음수 막대 모두 0을 기준으로 표시하며 음수 색상을 명시하고 OOXML 축 ID를 양수로 정규화했다. 엑셀/차트 캐시는 소수 6자리로 일치시키고 표시는 2자리, 전체 계산 정밀도는 메모에 보존한다.
- 관련 기존 검사 2개 통과. PowerPoint 15장 렌더에서 3장만 변경 확인. 원래 사진/캡션, 2개 신규 편집형 막대그래프, 기존 목표 차트/표 유지. 패키지·차트/엑셀 일치·Artifact Tool import 통과. 결과 output/ppt_photo_growth_20260915/. LLM/유료 호출 없음.

### D-168 SPICUS 참고 제안서 테마의 공통 PPT 적용 (2026-09-15)

- 사용자는 제공한 SPICUS 기업 제안서의 테마·박스·색상·인포그래픽으로 전체 PPT 디자인을 바꾸되 내용/수치/기능은 유지하도록 요청했다. 참고 32장(A4 가로)의 회색 배경, 검정 중앙 제목, F50045 강조, 둥근 흰 카드, 빨강/청록 차트를 16:9 발표용으로 적용했다. 타사 로고·사업 내용·수치는 가져오지 않는다.
- 목차 도입은 0, 첫 내용 페이지는 0. 기획 의도 / {지역} 관광 발전 기획안. 왼쪽 지역 사진과 캡션을 유지하고 오른쪽 방문/소비 각각을 제목·합계·막대·비교 해석의 카드로 묶는다. 지역별 공통 코드 `proposal_theme_spicus`로 표지/목차/사례/실행/목표/견적/파이프라인/출처의 시각 체계를 통일한다.
- 선택 모델·숫자·원자료·기획 본문·목표율·견적 산식·보고서 승인 상태는 변경하지 않는다. 차트의 색 설명만 실제 청록/빨강에 맞춘다. 공식 사진 및 출처 링크 보존, Lucide 원본 SVG에서 빨강 아이콘을 기존 sharp로 생성했다. 새 라이브러리 설치나 유료/LLM 호출 없음.
- 캐시 pptx-v28-spicus-editorial. 관련 2개 계약 검사 통과(사례가 적은 fixture 포함). 저장 제주시 15장 PowerPoint 렌더 확인, 4개 native chart 데이터·4개 표·전체 외부 출처 링크가 이전 파일과 일치. 패키지/기하/내장 Excel/Artifact Tool import 통과, 최종 형상 경고 0. 결과 output/ppt_spicus_20260915/. Word는 아직 보류.

### D-169 표지 중앙 지역명과 별도 지역 명소 사진 (2026-09-15)

- 사용자 요청에 따라 표지 중앙에 시도/시군구/프로젝트 제목을 차례로 배치했다. 표지와 3장 사진 캡션은 기존 네이비 0F315B, 흰 글씨 중앙 정렬로 맞춘다. 캡션 높이는 실제 줄 수에 따른다.
- 표지 사진은 선택 지역 주소가 일치하는 관광 Open API 관광지/문화시설에서 3장 장소명 및 이미지 해시가 중복되지 않는 후보를 고른다. 해변·호수 등 경관과 문화 명소를 우선하는 표현용 함수이며 학습한 관광지 인기/추천 순위가 아니다. 최대 3개 후보만 기존 이미지 검증 다운로드를 사용하고 로컬 storage/region_cover_images에 원본과 메타데이터를 캐시한다. 실패 시 반복 사진이나 타지역 사진으로 바꾸지 않으며 사진 없는 표지는 생성 가능하다.
- 제주시 표지는 곽지해수욕장(제주시 애월읍, 한국관광공사 TourAPI), 3장은 가마오름. 저장 보고서의 사진 URL/주소를 사용했고 한국관광공사 곽지해수욕장 안내로 소재지를 대조했다. 본문 수치·표·차트·기획 내용은 유지한다.
- 캐시 pptx-v29-regional-landmark-cover. 기존 검사 2개, 실제 PowerPoint 렌더에서 1/3장만 변경·다른 13장 동일, 지역/중복 배제·파란 캡션·차트/표 보존 검사 통과. 최종 패키지/차트/내장 Excel/Artifact Tool 검수 기록은 output/ppt_cover_20260915/. LLM/유료 호출 없음.

### D-170 마지막 장 별도 지역 사진과 사용자 표지 레이아웃 (2026-09-15)

- 마지막 장은 표지/3장 장소명과 이미지 해시를 모두 제외한 지역 사진을 사용한다. 제주시에서는 곽지해수욕장/가마오름/고내포구로 구분하며 저장 보고서의 TourAPI 사진 URL을 다운로드·캐시한다. 웹 검색·LLM·유료 호출 없음. 사업 사례 사진/포스터에는 공식 홈페이지 출처도 있으므로 모든 사진이 Open API 자료인 것은 아니다.
- 이어서 사용자가 제공한 표지 스크린샷에 맞춰 날짜, 흰색 시도명 띠와 분홍 사선, 왼쪽 정렬 사업 제목, 하단 사업기간을 공통 템플릿에 적용했다. 오른쪽 지역 명소와 파란 출처 표시는 유지한다. 프로젝트 제목/사업기간/본문 수치 변경 없음.
- 공통 PPT 캐시 pptx-v31-province-ribbon-cover. 사진만 변경한 v30 결과는 output/ppt_ending_20260915/, 표지까지 반영한 결과는 output/ppt_reference_cover_20260915/. PowerPoint 전체 렌더와 사진 중복/차트/표 보존 및 패키지 검수를 수행한다. Word 추가 작업은 보류 상태 유지.

### D-171 목차 회색 그라데이션과 역할별 색상 (2026-09-15)

- 사용자 요청으로 2장 목차의 큰 빨간 면을 회색 그라데이션으로 변경하고 제목/안내 글자는 검정·짙은 회색으로 표시한다. 목차 번호와 섹션 표시는 빨강을 유지한다.
- 7/8/9/10/12장의 주요 설명/소제목은 검정으로 정돈한다. 방문 추가 목표와 표는 파랑, 소비 추가 목표와 표는 청록, 사례 적용 표는 회색, 실행 단계는 파랑/청록/보라/버건디로 구분한다. 파이프라인 아이콘도 조사·비교·작성·검수 역할별 색을 사용하고 연결선은 회색으로 낮춘다.
- 기존 Lucide SVG 10개의 색상 변형을 기존 sharp 런타임으로 생성했다. 새 라이브러리/외부 검색/LLM/유료 호출 없음. 내용/메모/수치/사업 가정/사진/차트/표 구조는 유지한다.
- 공통 PPT 캐시 pptx-v32-balanced-semantic-colors. PowerPoint 전체 렌더 대조에서 지정한 6장만 변경, 모든 텍스트·메모·4개 차트·4개 표 데이터 일치. 패키지·기하(경고/발견 0)·Artifact Tool 검수 통과. 결과 output/ppt_balanced_colors_20260915/.

### D-172 작은 목차·소제목 왼쪽 정렬·SPICUS 글꼴 (2026-09-15)

- 사용자 최종 요청: 표지 사진은 장소명만 보이게 하고 출처 메타데이터는 메모에 보존한다. 파란 캡션은 실제 줄 수에 따라 높이를 계산한다. 마지막 장의 '지역 관광 전략기획안' 두 문구를 제거한다.
- 2장 목차는 큰 카드 목록 대신 회색 그라데이션 위 작은 2단 목록/얇은 구분선을 사용한다. 0번 항목과 3장 섹션명은 '개요'. 번호형 소제목은 전체 왼쪽 정렬이다. 디자인 탐색은 https://slidesgo.com/minimalist 를 참고하되 외부 템플릿 파일은 복제하지 않는다.
- 4장 글꼴은 사용자 SPICUS 원본의 Century Gothic(영문)/맑은 고딕(한글 테마)을 명시한다. 원본 3/28장의 활자 체계를 기준으로 제목 26pt, 사례 제목/배지 14pt, 본문 11pt, 출처 9pt를 적용한다. 원본 A4 가로와 현재 16:9 캔버스 크기는 다르지만 포인트 크기는 그대로 적용했다.
- 공통 PPT 캐시 pptx-v33-compact-contents-reference-type. 실제 PowerPoint 15장 렌더, 소제목 12개 왼쪽 정렬/허용 문구 외 텍스트 보존/차트·표 수치 보존 확인. 짧은/긴 표지 캡션 높이 42/60px 확인. 패키지·기하·Artifact Tool 검수 통과. 결과 output/ppt_compact_final_20260915/. LLM/유료 호출 없음.

### D-173 사례 카드 가독성과 지역 적용 공간 사진 (2026-09-15)

- 마지막 장도 표지처럼 파란 캡션에는 장소명만 표시한다. 출처 URL과 메타데이터는 메모에 보존한다. 사례 카드 안내 문장을 삭제하고 위로 42px 이동, 제목/배지 17pt·본문 15pt·번호 20pt로 확대했다. 글꼴은 이전 SPICUS 기준을 유지한다.
- 5장 사례명/실적 영역을 위로 배치하고 7장 중앙 설명을 29px로 조정한다. 적용 영역은 설명과 지역 시설 사진 2개의 구성으로 정돈한다. 사진이 없으면 compact text 배치로 생성한다.
- 사용자 요청 범위에서 기존 관광 Open API 키로 국립제주박물관/제주목 관아 사진만 조회, `ai_server/storage/application_photo_sources/50110.json`에 출처 메타데이터와 기존 지역 사진 캐시에 이미지를 보관했다. 일반 생성에서는 저장 보고서/API 사진 캐시의 지역 주소와 제안 문구의 시설 유형이 맞는 사진을 최대 2개 사용한다. 실제 사업 참여 확정 자료가 아니라 '적용 공간 예시'로 표시하며 출처를 보존한다. 타지역은 그 지역의 확보된 자료만 사용한다.
- 디자인 탐색은 https://slidesgo.com/theme/case-study-infographics-minimalist 와 사용자 SPICUS/스크린샷을 참고. 외부 템플릿 복제·새 라이브러리·LLM·유료 호출 없음.
- 공통 PPT 캐시 pptx-v34-readable-case-cards-local-photos. PowerPoint 15장 렌더에서 4/5/7/8/15장만 변경, 4개 차트/4개 표 수치 보존, 적용 사진 2개·표 간격 확인. 패키지/기하/Artifact Tool 검수 결과 output/ppt_case_layout_20260915/validation-v3.json. 중간의 자동 승인 검토 모델 용량 오류는 읽기 전용 렌더 작업 재시도로 해소했다.

### D-174 목차 날짜 제거와 회색 중간 제목 26pt (2026-09-15)

- 사용자 요청으로 목차 하단 날짜를 제거하고 5장 이름을 `1.2 참고 사례 실적`으로 변경했다. 사례명/설명을 위로 이동하고 사진의 장소·출처는 줄 수에 맞춘 파란 박스에 중앙 정렬한다. 표지/끝 장의 장소명만 표시하는 방침과 공식 근거 부록은 유지한다.
- 5/6/7/12장의 회색 중간 제목은 실제 PowerPoint 26pt로 통일한다. 6장의 한 줄 목표는 기존 전망/목표 시리즈의 기간 합계로 계산한다. 제주시 방문 +3.29%·소비 +3.33%이며 최종월 목표 +5%나 ML 사업효과 추정값과 혼동하지 않는다. 전망/목표가 없으면 증가율을 만들지 않는다.
- 공통 캐시 `pptx-v35-gray-intros-goal-summary`. PowerPoint 15장 렌더에서 2/5/6/7/12장만 변경, 차트 4개와 표 4개 수치 보존, 출처 링크·정확한 26pt 확인. 패키지/기하/내장 차트·Excel/Artifact Tool import 통과. 산출물과 검수 기록은 `output/ppt_gray_headings_20260915/`.
- 기존 `test_proposal_presentation_v3_contract` 8개 중 2개 통과, 6개 실패. 실패 항목은 v20 버전 상수/예전 슬라이드 순서/이전 표지 도형명 등 현재 승인 디자인과 다른 계약을 참조한다. 전체 자동검사 통과로 보고하지 않으며 해당 구형 계약은 이번 디자인 수정 범위에서 재작성하지 않았다. 저장 보고서·LLM·유료 호출·ML 학습 변경 없음.
- 후속 사용자 확인: `pptx-v35-gray-intros-goal-summary` 디자인을 일단 확정했다. 목표율의 사례·지역 특성 기반 산출은 별도 논의이며 디자인 확정이 고정 5%/20% 산식의 품질 승인을 뜻하지 않는다. 인구 보정 모델 구현·학습은 아직 수행하거나 승인받은 결과가 아니다.

### D-175 사업 운영 규모 기반 목표 시나리오 (2026-09-15)

- 사용자는 오차를 허용하는 아이디어 제안 서비스로서, 담당자에게 희망 증가율을 요구하지 않고 사업별 운영 규모부터 목표를 계산하도록 승인했다. 확정 PPT 디자인을 유지하며 공통 페이지 2장을 추가한다. 모든 지역의 생성/미리보기/PPT/Word에 동일한 함수를 적용한다.
- `operating_target.py`는 사업 유형별 운영일·회차·정원·가정 단가와 저장 사업기간 ML 방문 전망으로 제안량을 계산한다. 거점 수는 `ceil(sqrt(월평균 방문/100000))`, 1~12개 제한이라는 명시적 기획 규칙이다. 확보 시설 수·인구 보정·학습한 적정 규모로 주장하지 않는다. 예산 상한에 따라 거점과 편성 한도를 줄이며 기본 운영비도 충족하지 못하면 운영안 미편성으로 구분한다.
- 편성 한도×이용률→참여량, 참여량×추가 방문 비중→추가 방문. 기본 이용률 65%와 추가 방문 비중 30%는 계획 가정이다. 보수/확대는 각각 ±15%p의 가정 변화이며 통계 신뢰구간이 아니다. 검토 완료 표시·출처·기간·사업 참여 범위·원문 인용이 있는 사례 비율만 자동 대체한다. 전년 대비 지역 성장률이나 단순 외지인 비율은 변환하지 않는다. 현재 제주시 카드에 검토된 전환율은 없어 가정이 적용된다.
- 추가 소비는 `추가 방문×ML 소비/방문 합계 비율 + 기존 방문 참여×추가 구매 가정액`. 두 집단을 분리하며 비율은 실측 객단가가 아니다. 기존 참여자 추가 구매액은 유형별 1~3만원, 지역 소비/방문 비율의 50% 이하로 제한한 기획 가정이다. 대체 소비·반복 방문 등 효과는 사후 검증 대상이다. 예산과 목표 사이의 기존 역산 순환을 제거했다.
- 추가 규모를 기존 월별 전망과 단계 운영 가중치로 배분한다. API 상한(최종월 방문 20%/소비 30%)은 유지하고 확대 시나리오도 이를 넘지 않게 편성 한도를 제한한다. ML 전망/관측/공식 수치/검수 상태는 변경하지 않는다. 명시적 사용자 목표·견적은 보존하고 자동 초기값으로 식별되는 구형 5%/20%만 미리보기 사본에서 교체한다. 저장 원본과 승인 상태는 자동 수정하지 않는다.
- Case Scout 구조화 조사에 운영일수·시설·참여·이용률 등 원문 수치 필드를 추가했다. 자동 추출은 검토 완료가 아니며 호출 수/유료 한도/LLM 경로는 확대하지 않는다. 새 실제 LLM·유료 조사는 이번에 실행하지 않았다.
- PPT v36 / Word v13. 제주시 예시: 6개 거점×월 8일×3개월×2회×40명=11,520명분; 참여 7,488명; 추가 방문 2,246명; 추가 소비 157,353,164원; 기간 합계 전망 대비 약 +0.02%/+0.04%; 예상 견적 168,080,000원. 정확한 소비 계산값은 `output/ppt_operating_capacity_20260915/audit.json`을 우선한다.
- 검증: 계산/보존/예산/전망 경계/기존 Word·목표 계약 unittest 22개, 프론트 patch 4개, lint/build, Pydantic 응답 재입력, PowerPoint 17장 렌더 및 6개 내장 차트/4개 표·패키지·Artifact Tool 검사. Word에도 같은 산식·범위 표를 연결해 렌더 확인했다. 자료 없는 지역의 정확한 운영 가능성을 검증한 것으로 주장하지 않는다.

### D-176 작은 목표 증가율의 인원·금액 표시 (2026-09-15)

- 사용자 요청으로 6장 사업 목표 회색 설명은 방문/소비를 각각 판단한다. 기간 합계 전망 대비 증가율이 1% 미만이면 추가 인원/원화, 1% 이상이면 백분율을 표시한다. 판정은 반올림 전 증가율, 금액은 만원 단위 반올림과 '약' 표시를 사용한다.
- 제주시: 방문 +2,246명, 관광소비 약 +1억 5,735만 원. 기존 카드의 1,058만 명은 사업 반영 후 3개월 전체 규모이며 추가 방문 1,058명을 뜻하지 않는다. 정동야행 3만 명은 두 해 행사 방문객 차이로, 제주시의 운영 규모 기반 추가 방문 가정과 집계 범위가 다르다.
- 공통 PPT 캐시 pptx-v37-small-growth-amounts. 계산/목표/ML 값은 유지하고 표시만 변경. 경계값 6개 확인, PowerPoint 17장 렌더에서 회색 설명 확인, 텍스트 변경은 6장 한 곳뿐이며 차트 6개·표 4개 수치 보존. 패키지/기하/Artifact Tool 검수 통과. 산출물 output/ppt_small_growth_amounts_20260915/.

### D-177 목표 산출 설명의 지역·기간 명시 (2026-09-15)

- 7장 자동 목표 설명에 저장 보고서 지역명과 사업기간, 운영 개월 수를 표시한다. 편성 정원·이용률·예상 참여·추가 방문 비중은 해당 지역의 계획임을 명시하고 하단에 거점×운영일×개월×회차×정원 산식을 넣는다. 예산으로 줄인 편성 정원과 원래 운영 정원은 각각 유지한다.
- 공통 PPT 캐시 pptx-v38-capacity-region-period. 제주시 2026.10~2026.12 예시 PowerPoint 렌더 확인, 7장 설명 2개만 변경/차트·표 수치 보존/패키지·기하·Artifact Tool 통과. output/ppt_capacity_region_20260915/. 목표 산식·ML·Word·저장 원본 변경 없음.

### D-178 사례 실적과 제안 운영 정원의 구분 (2026-09-15)

- 사용자 질문에 따라 운영 규모 근거 장에 사례 발표 실적과 우리 지역 수용 정원의 집계 차이를 명시한다. 정동야행이 선택된 경우에만 기존 연결 근거의 행사 방문 10만→13만, 차이 3만을 표시한다. 타지역/타사례에 이 수치를 고정 사용하지 않는다.
- 지역 월평균 ML 전망은 운영 거점 규모 규칙의 입력일 뿐이다. 제주시의 6개 거점은 √(월평균 방문/10만) 올림 및 1~12 제한, 운영일·회차·정원은 사업 유형별 기획 가정임을 설명한다. 사례 3만이나 인구 비례 환산으로 도출한 11,520이 아님을 명시한다. 참여 목표 표기는 참여 가정으로 정돈했다. 수용 정원을 예상/추가 관광객으로 대체하거나 목표 계산을 바꾸지 않는다.
- PPT v39. 내부 PowerPoint 렌더/패키지/기하/Artifact Tool 확인. 변경은 8장 설명, 차트·표 수치는 유지. 미연결 사례에 서울 실적이 복사되지 않는 경계 확인. 사용자 요청으로 파일 전달은 보류하고 내부 검수본만 output/ppt_capacity_distinction_20260915/에 보관한다.

### D-179 아이디어·대략적 목표 중심의 기획서 (2026-09-15)

- 사용자는 정밀한 사업효과 검증보다 근거가 보이는 아이디어와 대략적 목표 제안을 우선한다고 재확인했다. 운영 정원 산식(거점×월 운영일×개월×일 회차×회당 정원)을 필수로 유지하고 '참여 가정' 표시를 '참여 목표'로 복구한다.
- 관측 사실을 꾸미거나 운영 정원을 실제 방문 예측으로 바꾸지 않는다. 목표와 견적은 기획용 수치임을 간결하게 설명한다. 이번 표기 수정에 LLM 호출·모델 재학습·광범위 품질시험은 수행하지 않는다.
- PPT v40. 공통 슬라이드 생성 함수에서 참여 목표 문구와 전체 운영 정원 산식 보존만 확인했다. 파일 재생성/전달은 사용자가 요청할 때 진행한다.

### D-180 사진과 연결되는 차분한 표지 (2026-09-15)

- 사용자 요청으로 첫 장의 붉은 단색 배경만 차분한 청록·남색 투명 그라데이션으로 바꾸고 기존 지역 사진을 화면 전체 비율에 맞춰 배치한다. 지역 띠는 반투명으로 연결하고 제목/날짜/기간/사진 캡션은 보존한다. 공통 표지 함수에만 적용하며 마지막 장은 변경하지 않는다.
- PPT v41. 직전 승인 v40 기준본과 2~17장 XML/메모/차트 모두 일치, 표지 텍스트 유지. PowerPoint 17장 렌더와 패키지/기하/Artifact Tool 검사 통과. output/ppt_soft_cover_20260915/. 기존 사진 재사용, 새 이미지 생성·웹·LLM 호출 없음.

### D-181 참여 목표 75% 하한과 실제 정원·참여 사례 인용 (2026-09-15)

- 사용자는 참여 목표를 75% 이상으로 유지하고, 유사 사업의 지역·사업명·총 정원·실제 참여·계산 비율을 근거로 표시하도록 요청했다. 중앙/보수/확대의 참여율 범위는 75~100%로 제한한다. 목표 하한은 계획 방침이며 실제 사례 참여율은 변경하지 않는다.
- operating-capacity-v2-participation-floor: 선택 후보에 연결된 검토 완료 사례의 같은 기간·사업 기준 정원/참여 인원으로 비율을 계산한다. 공개 비율과 계산값도 대조한다. 원자료 비율이 75% 미만이면 observed_value는 보존하고 목표만 75%로 설정했다는 설명을 사용한다. 정원 없는 퍼센트·예약 경쟁률·도시 전체 방문객은 참여율 근거로 채택하지 않는다.
- Case Scout 기존 구조화 조사에 capacity_count/participant_count/program_name nullable 필드를 추가했다. 확인할 수 없으면 null, 자동 추출과 검토 완료는 구분한다. 추가 유료 호출이나 조사 횟수 확대는 하지 않았다. 이번 웹 검색에서 제주 야간 체험에 인용할 정원과 실제 참여가 모두 확인된 유사 사례는 확보하지 못했다. 기존 정동야행 공식 발표는 10만→13만 행사 방문 실적이며 정원 대비 참여율 근거가 아니다.
- 제주시 기본 목표는 공통 하한 75%임을 명시한다. 운영 정원 11,520명분 유지, 참여 8,640명, 추가 방문 2,592명, 추가 소비 181,575,214원. 기존 65% 자동 목표는 출력/미리보기 사본에서 재계산하고 명시적 사용자 방문·소비 목표 및 저장 원본은 보존한다.
- PPT v42, Word v14. 웹 공통 설명/PPT/Word에 participation_basis와 원문 링크를 연결한다. 계산/보존/예산/출력 합계 unittest 23개, 기존 자동 보고서 이행/Pydantic/스키마 필드 확인. PPT 17장/Word 15장 내부 렌더, 변경 근거 페이지 확인, PPT 패키지/기하/Artifact Tool 통과. output/participation_floor_20260915/. 실제 LLM/유료 호출/새 학습 없음. 파일은 요청 시 전달한다.

### D-182 로컬 축제 실적을 모든 지역의 사례 비교에 활용 (2026-09-16)

- 사용자는 팀의 문화관광축제 데이터셋을 적극 활용하도록 승인했다. 원본 CSV 364개와 설명 이미지를 프로젝트 snapshot에 바이트 그대로 보관한다. 기존 rawdata.zip과 CSV 해시가 모두 일치하며 공유 폴더 재접속은 필요 없다.
- 재현 가능한 importer가 가공 CSV와 catalog를 만들고 신규 MySQL `festival_case_catalog`에 92개 축제를 버전별 적재한다. 기존 월별 사실·ML·저장 기획서·승인 상태는 변경하지 않는다. 방문 기록과 지역코드가 연결된 90개를 후보 조회에 사용하며 봉화·세종의 미연결 자료도 보관한다.
- 공통 함수가 선택 지역의 기존 축제, 검증 peer·ML 조사 방향·사용자 자원에 맞는 타지역 후보를 비교한다. 타지역 후보의 일평균 전체/외지인 방문 증가를 함께 확인하고 개최기간 차이를 보존한다. 점수는 조사 규칙이며 학습한 추천·인구 보정·인과효과 모델이 아니다. 지역·주제 다양성을 유지하고 기존 환급/숙박 등 사례도 비교한다.
- CSV에는 프로그램 세부 운영·원화 소비·정원·참여율이 없다는 경계를 모델에 전달한다. 같은 축제명과 시도·시군구가 확인된 공식 운영 문서에만 수치를 결합한다. 기존 유료 호출 상한은 유지하고 이번 작업에는 유료/LLM 호출을 하지 않았다. CSV만으로 공식 운영 조사가 완료된 것으로 처리하지 않는다.
- 출처가 같은 홈페이지라고 다른 축제를 합치지 않는다. 원문 파일명·해시·기간과 선정 이유를 근거 도구·저장 evidence_sources·PPT/Word에 전달한다. 새 다운로드 URL은 만들지 않는다. PPT v43/Word v15. 75% 참여 목표와 운영 규모 산식은 유지한다.
- 관련 자동 검사 62개 통과. 신규 테스트는 실제 데이터 산식, 3개 지역 후보, 감소 사례 오용 방지, 파일/문서 결합, Qwen 도구 전달 및 공통 출력 수치 연결을 포함한다. 실생성의 아이디어 품질 향상을 검증한 결과로 주장하지 않는다. 구현·복구·이관 명령은 `docs/FESTIVAL_CASE_DATA.md`, 내부 출력 점검본은 `output/festival_review_20260916/exports/`.

### D-183 대전 서구 실제 생성 점검 후 공통 연결 보완 (2026-09-16)

- 사용자 제공 대전 서구 보고서 `3ed8a97565df4fbaa7fd652c4d147a71`에서 MySQL 축제 4건의 전달은 확인했으나 최종 Qwen 평가에서 축제 적용/제외 설명이 빠졌다. 별도 local_instructions에서 축제 규칙을 누락한 경로를 수정하고 기존 조회 슬롯에서 한 축제를 우선 읽는다. 사용자의 사업 방향을 존중하고 선정을 강제하지 않는다. 기존 후보 보완 1회/유료 호출 상한 유지.
- 명시된 시범 개시월부터 운영량·자동 목표·견적을 공통 계산한다. 준비월은 ML 전망만 표시한다. 기존 75% 참여 목표, 30% 기획 추가 방문 비중, ML 값·사용자 목표·저장 원본은 보존한다. 대전 서구 11~12월 기준 추가 방문 2,304명, 추가 소비 82,382,730원, 견적 151,360,000원.
- 야간 방문 비중/전년 증가율 분모 혼용을 출력 사본에서 보정하고 원문을 기록한다. 월 합계만으로 야간 관측이나 낮 중심 방문 패턴을 확정하지 않도록 지침도 연결한다. PPT v44/Word v16, 승인된 디자인 유지.
- 관련 145개 자동 검사 통과. 원본 PPT 4/7/12장 렌더 및 Word 내용 대조. 구형 PPT v20 제목/순서/표지 기준의 별도 검사 7건은 현재 승인 양식과 불일치하여 미통과 상태로 기록했다. 새 LLM·유료 호출 또는 수정 후 Word 렌더는 하지 않았다. 상세: `docs/DAEJEON_REPORT_REVIEW_20260916.md`.

### D-184 생성 대기 안내·경과 시계 (2026-09-16)

- 사용자 요청대로 제목 바로 아래에 작은 빨간 30분 안내와 빛이 지나가는 글자 효과, MM:SS 경과 시간을 표시한다. 동작 줄이기 설정에서는 정적인 빨간 글자로 표시한다.
- 작업 요청 시작 시각을 브라우저 저장소·작업 URL에 보관한다. 화면 갱신 횟수 대신 현재 시각 차이로 계산하며 탭 복귀 시 경과 시간과 서버 상태를 다시 조회한다. 생성은 기존 서버 background task이고 UI 이동으로 취소하지 않는다. 30분 초과 시 지연 안내를 덧붙이며 자동 중단하지 않는다. 기존 시각 없는 작업은 안내 컴포넌트를 처음 연 시점부터 계산한다.
- 최신 목표 버전 v3와 프런트의 구버전 v1 검사 불일치로 미리보기 요청이 반복될 수 있는 조건도 맞췄다. 작업 진행 폴링은 중첩 요청을 막는다.
- frontend lint/build 및 Node 검사 21개 통과. 실제 LLM 생성은 실행하지 않았고 이번 점검 당시 5176 개발 서버가 꺼져 있어 실제 브라우저 화면 검수는 하지 않았다.

### D-185 긴 축제 원문 전달·실행 일정 표시 수정 (2026-09-16)

- 사용자 제공 대전 서구 두 번째 생성본과 저장 기록을 대조했다. 축제 4건은 확보됐지만 원문 조회 제한으로 후보 보완·Reviewer가 `LOCAL_EVIDENCE_NOT_READ`로 끝났다. 신규 웹 조사도 별도 실패했으며 강진 선택을 최적 선정으로 평가하지 않는다.
- 도구별 12KB 제한을 넘는 source+case 전체 JSON은 분할 전달한다. 전 페이지를 읽은 후에만 인용을 허용하며 서버 선조회와 기존 인용 교정도 모든 페이지를 따라간다. 근거 삭제·문맥 검사 완화·모델 호출 확대는 없다.
- 4단계 출력에서 준비/운영/평가를 위치만으로 묶어 11월 운영을 12월로 잘못 표시하던 부분을 명시적 시범 운영 단계로 맞췄다. PPT v45, Word v17. 원본·ML·계획 목표·견적은 보존한다.
- 관련 71개 검사 통과, 실제 축제 원문 4건 무손실 복원 및 저장 입력의 PPT·Word 재출력에서 11월 운영을 확인했다. 실제 LLM·유료 재생성은 하지 않았다. [상세 점검](DAEJEON_SECOND_REPORT_REVIEW_20260916.md).

### D-186 같은 참여 시나리오의 예상 사업비·소비와 공통 출력 보완 (2026-09-16)

- 사용자는 정밀 효과 추정보다 아이디어와 근거 있는 대략적 규모를 원하며, 과도한 견적·축제 후보 활용·제주 공통 양식을 확인하도록 승인했다. 추가 소비를 높여 수익성을 맞추지 않는다.
- `operating-capacity-v4-linked-cost`: 정원 전원에게 환급 상한을 지급하는 편성액 대신 참여 목표×예상 지급액을 견적으로 사용한다. 환급은 계획 결제액×10%, 건별 5만 원 상한. 공동 정산은 점포 묶음 최대 4개당 1팀을 제안한다. 이 설정은 공개 견적·계약·사례 실적이 아닌 계획값이다. 일반형도 참여 목표에 변동 지원단가를 곱한다.
- 참여자 전체 결제액, 추가 소비 목표, 예상 사업비, 100% 참여 참고예산을 구분하고 동일 시나리오에 묶는다. 100% 예산은 평균 계획 결제 기준으로 최대 지급 책임액이 아니다. 기존 ML·선정 결과·명시적 사용자 목표/견적·저장 원본은 보존한다.
- 대전 서구 검토 사본: 참여 4,800명·추가 방문 1,440명 유지. 계획 결제 16,503원, 환급 예상 1,650원, 예상 사업비 39,512,000원. 추가 소비 51,489,277원, 전체 참여 결제 79,214,400원. 종전 422,400,000원과의 차이는 지급상한/예상액 및 인력 편성 가정 변경이다. 효과 입증이나 흑자 판단이 아니다.
- PPT v46/Word v18: 제주 승인 디자인을 공통 유지하고 실적 수치 없는 사례는 이용 흐름 인포그래픽으로 표시한다. 축제 후보 카드에 실제 개최기간 수치를 표시하고 인천 섬 일반사진 대신 문화행사 참고사진을 실제 장소와 함께 명시한다. 관광 API 사진 캐시를 본문/Word에도 사용한다.
- 검사 및 실제 로컬 비교 결과: [기록](DAEJEON_LINKED_SCENARIO_20260916.md). 유료 호출 확대·새 모델 학습·기존 보고서 저장 변경 없음.


### D-192 Gemma 단일 로컬 우선 옵션 (2026-09-17)

- 사용자는 Qwen을 사용하지 않고 Gemma로 모든 로컬 작업을 실행할 관리자 옵션과 현재 설정 변경을 승인했다.
- `local_first_gemma`를 추가했다. 기존 local_first의 비용·근거·검수 경계는 유지하고 로컬 라우팅·사전점검·지역 준비 상태만 Gemma에 맞춘다.
- 개인 Gemma 문맥 131,072 유지, 요청 대기 60분 설정. 실제 짧은 JSON 및 할당 확인, 전체 생성은 사용자 시험 대상으로 남긴다. [동작·재사용 범위](GEMMA_FIRST_MODE.md).

### D-188 개인 노트북 Qwen 27B 시험 전환 (2026-09-16)

- 후속 사용자 요청: 요청 문맥을 65,536에서 70,000으로 조정했다. 라우터 설정 확인만 수행했으며 아래 실제 할당·입력 수용 결과는 이전 65,536 시험이다. 70,000 실측은 미실행이다.

- 사용자는 설치한 `qwen3.8:27b`를 기존 Qwen 역할에 적용하고 속도·품질 저하 시 14B로 복구하도록 승인했다.
- 개인 `.env` 모델 태그와 요청 문맥 65,536만 변경한다. 기존 공통 라우터로 전 지역에 적용한다. 이전 설정은 `qwen3:14b` / 40,960이다.
- 나머지 모델·유료 한도·출력 상한·근거 보존 검사는 유지한다. 후속 로컬 시험에서 실제 65,536 할당과 철원 재구성 입력 29,809토큰 수용을 확인했다. 지원 문맥은 메타데이터 기준 262,144. 짧은 계산 시험 오류가 있어 품질 승인은 보류하며 131,072로 추가 확대하지 않는다. 웹 서버는 아직 이전 모델이며 재시작이 필요하다. [실측·복구](QWEN_MODEL_SWITCH_20260916.md).

### D-187 시도 자료를 시군구 기획서의 상위 지역 맥락으로만 연결 (2026-09-16)

- 사용자는 시도 기획서 확장을 미루고 상위 시도 관광 흐름 비교만 적용하며, 공유 폴더의 필요한 자료를 로컬에 보관하도록 승인했다.
- ZIP 415개를 원형·해시와 함께 보관했다. 15개 시도 30개월 4개 지표 1,800행을 별도 CSV/MySQL 테이블에 적재한다. 옛 이름/빈 폴더는 보관만 하며 대전/경북은 이번 원본에 없다.
- 공식 지역코드/소속 시도를 대조해 같은 관측월의 방문·소비 증감률과 숙박 특성을 연결한다. 외지인 경계 차이를 명시하며 총량 순위·유사도·인과효과로 쓰지 않는다. 기존 ML·목표율·견적·저장 보고서·지원 지역·디자인은 그대로 둔다.
- 기존 get_region_metrics와 조사 입력을 통해 전달하고 evidence_sources에 보존한다. 데이터/동일 월이 없으면 해당 비교만 생략한다. 유료 호출 수·보완 횟수·문맥 상한을 변경하지 않는다.
- 관련 55개 자동 검사 및 실제 제주시/철원군/부평구 연결, 대전/강남 생략을 확인했다. LLM 추론이나 기획서 재생성은 하지 않았다. 유선 연결에서도 Ollama와 실제 개발 포트 8112의 Qwen/Gemma ACTIVE 확인. [재현/범위](PROVINCIAL_TOURISM_CONTEXT.md).

### D-189 TP2-4 독립 로컬 실행 포트와 모델 환경 고정 (2026-09-16)

- TP2-3을 보존한 채 TP2-4를 독립 실행하기 위해 개발 포트를 Frontend `5177`, Backend `8200`, AI Server `8212`로 분리한다. Vite 기본 프록시와 `start-dev.ps1`, README 실행 명령을 같은 값으로 맞춘다.
- 백업의 `.env`, 공식 원본·snapshot·가공 데이터·ChromaDB·RAG 문서·저장 기획서를 TP2-4 로컬에 복사한다. 비밀정보와 대용량 실행 데이터는 기존 `.gitignore` 규칙대로 Git에 올리지 않는다.
- 저장된 Joblib 모델이 생성된 `scikit-learn 1.9.0`을 실행 의존성에도 고정한다. 모델·ML 산출물·기획서 생성 코드·MySQL 레코드는 변경하지 않는다.
- MySQL 30개 테이블과 필수 migration 28개를 확인하고, 실행 중 생성 작업이 0건일 때 portable dump를 만든다. Python 구문 검사, 전국 데이터 파이프라인 40개, frontend 21개 검사와 lint/build, 임시 서버 health·지역 250개·강남 대시보드 조회를 통과했다.
- AI 전체 검사는 505개 중 483개 통과, 1개 skip, 22개 미통과다. 미통과 항목은 현재 PPT v46/16장 출력과 예전 v20/14장 계약 검사의 불일치이며, 백업과 TP2-4의 관련 런타임·검사 파일은 동일하다. 실제 신규 LLM 기획서 생성은 실행하지 않았다.

### D-190 입력 준비와 기획서 품질 검증 분리 및 회귀 검사 정비 (2026-09-17)

- 250개 지역의 파일·모델·SQL·공식 사례 입력 감사는 통과했지만 이를 전체 기획 품질 승인으로 표시하지 않는다. 초록색은 입력자료 준비다. 감사 조회 실패와 파일/SQL/신선도 무효화 시 이전 확인 상태를 재사용하지 않는다.
- 최소 운영비 미만의 0원 미편성 견적에서 실행 가능 플래그를 false로 하고 최소 필요 예산과 미편성 이유를 반환한다. 정상 운영량·계획 목표·견적 산식과 ML 원본은 유지한다.
- 현재 승인 양식 v46/운영량 기반 목표에 맞춰 예전 검사 계약을 갱신한다. 차트/표 수치·전체 페이지 순서·출처·일정·원본 보존 검증은 유지한다. 감사 CLI의 TP2-4 기본 AI 포트는 8212이고 주소 재정의가 가능하다.
- D-189의 검사 합계/원인 설명은 정정한다. 9월 17일 수정 전 실행 로그는 505개 중 481 통과, 17 실패, 6 오류, 1 skip이며 포트/예산 계약 차이도 포함한다. 수정 후 검증은 별도 기록한다.
- 사용자가 대전 서구 동일 조건의 새 기획서 1건 생성을 승인했다. 기존 기획서는 덮어쓰지 않고, 실제 유료 호출 가능성이 있는 요청은 1회만 전송했다. 실패 시 추가 전체 생성은 재승인 없이 실행하지 않는다. 원자료/모델/백업/기존 MySQL 보고서는 수정하지 않는다.
- 최종 검증 결과와 남은 출처 증빙/ML 성능/공모전 양식 한계는 [후속 검증 기록](SUBMISSION_VALIDATION_20260917.md)에 남긴다. 테스트 통과나 서버 연결만으로 최종 제출 준비 완료를 선언하지 않는다.
- 실제 Word 점검에서 발견한 한 문장짜리 넘침은 목표 산출근거 문단 간격만 좁혀 해결한다. 본문·수치·출처·승인 디자인을 보존한다. 출력 과정 설명은 저장 ML 비교 캐시가 없을 때의 계산을 명시하며 Word 캐시 버전은 v19다.
- 이미 메모리에 완성된 Word/PPT를 바이너리 줄 단위로 순회하지 않고 바이트 응답으로 전송한다. 세 다운로드 경로의 바이트·MIME·파일명·캐시 무재생성을 검사한다. 활성 기획서 생성 종료 전에는 서버에 새 코드를 적용하려고 재시작하지 않는다.
- 실제 대전 서구 1건은 약 93분 후 생성됐지만 78점·미승인이다. 승인 플래그를 바꾸거나 두 번째 유료 생성을 시작하지 않는다. KPI·실행월·혼합 사업 분류·지역 적용 조건의 정합성은 보완 대상으로 명시한다.
- 실생성 출력 결함: 축약된 solution으로 선정 후보를 재분류하지 않고 상세 mechanism의 유효한 선정 출처를 표시한다. 긴 Markdown URL은 본문 대신 링크·노트에 보존하며 사례 카드 맞춤 글자 크기를 테마가 다시 키우지 않는다. Word 마지막 사례 뒤 빈 spacer도 제거한다. 최종 출력 버전은 Word v20/PPT v47이며 원문·선정 결과·ML·품질 검수는 변경하지 않는다.

### D-191 아이디어 중심의 최소 보완과 사용자 직접 생성 (2026-09-17)

- 사용자는 크레딧·시간 절약을 위해 정밀 효과 추정과 반복 전체 검사를 원하지 않으며, 수정 후 웹에서 직접 생성하고 검토받기로 했다. 새 생성·유료/로컬 LLM 호출은 하지 않는다.
- 공통 Qwen/Gemma/OpenAI 작성 지침에 핵심 KPI 2~3개의 정의 예시, 준비와 별도의 실제 운영 개시 일정, 지역상품권/협약의 담당·확인 시점·미충족 대안을 간결하게 추가한다. 미확인 조건을 명시한 기획 제안과 허위 집행 확정을 구분한다. 공식 협약을 확보했다고 주장하거나 승인 조건을 해제하지 않는다.
- 운영 규모의 사업 유형은 출력 사례와 동일하게 선정 후보의 상세 mechanism을 우선한다. '운영 시작'도 명시적 개시 단계로 인식한다. 모니터링뿐인 기존 보고서의 개시월을 임의 추정하지 않으며 신규 작성 지침으로 개시 누락을 예방한다. 기본 산식·ML·사용자 명시 목표·저장 원문은 유지한다. 자동 계획 규모는 실제 선택 유형/일정에 따라 달라질 수 있다.
- 관련 오프라인 검사 62개 중 61개 통과, 제주 로컬 진단 파일 미확보 1개 skip. 새 생성·전체 회귀·문서 재렌더는 생략했다. 마지막 확인 시 8212 AI 서버는 실행 중이 아니므로 다음 시작 시 변경을 로드한다. 서버를 켜거나 기존 작업을 재개하지 않았다.


### D-193 TP2-4 최종 작업 폴더 확정 및 누락 이관 (2026-09-17)

사용자가 TP2-3 작업의 누락 없는 이관을 승인했다. 최신 TP2-4 UI·포트·출력 개선을 보존하며 Gemma 모드와 누락 산출물을 병합했다. 이전 대체 UI는 참고 보관하고 동일 MySQL의 문서 경로 12개를 검증 후 TP2-4로 변경했다. 취소 작업은 재개하지 않았다. [내역](TP2_4_MIGRATION_20260917.md).


### D-194 후보 비교·환급 혜택·사례 표시 보완

TP2-4에서 공통 환급률 미지정 기본 제안을 30%로 조정한다. 사례에서 추정한 효과가 아닌 계획 설정이며, 기존 명시 환급률과 예산 상한을 보존하고 방문·소비 목표를 자동 상향하지 않는다. 저장된 selection_reason을 Word/PPT에 직접 표시하고 신규 비교 지침에 대안별 행동·준비 부담·성과 근거 비교를 명시한다. 수치 성과가 없는 PPT 페이지는 참고 사례 운영 방식으로 표시하고 다른 지역 사진은 실제 지역과 참고 사진임을 명시한다. 출력 캐시를 갱신하며 유료 재생성·서버 재시작은 하지 않는다.


### D-195 공통 제안 사업개요 추가

3번 슬라이드에 사업명·사업개요·운영 흐름 이미지와 3개월 추가 방문 합계·월평균 추가 소비 목표를 추가한다. 기존 전망 페이지는 4번으로 이동하고 개요부터 섹션 번호를 1부터 시작한다. 모든 지역의 저장된 사업과 공유 목표 산식을 사용하며 월평균은 준비 기간 포함 전체 사업 개월 수로 나눈다. PPT 출력 캐시는 v49다.


### D-196 사업개요 전용 테마 이미지

사용자가 선택한 관광객 쿠폰 사용 이미지를 환급 사업에 적용한다. OpenAI로 생성한 환급·야간·숙박·교통·체험·예약 테마 6장을 assets/proposal_business에 영구 저장하고 선정된 사업의 상세 운영 방식으로 선택한다. 지역명 대신 테마 기반 예시 이미지이며 AI 생성임을 표시한다. 매 출력 시 검색·이미지 생성 비용은 발생하지 않는다. 사업개요의 사진 위 운영 박스를 제거하고 다른 슬라이드와 이미지 해시 중복을 검사한다. 출력 캐시는 v50이다.


### D-197 월별 목표·사례 적용 근거 통합 및 사진 캡션 확정

사업개요의 가시 캡션은 내용만 가운데 정렬하고 파란 배경을 문장 너비·줄 수에 맞춘다. D-196의 AI 생성 표시는 사진 메타데이터와 발표 노트에 유지한다. 보수·기준·확대 출력 페이지를 동일한 공유 목표값의 월별 추가 방문·소비 막대그래프로 교체한다. 사례 선정과 적용 근거는 참고사례 페이지의 세 카드로 합치고 중복 페이지를 제거한다. 생성 흐름의 로컬 모델명은 AI로 표시하고 간격을 넓힌다. 전 지역 공통 PPT 생성 코드에 반영하며 출력 캐시는 v52다. 저장 보고서 재출력으로 확인하고 LLM 신규 생성은 하지 않는다.


### D-198 사업개요 사진 내부 캡션과 여백 조정

사업개요의 사진과 설명 패널을 30px 내리고 설명 패널 높이를 408px에서 368px로 줄인다. 파란 캡션은 실제 사진 경계 안쪽 하단에 12px 여백으로 배치하고, 문장 너비·줄 수에 맞춰 크기를 조절하며 가로·세로 가운데 정렬한다. 사진 원본 비율과 목표 수치는 유지한다. 전 지역 공통 PPT 생성 캐시는 v53이다.


### 사업기간 자동 선택 규칙 (2026-09-17)

한국 시간 기준 생성일이 1~15일이면 다음 달부터, 16일~말일이면 다다음 달부터 연속 3개월로 정한다. 예: 2026-09-15 → 2026-10~12, 2026-09-16 → 2026-11~2027-01. 웹 입력·대시보드와 신규 보고서 API는 같은 규칙을 사용하고, 서버가 생성 요청 시 날짜를 확정한다. ML은 종료월까지 실제 월별 전망을 계산하고 PPT·Word·웹 목표는 저장된 같은 기간을 사용한다. 기존 저장본·진행 중 작업을 현재 날짜로 이동하지 않는다. 신규 본문은 첫 사업월 운영 개시를 명시하며 실제 준비일에는 운영량을 배분하지 않는다.


### D-199 사업개요의 정렬·설명 보강

사업 테마 사진 캡션 끝에 예시안을 붙인다. 사진과 사업개요를 아래 목표 카드와 동일한 674px 폭·좌우 위치에 정렬한다. 저장된 해결 방법과 실행 작업을 연결하여 5~6줄 내외의 사업개요를 구성하고 숫자나 조건을 새로 만들지 않는다. 제목·본문·목표 글자를 소폭 줄이고 두 목표 카드에 옅은 파랑·민트 배경을 적용한다. 모든 지역 공통 PPT 캐시는 v54다.


### D-200 전체 PPT 헤딩과 사례 흐름 정리

개요 두 페이지를 지역 전망→제안 사업 순서로 바꾸고 1.1/1.2도 맞춘다. 모든 콘텐츠 페이지의 섹션 헤딩은 17.3pt, 제목은 30pt로 통일한다. 운영 사례 페이지의 불필요한 설명 헤딩을 제거하고 사진과 이용 흐름을 확대한다. 운영 규모 카드의 글자를 줄이고 사례와의 차이 문구를 제거하며 참여·추가 방문 계산을 평이한 문장으로 제시한다. 파이프라인의 클릭 아이콘을 빨강으로, 출력을 P/W 문서 아이콘으로 표시한다. 공통 출력 캐시는 v55다.


### D-201 목차 글자 크기 확정

목차의 1~7번 번호와 항목명을 모두 실제 PowerPoint 20pt로 통일한다. 기존 배치·색상·다른 페이지는 유지하며 전 지역 공통 출력 캐시는 v56이다.


### D-202 참고사례 카드 강조

첨부 이미지의 참고사례 카드 3개는 상단을 20px 위로 확장하고, 첫 적용 사례 카드 전체 배경을 옅은 하늘색으로 표시한다. 내용·사진·번호 위치와 카드 하단은 유지한다. 전 지역 공통 PPT 캐시는 v57이다.


### D-203 KPI 근거 본문 글자 크기

첨부 예시를 기준으로 목표 KPI 산출근거 페이지의 설명은 16pt, 산식 설명은 15pt, 목표 숫자는 27pt로 소폭 축소한다. 카드 제목·강조 문구도 18~20pt로 맞춘다. 공통 섹션·페이지 제목 17.3/30pt와 회색 도입문은 유지한다. 모든 지역 출력 캐시는 v58이다.


### D-204 전망 비교 카드 내부 디자인 확정

전망 페이지의 기존 사진·헤딩·외곽 카드 크기와 위치를 유지한다. 카드 내부만 아이콘·합계·비교 그래프·격차 배지로 구성하고 숫자 오른쪽에 높음/낮음을 같은 줄로 표시한다. 숫자 옆 p는 제거하되 퍼센트포인트 차이라는 의미를 페이지 하단에 명시한다. 실제 차트 데이터·합계·비교 차이는 변경하지 않는다. 공통 PPT 캐시는 v59다.


### D-205 전 시군구 최종 PPT 양식 승인

사용자가 승인한 전망 카드 내부 변경 및 D-200~204의 기존 페이지 수정을 전 시군구 공통 최종 양식으로 확정한다. 실제 다운로드 진입점은 proposal_presentation → proposal_presentation_v4이며 마지막에 proposal_final_polish를 적용한다. 캐시 버전은 pptx-v60-approved-regional-template로 갱신하여 이전 디자인의 문서 캐시가 재사용되지 않게 한다. 섹션 헤딩 17.3pt, 페이지 제목 30pt, 목차 번호·항목 20pt와 공통 좌표·카드 배치를 유지한다. 지역별 수치·출처·이미지·문장만 저장 보고서에서 공급하며 긴 본문은 공통 맞춤 규칙으로 처리한다. 개별 지역 보고서를 공통 데이터로 복제하지 않는다.


### D-206 품질검토 상태 공개와 Word 연속 배치

기획안 화면은 quality_review의 실제 승인 여부·점수·보완 권고를 표시한다. 미승인 또는 최종 감사 미완료 보고서를 초록색 승인 상태로 표시하지 않는다. Word에서는 KPI 근거와 견적의 마지막 문장만 다음 페이지에 고립되지 않도록 참고 사례와 생성 과정의 강제 페이지 나눔을 제거하며, 출력 캐시는 strategy-docx-v22-pagination으로 갱신한다. 생성 진행 문구는 특정 로컬 모델명에 고정하지 않고 AI로 표시한다.


### D-207 환급형 시범사업의 기존 수단 활용 견적

지역상품권 환급 시범사업은 신규 앱 구축 대신 기존 지역화폐·웹 신청 수단을 설정해 쓰는 범위로 견적한다. 신청·운영 설정 300만원, 참여처 안내·온라인 홍보 200만원, 정산 원장 집계 100만원의 기획 단가를 적용하고 현장 정산·환급 지원·예비비는 기존 산식을 유지한다. 추가 소비 목표와 사업비는 서로 다른 값으로 보존하며 문서에는 단순 계획 비교 비율을 함께 표시한다. 이는 세수·사업 수익률이나 확정 견적이 아니다. 운영 산식 버전은 operating-capacity-v6-lean-refund-pilot, Word 캐시는 strategy-docx-v23-lean-refund-pilot, PPT 캐시는 pptx-v61-lean-refund-pilot이다. PPT의 승인된 디자인과 좌표는 변경하지 않는다.


### D-208 계절 기준선 0% 설명 (D-210으로 대체)

이전에는 검증 결과에 따라 전년 동월 기준선을 전망으로 사용할 수 있었으나, D-210에서 이 정책을 폐기했다.


### D-209 방문·소비 핵심 지표의 학습모델 전망 (D-210으로 확대)

방문자 수와 관광소비액에 먼저 적용했던 학습모델 강제 사용 정책을 D-210에서 7개 지표 전체로 확대한다.


### D-210 모든 지표의 학습모델 전망

방문자 수·관광소비액·숙박일수·숙박방문자 비율·체류시간·내비게이션 검색량·숙박 검색량의 7개 전망은 모두 학습된 모델로 산출한다. 전년 동월 값은 Validation·Test 성능 비교 기준으로만 사용하며 최종 전망값으로 복사하지 않는다. 저장 산출물에 학습모델이 하나라도 없으면 전년값으로 대체하지 않고 재학습 필요 오류로 중단한다. 학습모델이 기준선보다 열세이면 below_baseline_on_test 또는 혼합 재귀 성능으로 표시해 탐색적 전망임을 공개한다. 모델 버전은 demand/regional-demand-v3.3-learned-all, 문서 캐시는 strategy-docx-v24-learned-all-forecast와 pptx-v63-learned-all-forecast다.


### D-211 Netlify Drop의 SPA 경로 fallback

프런트는 별도 React Router 의존성 없이 `routes.js`의 경로 계약과 `window.location.pathname`으로 10개 화면을 선택한다. Netlify Drop에서 직접 주소 입력과 새로고침을 지원하기 위해 `frontend/public/_redirects`에 `/* /index.html 200`을 두고 빌드 산출물 `dist/_redirects`로 복사한다. 정적 Netlify 배포가 `/api`, `/ai`, Streamlit `:8501`을 제공한다고 표시하지 않으며 해당 기능은 별도 공개 서버 또는 프록시가 있을 때만 완전히 동작한다.


### D-212 Tailscale Funnel 임시 공개 시연

학원 노트북에서 Backend 8200·AI 8212·Vite 5177을 실행하고 Tailscale Funnel이 5177 하나를 공개한다. 브라우저의 `/api`, `/ai` 요청은 기존 Vite 프록시가 로컬 서버로 전달한다. Vite 허용 호스트는 전체 허용하지 않고 Funnel 도메인인 `*.ts.net`으로 제한한다. Funnel은 발표·검증 중에만 켜고 종료 후 reset하며, 장기 공개·인증 없는 운영 배포로 간주하지 않는다.


### D-213 업무 화면 상단 고정 헤더 여백

서울 테마를 포함한 모든 업무 화면은 고정된 64px 전역 헤더 높이만큼 본문 시작 여백을 확보한다. 상단 단계 안내 바와 지역 제목·페이지 본문이 겹치지 않도록 하며 백엔드, AI 및 문서 생성 로직에는 영향을 주지 않는다.


### D-214 오른쪽 지역 사진형 PPTX 표지

모든 시군구 PPTX 표지는 승인된 남색·민트 디자인과 제목·날짜·사업기간을 유지하고, 지역 사진을 마지막 페이지와 같은 오른쪽 세로 영역에 배치한다. 표지 지역명은 시도까지만 줄이지 않고 선택된 전체 시군구명으로 표시한다. 첨부 승인본의 흰색 띠와 회색 사선 장식도 동일하게 재현하고, 지역명은 검정색으로 띠의 시각적 세로 정중앙에 배치한다.

### D-215. 한 PPTX 안에서 같은 사진을 다른 슬라이드에 재사용하지 않는다

표지·맺음말·지역 전망·참고 사례·적용 공간·사업개요의 사진은 이미지 파일 해시로 비교한다. 표지와 맺음말 사진은 이미 본문에 쓰인 사진을 후보에서 제외하고, 사례 카드의 중복 사진은 두 번째 카드부터 사용하지 않는다. 최종 저장 직전에도 모든 사진 해시를 검사하여 중복 문서 출력을 차단한다. 출력 캐시는 `pptx-v71-distinct-slide-images`로 갱신한다.

### D-216. 결과 설명 목록을 실제 기획서 미리보기로 교체한다

기획안 결과 화면의 장문 설명 카드는 제거하고, 최종 PowerPoint와 동일한 생성 입력을 PDF로 변환해 페이지별로 확인하는 미리보기를 표시한다. 기본 화면은 페이지 번호와 문서 뷰어를 제공하고, 전체보기에서는 왼쪽 페이지 목록·가운데 기획서·오른쪽 기존 AI 챗봇을 함께 제공한다. 변환은 서버의 LibreOffice 또는 Windows PowerPoint를 사용하며 변환 결과는 PPTX 내용 해시로 캐시한다. 이 과정은 새 LLM 호출이나 보고서 수치 변경을 수행하지 않는다.

### D-217. 기획안 생성·수정 자동 저장과 미리보기 중심 결과 화면

기획안은 생성 작업 완료 시 기존 서버 흐름에서 MySQL과 공용 저장기록 게시판에 자동 등록한다. 별도의 `기획안 저장하기` 버튼과 중복 제목 영역은 제거하며, 챗봇 수정안도 적용 즉시 같은 MySQL 기록에 반영한다. Word·PowerPoint 다운로드는 미리보기 헤더의 `전체보기` 왼쪽에 배치한다. 브라우저 캐시가 비어 있어도 선택 지역의 최신 MySQL 저장본을 다시 불러와 미리보기를 복원한다. 미리보기 POST의 전체 본문이 저장 원문과 일치할 때 생성 완료 시 보관한 같은 버전 PPTX를 재사용한다.


### D-218. 최종 배포 전 오프라인 점검과 관리자 실행 안내 (2026-09-20)

TP2-4만 수정한다. AI Router는 서버의 effective_routes를 읽어 조사 → 비교 → 작성 → 검수 → 자동 저장/출력 파이프라인, 입력·출력·소스 위치, 프롬프트·문맥·시간·호출 제한과 실행 명령을 설명한다. GET /ai/v1/llm/overview는 외부 모델 접속 없이 현재 설정을 반환하며 자동 갱신에 사용한다. 실제 연결 확인은 명시적인 버튼으로 분리한다. 미저장 관리자 설정은 갱신으로 덮지 않는다.

자동 저장은 보고서별 순차 처리하고 저장 실패를 성공과 구분한다. 생성 작업의 persistence_status는 saved/failed/unknown이다. PDF 미리보기는 보고서 본문+PPT 렌더 버전으로 캐시하고 변환 대기는 제한한다. 기본/전체보기 챗봇은 동일한 대화·진행 상태를 공유하며 중복 전송을 방지한다. PowerPoint 변환은 사용자가 열어둔 다른 문서를 닫지 않는다. 승인된 PPT/Word 디자인, ML 전망, 목표 산식은 이번 정비로 바꾸지 않는다.

실제 모델 생성 품질은 오프라인 검사로 확정하지 않는다. 상세 결과와 남은 실생성 확인은 [최종 점검 기록](FINAL_OFFLINE_REVIEW_20260920.md)을 따른다.

### D-219. 기획서 수정 이력·웹 조사 경계와 관리자 화면 잠금 (2026-09-20)

기획서 미리보기는 생성 직후 원본을 브라우저 편집 기준으로 보관하고, 챗봇 수정 직전 상태를 최대 20개까지 쌓아 `한 단계 되돌리기`와 `처음으로 리셋`을 제공한다. 복원 결과도 기존 MySQL 자동 저장 큐를 사용하며 관측값·ML·공식 사례 수치의 수정 허용 범위는 바꾸지 않는다. 생성·수정·미리보기 변환에는 움직이는 진행 표시를 사용한다.

기획안 챗봇의 웹 검색 OFF는 관리자 Router의 `chat_explain`·`chat_revise` 경로를 사용하며 현재 `local_first_gemma`에서는 Gemma가 담당한다. 웹 검색 ON은 `chat_research`를 통해 OpenAI 공식 웹 검색을 사용하되 선택 지역명과 현재 질문만 전송하고, 저장 보고서 원문·사업 여건·과거 대화는 전송하지 않는다. 조사 결과는 자동 수정안으로 적용하지 않으며 수정은 웹 검색을 끈 뒤 로컬 경로에서 수행한다.

관리자 React 화면은 `/admin-login`의 교육용 세션 잠금 뒤에 둔다. 계정은 수업 시연용 고정값이며 브라우저 `sessionStorage`만 사용하는 화면 접근 장치다. 실제 외부 배포 보안 인증이나 Backend 관리자 토큰을 대체하지 않는다.

PPT 표지의 전체 시군구명은 흰 리본과 같은 세로 좌표를 사용한다. 선정 사례 사진이 다음 장의 실적 사진과 중복되면 빈칸으로 제거하지 않고, 실제 사례 사진이 아님을 명시한 전용 AI 생성 운영 예시 이미지를 같은 카드 규격으로 대체한다. 출력 캐시는 `pptx-v72-centered-cover-unique-case-fallback`이다.
### D-220 적용 사례 사진의 두 장 일치와 사진 선택 우선순위 (2026-09-20)

- `2.1 지역별 참고 사례`의 적용 사례와 `2.2 적용 사례 운영 방식`은 같은 사례를 설명하므로 동일한 사진과 출처를 의도적으로 재사용한다. 이 두 개체(`case-photo-0`, `result-photo`)만 사진 중복 검사에서 예외로 두고 표지·제안 사업·후보 사례 등 다른 사진 중복은 계속 차단한다.
- 사진은 `해당 사례의 확인된 공식·기사·홈페이지 사진 → 같은 운영 방식의 타지역 참고 사진 → 실제 현장 사진이 아님을 밝힌 생성 이미지` 순서로 선택한다. 관련성이 없는 일반 관광 사진으로 사례를 대신하지 않는다.
- 타지역 참고 사진은 지역명과 출처를 표시하고, 생성 이미지는 `AI 생성 이미지 · 실제 사례 현장 사진 아님`을 표시한다. 2.2 헤딩은 모든 제안서에서 `적용 사례 운영 방식`으로 통일한다.
- D-219의 선택 사례 중복 사진을 생성 이미지로 치환하던 규칙은 이 결정으로 대체한다. 렌더 버전은 `pptx-v73-matched-selected-case-photo`로 갱신한다.

### D-221 전국 행정지역 선택의 공통 코드 기준 (2026-09-23)

- Dashboard·Signup·My Page의 시도/시군구 선택은 기존 Backend 경계 API(`/api/v1/boundaries/sido`, `/api/v1/boundaries/sigungu?sido_code=...`)를 공통 기준으로 사용한다. 모델 준비 지역만 반환하는 `/ai/v1/regions/catalog`는 전국 행정지역 선택 목록으로 사용하지 않는다.
- Frontend 공통 helper는 `region_code`를 문자열로 보존하고 시도 코드·시도명·시군구명·전체 지역명을 구분한다. Signup/My Page는 선택 코드만 화면 state에 유지하며 시도 변경 시 시군구 코드를 비운다.
- 실제 회원 DB/API 연결 전에는 Signup 선택을 My Page에 복사하거나 저장 성공으로 표시하지 않는다. 향후 회원 API는 `region_code`로 지역을 식별하고 카탈로그에서 이름을 역조회한다.

### D-222 TP2-5 선택형 회원 인증 (2026-09-24)

- D-221의 회원 연결 대기 상태를 완료한다. Signup·Login·My Page만 MySQL 회원 API를 사용하며 관광 화면·저장 기획안·OpenAI BYOK는 익명 접근을 유지한다.
- 기존 회원 테이블이 없어 `oligo_members`와 세션 해시 테이블을 별도 SQL로 추가한다. 비밀번호·힌트 답변은 Argon2id 해시만 저장한다. 탈퇴는 이 신규 회원 테이블의 자기 행만 삭제하며 연결된 세션은 FK cascade로 삭제한다.
- 브라우저 인증은 7일 만료의 HttpOnly SameSite=Lax 쿠키이며 Production은 HTTPS 및 Secure 쿠키를 요구한다. 회원 변경 요청은 Origin을 확인한다. BYOK 메모리 세션 및 별도 쿠키는 수정하지 않는다.

### D-223 Oligo World 공개 진입 기반 (2026-09-25)

- `/game`은 인증 없이 접근하는 독립 React route로 둔다. 게임 전용 화면과 CSS는 `frontend/src/GAME/Oligo-world/`에 격리한다.
- My Page 제목 옆과 기획서 생성 중 취소 버튼 옆에서 새 탭으로 `/game`을 연다. 생성·취소·진행 조회와 BYOK 상태는 기존 탭에서 그대로 유지한다.
- 이 단계는 시작 화면만 제공하며 게임 엔진, 점수, 저장, 순위, Backend 또는 DB 연결을 추가하지 않는다.

### D-224 GAME Hub와 저장 없는 Quick Game (2026-09-25)

- `/game`은 공개 GAME Hub로 바꾸고 기존 Oligo World 화면을 `/game/oligo-world`에서 재사용한다. `/game/ladder`는 별도 폴더의 점심메뉴 사다리게임이다. 세 경로 모두 익명 접근을 허용한다.
- 업무 화면에서 Oligo World로 들어가는 두 링크는 새 탭을 유지한다. GAME 내부 링크는 기존 `pushState`/`popstate` 라우팅으로 이동하고 브라우저 뒤로가기를 지원한다.
- 사다리 가로선이 실제 결과 순열을 결정한다. 2~8명, 개별 경로 공개, 전체 결과와 다시 하기를 제공하지만 회원·DB·브라우저 저장소에는 기록하지 않는다.

### D-225 Oligo World 회원별 게임 프로필 (2026-09-25)

- 공개 GAME Hub와 저장 없는 Ladder는 그대로 유지하고, Oligo World RPG만 기존 HttpOnly 회원 세션으로 시작한다. 익명 사용자는 로그인 안내를 본다.
- `007_oligo_game_profile.sql`은 회원당 한 행의 `oligo_game_profiles`를 별도로 추가한다. DRAFT 설정은 서버에 자동 저장하고 ACTIVE는 생성 완료 후 map001 진행을 저장한다. 회원 지역은 최초 제안값이며 게임 지역 코드는 독립 보존한다.
- 게임 닉네임은 2~16자 한글·영문·숫자·밑줄로 제한하고 ACTIVE 닉네임만 DB UNIQUE로 관리한다. 미완성 DRAFT는 닉네임을 선점하지 않는다. 여행 스타일은 10개 안정 ID 중 하나만 선택하며 향후 스킬과 연결한다.
- Tutorial 보상 EXP 10/W 10은 서버에서 한 번만 지급한다. 캐릭터·닉네임·지역 변경은 미구현이며 향후 최소 Level(미정)과 5,000 W 비용 정책을 적용할 수 있게 둔다. 실제 MySQL 적용은 별도 환경에서 검증한다.
