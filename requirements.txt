# TP2-3 Python 서버 공통 의존성
# 설치: python -m pip install -r requirements.txt
# React/Vite 의존성은 frontend/package.json에서 npm install로 설치합니다.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
# Ollama가 반환한 구조화 JSON을 Agent 계약과 대조해 잘못된 필드를 다음 단계에 넘기지 않습니다.
jsonschema>=4.23,<5.0
python-dotenv>=1.0,<2.0
# AI 전략기획서 Word(.docx)와 원자료 추세 그래프 생성에 사용합니다.
python-docx>=1.2,<2.0
# 사용자 참고문서의 텍스트만 메모리에서 추출합니다(PDF·Excel). 원본 파일은 서버에 저장하지 않습니다.
pypdf>=6.0,<7.0
openpyxl>=3.1,<4.0
# bid3의 제안서 제작 장점을 관광 기획안에 적용하는 PowerPoint(.pptx) 출력입니다.
python-pptx>=1.0,<2.0
Pillow>=10.0,<12.0
matplotlib>=3.10,<4.0
# 강남구 월별 방문자·외지인 관광소비 예측의 전처리, 시간순 검증, 저장 모델에 사용합니다.
pandas>=2.2,<4.0
numpy>=1.26,<3.0
scikit-learn==1.9.0
joblib>=1.4,<2.0
# 공식 정책·관광 문서를 지역 필터로 검색하는 영속형 RAG 저장소입니다.
chromadb>=1.0,<2.0
# 생성 기획안 기록과 Word/PPT 파일 경로를 MySQL에 저장합니다.
PyMySQL>=1.1,<2.0
argon2-cffi>=23.1,<26.0
